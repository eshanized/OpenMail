---
phase: 04-organization-intelligence
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - app/Models/MessageMetadata.php
  - app/Services/SearchService.php
  - app/Livewire/Mailbox/SearchBar.php
  - app/Livewire/Mailbox/SearchResults.php
  - app/Http/Controllers/SearchController.php
  - resources/views/livewire/mailbox/search-bar.blade.php
  - resources/views/livewire/mailbox/search-results-dropdown.blade.php
  - resources/views/livewire/mailbox/search-results-page.blade.php
  - routes/web.php
  - tests/Feature/SearchTest.php
  - tests/Feature/SearchIndexingTest.php
  - tests/Unit/SearchHighlightTest.php
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03, SRCH-04, SRCH-05, SRCH-06, SRCH-07]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Full-text search works across subject, sender, recipients, and body with highlighted results"
    - "Search supports folder filter, date range, attachment presence, read/unread, flagged filters"
    - "Instant search dropdown shows results within 300ms debounce (D-08)"
    - "Search results page has two-column layout with filter sidebar and paginated results"
    - "Scout database engine indexes MessageMetadata on save/sync in real-time (D-05)"
    - "Search highlighting wraps matches in <mark> tags after DOMPurify sanitization"
  artifacts:
    - "app/Models/MessageMetadata.php (Searchable trait configured)"
    - "app/Services/SearchService.php"
    - "app/Livewire/Mailbox/SearchBar.php"
    - "app/Http/Controllers/SearchController.php"
    - "resources/views/livewire/mailbox/search-bar.blade.php"
    - "resources/views/livewire/mailbox/search-results-dropdown.blade.php"
    - "resources/views/livewire/mailbox/search-results-page.blade.php"
  key_links:
    - "MessageMetadata → Scout index (toSearchableArray, searchableAs per-user)"
    - "SearchService → MessageMetadata Scout queries with filter scopes"
    - "SearchBar (Alpine.js) → SearchService.instantSearch() → dropdown"
    - "SearchController → SearchService.search() → results page"
    - "Search highlighting → DOMPurify sanitization BEFORE mark wrapping (SEC-01)"
---

<objective>
Implement full-text search infrastructure using Laravel Scout with MySQL FULLTEXT database engine. Includes SearchService, instant search dropdown (SearchBar), search results page with filters, and real-time index synchronization on message sync.
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-organization-intelligence/04-CONTEXT.md
@.planning/phases/04-organization-intelligence/04-RESEARCH.md
@.planning/phases/04-organization-intelligence/04-UI-SPEC.md
@app/Services/ImapMailboxService.php
@app/Models/MessageMetadata.php
@app/Livewire/Mailbox/MessageList.php
@config/scout.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scout configuration on MessageMetadata + SearchService + real-time indexing</name>
  <files>
    app/Models/MessageMetadata.php
    app/Services/SearchService.php
    config/scout.php
    tests/Feature/SearchIndexingTest.php
    tests/Unit/SearchHighlightTest.php
  </files>
  <behavior>
    - Test 1: MessageMetadata uses Searchable trait, toSearchableArray includes all D-05 fields (subject, from_address, from_name, to_address, snippet, body_text, message_id, folder_path, date, has_attachments, is_seen, is_flagged)
    - Test 2: searchableAs() returns per-user index name 'message_metadata_{user_id}' (D-05 isolation)
    - Test 3: shouldBeSearchable() returns true
    - Test 4: SearchService.search() applies folder, date range, has_attachment, is_seen, is_flagged, labels filters (SRCH-02, SRCH-03, SRCH-04, SRCH-05)
    - Test 5: SearchService.instantSearch() returns limited results for dropdown (D-08)
    - Test 6: Real-time index sync: after ImapMailboxService sync, calling searchable() on collection updates index (SRCH-07)
    - Test 7: Highlighting: SearchHighlightTest verifies matches wrapped in <mark class="bg-yellow-100 text-yellow-900 px-0.5 rounded"> AFTER DOMPurify sanitization (SRCH-06, SEC-01)
  </behavior>
  <action>
    1. Configure Scout in config/scout.php: driver = 'database', prefix = 'openmail_', ensure database engine uses MySQL FULLTEXT.

    2. Extend MessageMetadata per RESEARCH.md Pattern 2:
       - Use Searchable trait
       - #[SearchUsingFullText(['subject', 'from_address', 'from_name', 'to_address', 'snippet', 'body_text'])]
       - #[SearchUsingPrefix(['message_id'])]
       - toSearchableArray() returns all fields needed for search + filtering
       - searchableAs() = 'message_metadata_' . $this->user_id (per-user isolation)
       - shouldBeSearchable() = true

    3. Implement SearchService per RESEARCH.md Pattern 2:
       - search(userId, query, filters, perPage) → LengthAwarePaginator with MATCH...AGAINST relevance + date DESC ordering (D-07)
       - instantSearch(userId, query, limit) → Collection for dropdown (D-08)
       - applyFilters() handles: folder, date_from, date_to, has_attachment, is_seen, is_flagged, labels (whereHas)

    4. Hook into ImapMailboxService sync: after folder sync saves MessageMetadata collection, call $messages->searchable() to update index. Add SearchIndexShouldBeUpdated to limit reindexing.

    5. Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd /home/snigdha/Desktop/OpenMail && ./vendor/bin/pest tests/Feature/SearchIndexingTest.php tests/Unit/SearchHighlightTest.php 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or "0 passed" in summary, or any test shows "FAIL"</fails_when>
  </verify>
  <done>
    MessageMetadata is Scout-searchable with per-user indexes. SearchService handles all filters. Real-time indexing works on folder sync. Highlighting wraps matches in <mark> after sanitization.
  </done>
</task>

<task type="auto">
  <name>Task 2: SearchBar Livewire component with instant dropdown (Alpine.js)</name>
  <files>
    app/Livewire/Mailbox/SearchBar.php
    resources/views/livewire/mailbox/search-bar.blade.php
    resources/views/livewire/mailbox/search-results-dropdown.blade.php
    tests/Feature/SearchTest.php
  </files>
  <action>
    Create SearchBar Livewire component per UI-SPEC §1.1 and RESEARCH.md code example.

    1. SearchBar.php: 
       - Properties: query (string), results (Collection), loading (bool)
       - Method instantSearch(): debounced call to SearchService.instantSearch(auth()->id(), $this->query, 8)
       - Method openResult(result): redirect to message view (wire:navigate to /mailbox/{folder}/{uid})
       - Keyboard shortcut: '/' focuses input (Alpine.js handles in blade)

    2. search-bar.blade.php per UI-SPEC §1.1:
       - Fixed top toolbar, full width minus sidebar
       - Input with search icon, placeholder "Search all mail (shortcut: /)"
       - Focus ring: blue-500
       - Loading spinner in input right side
       - Alpine.js x-data: query, results[], loading, open, debounce timer (300ms per D-08)
       - @input debouncedSearch(), @keydown.escape close, @keydown.arrow-down focus first, @focus open if query>=2, @blur delayed close

    3. search-results-dropdown.blade.php per UI-SPEC §1.1:
       - Absolute positioned, max-h-96 overflow-auto, z-50, shadow-xl
       - Max 8 results with sender avatar initial, from_display, subject (highlighted), snippet (highlighted), folder badge, date
       - Arrow key navigation (highlightedIndex), Enter opens, Escape closes
       - Footer: "View all results →" link to /search?q={query} with wire:navigate

    4. Register component in AppServiceProvider or auto-discovery. Include in mailbox layout toolbar.

    5. Add feature tests: testInstantDropdown, testKeyboardNavigation, testViewAllResultsLink.
  </action>
  <verify>
    <automated>cd /home/snigdha/Desktop/OpenMail && ./vendor/bin/pest tests/Feature/SearchTest.php::testInstantDropdown 2>&1 | tail -15</automated>
    <fails_when>non-zero exit, or "0 passed" in summary, or test shows "FAIL"</fails_when>
  </verify>
  <done>
    SearchBar renders in mailbox toolbar. Typing shows dropdown with up to 8 highlighted results within 300ms. Keyboard navigation works. "View all results" navigates to search page.
  </done>
</task>

<task type="auto">
  <name>Task 3: Search results page with filter sidebar + pagination</name>
  <files>
    app/Http/Controllers/SearchController.php
    resources/views/livewire/mailbox/search-results-page.blade.php
    routes/web.php
    tests/Feature/SearchTest.php
    tests/Feature/SearchResultsPageTest.php
  </files>
  <action>
    Create search results page per UI-SPEC §1.2.

    1. SearchController.php:
       - index(Request $request): validates query, extracts filters (folder, date_from, date_to, has_attachment, is_seen, is_flagged, labels), calls SearchService.search(), returns view with results + filters
       - Handles empty query (redirect to mailbox)

    2. search-results-page.blade.php per UI-SPEC §1.2:
       - Two-column layout: filter sidebar (280px, lg:w-72) + results list (flex-1)
       - Filter sidebar: Folder select (all user folders), Date range presets (Today, Week, Month, Year, Custom), Has attachment toggle, Read/Unread/Flagged radios, Label multi-select (checkboxes)
       - Active filters displayed as removable chips above results (click X to remove)
       - Results: same row structure as message list but with highlighted snippets (mark tags)
       - Pagination: Laravel paginator links at bottom
       - Empty state: centered illustration + copy from UI-SPEC Copywriting Contract

    3. Add route: GET /search → SearchController@index, name 'search'

    4. Wire "View all results" link in dropdown to this route with query preserved.

    5. Add feature tests: testFolderFilter, testDateRangeFilter, testAttachmentFilter, testReadUnreadFilter, testResultsPageLayout, testActiveFilterChips, testPagination.
  </action>
  <verify>
    <automated>cd /home/snigdha/Desktop/OpenMail && ./vendor/bin/pest tests/Feature/SearchTest.php::testFilters tests/Feature/SearchResultsPageTest.php 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or "0 passed" in summary, or any test shows "FAIL"</fails_when>
  </verify>
  <done>
    /search page renders with two-column layout. All filters work (folder, date range, attachment, read/unread, flagged, labels). Active filters show as removable chips. Results paginated. Highlighted snippets in results. Empty state shows correct copy.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Search Query → Backend | Search query string from user input; must be sanitized for MySQL FULLTEXT boolean mode |
| Search Results → Client | Message snippets rendered in dropdown/page; must be sanitized (DOMPurify) before highlight injection |
| Filter Parameters → Backend | Folder, date, label filters from URL params; validated server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Injection | Search query (FULLTEXT boolean mode) | high | mitigate | Escape boolean operators (+ - > < * ") in user query before passing to MATCH...AGAINST; use parameter binding |
| T-04-07 | XSS | Search snippet highlighting | high | mitigate | Apply DOMPurify to snippet BEFORE wrapping matches in <mark>; never trust raw message content |
| T-04-08 | Info Disclosure | Search results cross-user | medium | mitigate | SearchService always scopes to authenticated user_id; per-user Scout index isolation |
| T-04-09 | DoS | Expensive search queries | low | mitigate | Scout database engine paginates; limit instantSearch to 8 results; max page size 50 |
| T-04-10 | Tampering | Filter parameter manipulation | low | mitigate | Validate folder exists for user; date ranges sanitized; label IDs verified owned by user |
</threat_model>

<verification>
Run `./vendor/bin/pest tests/Feature/SearchTest.php tests/Feature/SearchIndexingTest.php tests/Unit/SearchHighlightTest.php tests/Feature/SearchResultsPageTest.php` — all tests must pass.
Verify Scout config: `php artisan tinker --execute="echo config('scout.driver');"` returns 'database'.
</verification>

<success_criteria>
- Scout database engine configured and indexing MessageMetadata per-user
- SearchService handles all 6 filter types (folder, date range, attachment, read/unread, flagged, labels)
- Instant search dropdown shows 8 results within 300ms debounce (D-08)
- Search results page at /search with two-column layout, filter sidebar, pagination
- Search highlighting wraps matches in <mark> AFTER DOMPurify (SRCH-06, SEC-01)
- Real-time index sync on folder sync (SRCH-07, D-05)
</success_criteria>

<output>
Create .planning/phases/04-organization-intelligence/04-02-SUMMARY.md when done
</output>