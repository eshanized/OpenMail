---
phase: 05-security-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Livewire/Settings/SettingsPage.php
  - app/Livewire/Settings/AppearanceTab.php
  - resources/views/livewire/settings/settings-page.blade.php
  - resources/views/livewire/settings/appearance-tab.blade.php
  - resources/views/layouts/app.blade.php
  - resources/css/app.css
  - resources/js/app.js
  - routes/web.php
  - app/Models/Setting.php
  - database/migrations/2026_09_08_000002_add_user_id_to_settings.php
  - config/openmail.php
autonomous: true
requirements:
  - SET-03
  - SET-06
estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - User can toggle theme (light/dark/system) and density (compact/regular/comfortable) in Settings > Appearance
    - Theme/density preferences persist to database and apply immediately without page reload
    - Settings page accessible at /settings with tabbed navigation
    - No theme flash on page load
  artifacts:
    - app/Livewire/Settings/SettingsPage.php with tabbed navigation
    - app/Livewire/Settings/AppearanceTab.php with theme/density controls
    - CSS custom properties for density and dark mode in resources/css/app.css
    - Alpine.js theme/density initializer in resources/views/layouts/app.blade.php
    - Setting model with user_id scoping
  key_links:
    - Theme/density CSS vars on <html> → Livewire component styles → mailbox UI
    - Settings page → Setting model → per-user preferences → immediate UI update
    - Alpine.js initializer → server-rendered settings → zero-flash theme application

---

<objective>
Build the Settings page foundation with tabbed navigation and a fully functional Appearance tab (theme toggle + density picker with live preview). This plan establishes the UI foundation that subsequent plans will extend with Profile, Mail, Security, and Signatures tabs.
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-security-polish/05-CONTEXT.md
@.planning/phases/05-security-polish/05-RESEARCH.md
@.planning/phases/04-organization-intelligence/04-CONTEXT.md
@app/Models/Setting.php
@resources/views/layouts/mailbox.blade.php
@resources/views/layouts/app.blade.php
@config/openmail.php
</context>

<tasks>

<task type="tracer">
  <name>End-to-End Settings Page + Appearance Tab with Live Theme/Density</name>
  <files>
    app/Livewire/Settings/SettingsPage.php
    app/Livewire/Settings/AppearanceTab.php
    resources/views/livewire/settings/settings-page.blade.php
    resources/views/livewire/settings/appearance-tab.blade.php
    resources/views/layouts/app.blade.php
    resources/css/app.css
    resources/js/app.js
    routes/web.php
    app/Models/Setting.php
    database/migrations/2026_09_08_000002_add_user_id_to_settings.php
    config/openmail.php
  </files>
  <action>
    Implement the thinnest vertical slice proving Settings page + Appearance tab work end-to-end with live theme/density updates:

    1. **Settings Page Scaffold (per D-12)**:
       - Create app/Livewire/Settings/SettingsPage.php with tabs: profile, mail, appearance, security, signatures; activeTab state; setTab() method; #[Layout('layouts.mailbox')]
       - Create resources/views/livewire/settings/settings-page.blade.php with tab navigation (role=tablist, aria attributes) and @switch for tab panels
       - Add route GET /settings → SettingsPage::class in routes/web.php (auth middleware)
       - Only Appearance tab panel renders initially; others show "Coming soon" placeholder

    2. **Appearance Tab with Theme/Density Controls (per D-09, D-10, D-11, D-12)**:
       - Create app/Livewire/Settings/AppearanceTab.php with theme (light/dark/system) and density (compact/regular/comfortable) properties loaded from Setting model in mount()
       - On updatedTheme/updatedDensity: persist via Setting::setForUser() and dispatch browser events (theme-changed, density-changed) with debounced 300ms delay
       - Create resources/views/livewire/settings/appearance-tab.blade.php with radio groups for theme/density, live preview section using Alpine.js appearancePreview() component

    3. **Theme/Density CSS + Client Initialization (per D-10, D-11)**:
       - Extend resources/css/app.css: add @theme block with CSS custom properties (--spacing-unit, --font-size-base, --line-height-base, --border-radius) for regular density; .density-compact, .density-comfortable, .density-regular utility classes using @utility; @custom-variant dark (&:where(.dark, .dark *))
       - Update resources/views/layouts/app.blade.php: add @cspNonceMetaTag, inline themeInitializer() Alpine.js component on <html> with x-data/x-init that reads server-rendered user settings via @json(auth()->user()?->setting('theme', 'system')) and applies dark class + density-* class immediately to prevent flash
       - Extend resources/js/app.js to import/initialize theme/density if needed

    4. **Setting Model User Scoping (per D-09)**:
       - If Setting model is currently global-only, create migration add_user_id_to_settings adding user_id column with foreign key and unique index on [user_id, key]
       - Update app/Models/Setting.php: add user_id to $fillable, add user() belongsTo, add scopeForUser($userId) and static getForUser()/setForUser() methods with caching
       - Add scopeSetting($key, $default) helper method to User model for clean access

    5. **Config Extensions**:
       - Add appearance defaults (theme=system, density=regular) to config/openmail.php
  </action>
  <verify>
    <automated>php artisan migrate --force && php artisan test --filter=SettingsTest::test_settings_page</automated>
    <fails_when>non-zero exit, or Settings page not accessible at /settings, or tab navigation broken</fails_when>
    <automated>php artisan test --filter=SettingsTest::test_theme</automated>
    <fails_when>non-zero exit, or theme not persisted to settings table, or CSS vars not applied to <html></fails_when>
    <automated>php artisan test --filter=SettingsTest::test_density</automated>
    <fails_when>non-zero exit, or density CSS vars not reflected in computed styles</fails_when>
    <automated>php artisan test --filter=SettingsTest::test_no_theme_flash</automated>
    <fails_when>non-zero exit, or theme flash detected on page load (DOM snapshot comparison)</fails_when>
  </verify>
  <done>
    Settings page accessible at /settings with tabbed navigation; Appearance tab shows theme/density controls that persist to DB and update UI live via CSS vars; theme flash eliminated via inline Alpine.js initializer; Setting model supports per-user preferences.
  </done>
  <reversibility>
    rating: reversible
    rationale: Settings page is new route/component; Setting model migration adds column; theme/density are CSS-only changes; all can be disabled/removed without data loss
  </reversibility>
</task>

<task type="auto">
  <name>Appearance Tab Polish & Live Preview Enhancement</name>
  <files>
    app/Livewire/Settings/AppearanceTab.php
    resources/views/livewire/settings/appearance-tab.blade.php
    resources/css/app.css
    resources/views/layouts/mailbox.blade.php
  </files>
  <action>
    Polish the Appearance tab with enhanced live preview and accessibility:

    1. **Live Preview Enhancement** (per D-10, D-11):
       - Enhance appearance-tab.blade.php live preview to show actual message row density changes (reuse message-row component styles via @apply)
       - Add transition animations for theme/density changes (Alpine.js x-transition)
       - Ensure radio button selection updates are debounced (Alpine.js $watch with 300ms debounce) before Livewire persist call

    2. **Accessibility**:
       - Proper labels for radio groups (fieldset/legend)
       - Keyboard navigation between theme/density options
       - ARIA attributes on tab list (role=tablist, aria-selected, aria-controls)
       - Focus indicators for all interactive elements

    3. **CSS Density Integration - Mailbox Container**:
       - Update resources/views/layouts/mailbox.blade.php to bind density class on main container: :class="densityClass"
       - Add densityClass computed property to themeInitializer() Alpine.js component
       - Verify density-* utility classes apply to mailbox container and cascade to child components

    4. **Setting Defaults & Fallback**:
       - Ensure guest users (not authenticated) fall back to localStorage for theme/density persistence
       - Test: unauthenticated visit → change theme → refresh → theme persists via localStorage
       - Test: login as user with saved preferences → preferences override localStorage
  </action>
  <verify>
    <automated>php artisan test --filter=SettingsTest::test_appearance_tab</automated>
    <fails_when>non-zero exit, or Appearance tab not accessible, or live preview not updating</fails_when>
    <automated>php artisan test --filter=SettingsTest::test_density_mailbox</automated>
    <fails_when>non-zero exit, or density class not applied to mailbox container, or CSS vars not cascading</fails_when>
    <automated>php artisan test --filter=SettingsTest::test_accessibility</automated>
    <fails_when>non-zero exit, or ARIA attributes missing, or keyboard navigation broken</fails_when>
    <automated>php artisan test --filter=SettingsTest::test_guest_fallback</automated>
    <fails_when>non-zero exit, or guest theme/density not persisted to localStorage</fails_when>
  </verify>
  <done>
    Appearance tab polished with accessible controls and live preview; density CSS vars applied to mailbox container and cascading; guest fallback to localStorage works; authenticated user preferences take precedence.
  </done>
</task>

<task type="auto">
  <name>Settings Page Layout Integration & Tab State Management</name>
  <files>
    app/Livewire/Settings/SettingsPage.php
    resources/views/livewire/settings/settings-page.blade.php
    resources/views/layouts/mailbox.blade.php
    resources/css/app.css
  </files>
  <action>
    Complete Settings page layout integration and tab state management:

    1. **Settings Page Layout Integration**:
       - Ensure Settings page inherits sidebar/navigation from mailbox layout for consistency
       - Add breadcrumb/back-link to mailbox in Settings page header
       - Update mailbox.blade.php to conditionally render SettingsPage when on settings route (or use dedicated settings layout extending mailbox layout)

    2. **Tab State Management**:
       - Add wire:navigate support for SPA-like tab switching (no full page reload)
       - Preserve activeTab in URL query parameter (?tab=appearance) for deep linking
       - On tab switch: dispatch Livewire event to save current tab state before switching

    3. **Placeholder Tabs for Future Plans**:
       - Profile, Mail, Security, Signatures tabs render "Coming in next update" placeholder with disabled state
       - Tab buttons for these tabs are visible but non-interactive (aria-disabled)
       - Ensures tab navigation structure is complete for Plan 05-05

    4. **Mobile Responsiveness**:
       - Tab navigation becomes horizontally scrollable on small screens (overflow-x-auto)
       - Tab panels stack vertically on mobile
       - Ensure appearance-tab live preview works on mobile viewport
  </action>
  <verify>
    <automated>php artisan test --filter=SettingsTest::test_layout_integration</automated>
    <fails_when>non-zero exit, or Settings page doesn't inherit mailbox layout, or breadcrumb missing</fails_when>
    <automated>php artisan test --filter=SettingsTest::test_tab_state</automated>
    <fails_when>non-zero exit, or tab state not in URL, or wire:navigate not working</fails_when>
    <automated>php artisan test --filter=SettingsTest::test_mobile_responsive</automated>
    <fails_when>non-zero exit, or tabs not scrollable on mobile, or preview broken</fails_when>
  </verify>
  <done>
    Settings page fully integrated with mailbox layout; tab state managed via URL; wire:navigate enables SPA-like switching; placeholder tabs ready for future expansion; mobile responsive.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Settings UI | Authenticated user modifies own preferences |
| Browser → Theme/Density Initializer | Client-side Alpine.js reads server-rendered settings |
| Laravel → Database | Trusted application writes user preferences |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Tampering | Theme/density persistence | low | accept | User controls own preferences via authenticated Livewire component; Setting model scopes to user_id |
| T-05-08 | Elevation of Privilege | Settings access control | low | mitigate | Settings routes behind auth middleware; Livewire components check auth()->check(); user_id scoping on Setting model |
| T-05-09 | Denial of Service | Settings page DoS | low | accept | Rate limited by api.settings limiter (Plan 05-03) |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Package legitimacy audit completed; all packages [OK]; no new installs in this phase |

</threat_model>

<verification>
Overall phase verification for Plan 2:
- Settings page at /settings renders with tabbed navigation
- Appearance tab: theme toggle (light/dark/system) and density picker (compact/regular/comfortable) work
- Preferences persist to settings table per user
- Theme/density apply immediately via CSS vars on <html> without page reload
- No theme flash on page load (inline initializer in app.blade.php)
- Mailbox container applies density class and CSS vars cascade to child components
- Guest users fall back to localStorage
</verification>

<success_criteria>
- All tracer task verify commands pass
- Settings page Appearance tab fully functional with live preview
- Theme/density CSS vars drive mailbox UI spacing/typography
- Settings page layout integrated with mailbox navigation
</success_criteria>

<output>
Create .planning/phases/05-security-polish/05-02-SUMMARY.md when done
</output>