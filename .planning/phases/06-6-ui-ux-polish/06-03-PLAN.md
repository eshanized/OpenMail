---
phase: 06-6-ui-ux-polish
plan: 03
type: execute
wave: 2
depends_on: [06-02]
files_modified:
  - resources/css/app.css
  - resources/views/layouts/app.blade.php
  - resources/views/layouts/mailbox.blade.php
  - resources/views/livewire/mailbox/folder-sidebar.blade.php
  - resources/views/livewire/mailbox/message-row.blade.php
  - resources/views/livewire/mailbox/search-results-dropdown.blade.php
  - resources/views/livewire/mailbox/label-chips.blade.php
  - resources/views/livewire/mailbox/message-toolbar.blade.php
  - resources/views/livewire/mailbox/composer.blade.php
  - resources/views/components/composer-recipient-chips.blade.php
autonomous: true
requirements: [D-01, D-04, D-05, D-06, D-07, D-08, D-10]
user_setup: []

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - Text inputs use the glass pattern — translucent fill, backdrop blur at the v4 small scale, focus ring on a 600-level stop — with a solid near-opaque fallback when backdrop-filter is unsupported or reduced-transparency is requested (D-06, research Pattern 4)
    - Cards and the mailbox sidebar use the glass-card treatment (translucent fill + blur + subtle border) over a fixed decorative blob layer so the blur is actually visible on flat cream/soft-dark surfaces (D-07, research Pitfall 4)
    - Status badges (unread pill, overflow pill) use the soft-tag style — tinted bg with 600-level colored text that passes AA on cream — and color-coded label chips keep their existing validated color mechanics without new interpolation (D-08)
    - Primary actions Send and Archive carry the same gradient + glow + hover-scale + focus-ring treatment as the tracer's Compose button (D-01, D-04, D-05, D-10) while destructive Delete stays a solid AA-stop red, and secondary buttons stay neutral (UI-SPEC accent-reserved rule)
    - Interactive list rows and folder rows get the scale+glow hover (transform-only, never animated backdrop-filter per Pitfall 6) with visible focus states
    - The sanitized message-viewer markup remains free of glass/gradient chrome classes (T-06-03 guard stays green)
  artifacts:
    - resources/css/app.css (+glass-card utility, .glass-input component class, @supports fallback, reduced-transparency guard, .bg-blobs decorative layer)
    - resources/views/layouts/app.blade.php (decorative blob layer div)
    - mailbox.blade.php (glass sidebar shell), folder-sidebar.blade.php (rows/tabs/badges), message-row.blade.php (hover/focus/density), search-results-dropdown.blade.php (glass panel), label-chips.blade.php (soft-tag pills), message-toolbar.blade.php (gradient Archive), composer.blade.php (gradient Send), components/composer-recipient-chips.blade.php (glass inputs)
  key_links:
    - The blob layer (fixed, z-0, pointer-events-none) must sit behind nav/main in DOM paint order — placed immediately after body open before nav — so no stacking-context trap hides it or the glass above it (Pitfall 8: backdrop-filter creates containing blocks — overlays/dropdowns stay outside glass containers)
    - glass-card/glass-input utilities must be authored with the @supports solid fallback, wired to the same surface colors as the surface token so fallback matches theme
---

<objective>
Expand the tracer into the full mailbox chrome: glass surfaces (sidebar, cards, inputs), soft-tag status badges, gradient + glow + hover-scale primary actions (Send, Archive), and hover/focus polish on list rows — all built on plan 06-02's tokens with zero new dependencies.

Purpose: D-06/D-07 (glass), D-08 (badges), and the remaining D-01/D-05/D-10 coverage land here as pattern expansions of the proven slice. Glass needs the blob layer to be visible over flat cream/soft-dark (research Pitfall 4) — that layer is part of this plan because it exists only to serve the glass panels.
Output: A mailbox chrome that visibly reads as the phase's target design in both themes, with the UiPolishTest glass/badge/gradient methods flipped green and the message-viewer no-chrome guard still green.
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-6-ui-ux-polish/06-CONTEXT.md
@.planning/phases/06-6-ui-ux-polish/06-RESEARCH.md
@.planning/phases/06-6-ui-ux-polish/06-UI-SPEC.md
@.planning/phases/06-6-ui-ux-polish/06-VALIDATION.md
@resources/css/app.css
@resources/views/layouts/app.blade.php
@resources/views/layouts/mailbox.blade.php
@resources/views/livewire/mailbox/folder-sidebar.blade.php
@resources/views/livewire/mailbox/message-row.blade.php
@resources/views/livewire/mailbox/message-toolbar.blade.php
@resources/views/livewire/mailbox/composer.blade.php
@resources/views/components/composer-recipient-chips.blade.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Glass foundation — glass-card utility, glass input class, fallbacks, blob layer (D-06, D-07)</name>
  <files>resources/css/app.css, resources/views/layouts/app.blade.php</files>
  <read_first>
    RESEARCH.md Pattern 4 (glass surfaces: translucent fill + 8px blur at v4 scale = backdrop-blur-sm, 1px subtle border, @supports solid fallback keyed to the surface colors, dark-mode white/10 tint over soft-dark), Code Example 5 (prefers-reduced-transparency guard), Pitfall 4 (blob layer — fixed, pointer-events-none, aria-hidden, 2-3 max, z-0 behind glass at z-10), Pitfall 8 (containing-block trap — no fixed/absolute descendants inside glass).
  </read_first>
  <action>
    In resources/css/app.css (digest region, per D-06/D-07):
    (1) Add a glass-card utility that is pure decoration: the backdrop blur stack (both prefixed and unprefixed), a translucent fill, and a subtle border. Keep the blur radius at the research cap of 8px for panels. Any translucent fill defaults in the utility itself must pair with the layer tokens added in 06-02 (white/60 over cream token in light, white/10 over soft-dark token in dark — resolved through the theme's runtime dark override).
    (2) Add a companion input class for D-06 glass text inputs with the same fill/blur treatment plus an explicit focus ring on the blue-600 primary stop and the rounded shape, and dark-mode adjustments gated by the existing custom dark variant.
    (3) Add the @supports fallback — when backdrop-filter is unsupported, glass surfaces snap to near-solid fills derived from the surface tokens (cream light / soft-dark dark) so contrast is not lost.
    (4) Add a prefers-reduced-transparency media block that removes blur and forces near-solid fills with an important precedence for both surface classes (WCAG intent per research Code Example 5).
    (5) Add a decorative blob layer class: fixed, full-viewport, pointer-events-none, no text content, composed of 2-3 radial gradients using the approved 500-level blue/violet decorative stops (the only allowed use of the locked D-04 stops — large non-text fills at low opacity, per the D-04 reconciliation in 06-02). Static CSS only — no user input, no animation of the blur (Pitfall 6).
    In resources/views/layouts/app.blade.php: insert the blob layer element as the second element inside body (immediately after the route overlay div, before the nav) with the decorative class, aria-hidden, and pointer-events-none — positioned z-0 so nav/main (later in DOM paint order) stay above it; this is a chrome-only decoration and must never be reused inside the message render path (T-06-03).
    Do not restyle any component view in this task — the glass classes land on components in task 2/3. The reduced-transparency guard and @supports fallback reference the utility class names that task 2/3 apply, so CSS only changes here.
  </action>
  <verify>
    <automated>npm run build && rg -l "glass-card" public/build/assets/*.css && rg -c "prefers-reduced-transparency" resources/css/app.css | rg -q '^1$'</automated>
    <fails_when>npm run build exits non-zero; no built css asset contains the glass-card class (utility not compiled); the reduced-transparency guard count is not exactly one</fails_when>
  </verify>
  <acceptance_criteria>
    app.css grows the glass-card utility, the glass input class, the @supports solid fallback, the reduced-transparency guard, and the blob-layer class; app.blade.php carries the decorative blob div; build output contains glass-card.
  </acceptance_criteria>
  <done>
    Glass CSS foundation is committed and compiled; the blob layer renders behind the layout without intercepting pointer events. Blob/glass classes appear nowhere in any message-render view (T-06-03 — verified in task 3's guard run).
  </done>
  <reversibility rating="reversible">CSS utility + one layout div; removable without architectural impact.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Mailbox chrome restyle — glass sidebar/shell, folder rows, message rows, search panel, soft-tag badges (D-07, D-08, D-10)</name>
  <files>resources/views/layouts/mailbox.blade.php, resources/views/livewire/mailbox/folder-sidebar.blade.php, resources/views/livewire/mailbox/message-row.blade.php, resources/views/livewire/mailbox/search-results-dropdown.blade.php, resources/views/livewire/mailbox/label-chips.blade.php</files>
  <read_first>
    CURRENT markup anchors in these views: mailbox.blade.php aside (bg-white dark:bg-gray-800 border-r) and its mobile overlay drawer; folder-sidebar tab bar (active tab bg-blue-50 border-b-2 border-blue-600) and unread pill (bg-blue-100 text-blue-600 rounded-full — already soft-tag; D-08 keeps the tinted pattern but floor-checks the text stop to blue-600 which passes AA on cream); message-row (bg-white list rows, hover:bg-gray-50, focus ring via .focus-ring class in app.css); search-results-dropdown (white dropdown panel with existing Alpine-flag skeleton at lines 55-75 — that skeleton restyle belongs to plan 06-04); label-chips (label chips keep their validated per-label inline hex colors — do NOT convert to new interpolation; the overflow +N pill is bg-gray-100 text-gray-600 — floor-check the text to gray-500 per the cream contrast table).
  </read_first>
  <action>
    Restyle the mailbox chrome (D-07 glass cards, D-08 soft-tag badges, D-10 hover) on the 06-02 tokens:
    (1) mailbox.blade.php — apply the glass-card treatment to the desktop sidebar shell (translucent fill + blur + subtle border via the utility from task 1, keeping the w-64 flex layout and the border dividing line), and to the mobile drawer panel; keep the existing mobile overlay transitions (they already use the x-transition pattern from research Code Example 4). Keep the sidebar content container unchanged structurally.
    (2) folder-sidebar.blade.php — restyle folder rows and tab states with the surface tokens (active folder: tinted fill + the primary stop for the accent border and text; hover rows tinted on the surface token instead of gray-50). Keep the unread pill's soft-tag tinted pattern but standardize its text stop to the 600-level blue so it passes AA on cream (D-08); floor-check the total-count text as well. Do not change any wire:click handler or the x-cloak panels (x-cloak now works — the 06-02 rule is live).
    (3) message-row.blade.php — apply D-10 hover polish: scale on hover is NOT appropriate for full-width list rows (Pitfall 6 + Gmail benchmark) — instead use a subtle surface-tinted hover plus a left accent that fades in on the primary gradient, a focus-visible ring on the row, and keep the density-driven message-row custom properties untouched (D-14/D-16 invariant). Never animate backdrop-filter.
    (4) search-results-dropdown.blade.php — convert the white dropdown panel to the glass-card utility (same panel style as the sidebar so the search flyout matches), keeping the existing Alpine open/loading logic and the existing loading skeleton markup in place (its restyle is plan 06-04's task).
    (5) label-chips.blade.php — soft-tag restyle of the +N overflow pill (tinted gray fill, gray-500 text per the cream contrast floor) while label chips themselves keep their validated inline hex styling unchanged — no new class interpolation, no new inline styles (V5/T-06-02).
    All class strings static; zero user-controlled interpolation into class lists (V5).
  </action>
  <verify>
    <automated>php artisan test --filter='UiPolishTest::(test_glass_surface_classes_present|test_status_badges_use_soft_tag_style|test_message_viewer_has_no_chrome_classes)' && npm run build && php artisan test --filter=AppearanceTest</automated>
    <fails_when>any of the three UiPolishTest methods reports failure (glass classes missing from folder-sidebar/search/mailbox markup; soft-tag badge pattern regressed; or the message-viewer chrome guard broke — message-viewer must stay glass/gradient-free); build non-zero; AppearanceTest fails (density/theme regression)</fails_when>
  </verify>
  <acceptance_criteria>
    Sidebar and search panel carry the glass-card treatment; folder/message rows hover on surface tints with gradient accent; unread and overflow pills are soft-tag with AA-passing 600/500 stops; label chips untouched in mechanics; message-viewer still chrome-free.
  </acceptance_criteria>
  <done>
    The five views are restyled on the token system, all three pinned UiPolishTest methods green, AppearanceTest green — a user can browse folders, hover rows, and see glass panels + soft-tag counts in both themes.
  </done>
  <reversibility rating="reversible">Utility-class swaps on views; per-view revert is local.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Primary actions + glass inputs — gradient Send/Archive, glow, glass text inputs (D-01, D-04, D-05, D-06, D-10)</name>
  <files>resources/views/livewire/mailbox/composer.blade.php, resources/views/livewire/mailbox/message-toolbar.blade.php, resources/views/components/composer-recipient-chips.blade.php</files>
  <read_first>
    Anchors: composer.blade.php Send button (currently bg-blue-600 rounded-md hover:bg-blue-700 with wire:loading.text spans — the loading swap is reworked in plan 06-04, keep the current wire:loading structure working); message-toolbar Archive button (neutral bordered button — becomes the gradient primary per D-01, exactly the tracer's Compose treatment) and Delete button (solid destructive red — floor to the 600 stop for AA on cream); composer-recipient-chips.blade.php contains the To/CC/BCC input fields that get the D-06 glass input treatment; the composer container panel gets the glass-card treatment per D-07.
  </read_first>
  <action>
    Apply the remaining primary-action and glass-input decisions:
    (1) composer.blade.php — restyle the Send button to the exact gradient primary treatment proven in 06-02 (gradient fill from-blue-600 to-purple-600, rounded-lg, glow shadow tokens, hover scale 1.02 + stronger glow, focus-visible ring with offset, motion-reduce suppression), D-05/D-10. Keep the existing wire:loading.attr and the Send/Sending span swap working; keep the Discard button neutral. Give the composer modal container the glass-card treatment (D-07).
    (2) message-toolbar.blade.php — restyle the Archive action to the same gradient primary treatment (D-01: Archive is a locked gradient action); restyle Delete to the solid destructive red at the 600 stop (AA floor per contrast table) with its own focus-visible ring; keep secondary/neutral buttons gray-surface (UI-SPEC: gradient on primary only — the per-selection count text stays neutral).
    (3) composer-recipient-chips.blade.php — apply the D-06 glass input class (translucent fill + blur + focus ring on the primary stop) to the recipient input fields; keep placeholder text and all wire:model/Alpine behaviors byte-identical.
    Static classes only; no dynamic class interpolation from user input anywhere (V5/T-06-02). No new inline script in any view (T-06-04).
  </action>
  <verify>
    <automated>php artisan test --filter='UiPolishTest::(test_send_and_archive_buttons_have_gradient_classes|test_glass_surface_classes_present|test_message_viewer_has_no_chrome_classes)' && npm run build && php artisan test --filter=AppearanceTest</automated>
    <fails_when>the send/archive gradient method fails (missing gradient substring in composer or toolbar); the glass method fails; the message-viewer chrome guard fails; build non-zero; AppearanceTest fails</fails_when>
  </verify>
  <acceptance_criteria>
    Send and Archive carry the AA-passing gradient with glow/scale/focus-ring; Delete is the solid AA red; recipient inputs are glass with focus rings; composer container is glass; message-viewer remains chrome-free.
  </acceptance_criteria>
  <done>
    All D-01/D-04/D-05/D-06/D-10 surface coverage in the mailbox is complete: a user composing or bulk-archiving sees gradient primaries, glass inputs/panels, and hover glow — with UiPolishTest methods 2, 4, 5, 6 green and AppearanceTest green.
  </done>
  <reversibility rating="reversible">Class swaps on three views; all decisions marked reversible in CONTEXT.md.</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces

| Artifact | Type | Consumed By |
|----------|------|-------------|
| `resources/css/app.css` — glass-card utility, glass-input class, @supports fallback, reduced-transparency guard, blob-layer class | CSS | plan 06-05 reuses glass-card on settings pickers |
| `resources/views/layouts/app.blade.php` — blob layer div | layout | backdrop for all glass panels app-wide |
| mailbox.blade.php (glass sidebar shell + mobile drawer), folder-sidebar.blade.php (rows/tabs/badges), message-row.blade.php (hover/accent/focus), search-results-dropdown.blade.php (glass panel), label-chips.blade.php (soft-tag overflow pill) | views | 06-04 adds skeletons to folder-sidebar + search panel (same files, later wave) |
| composer.blade.php (gradient Send + glass modal), message-toolbar.blade.php (gradient Archive, AA Delete), components/composer-recipient-chips.blade.php (glass inputs) | views | 06-04 converts Send's loading spans to data-loading variants (same files, later wave) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| blob layer / glass panels → viewport | Decorative chrome behind interactive content; pointer-events-none div must never intercept clicks |
| label color → badge markup | Pre-existing per-label inline hex (validated palette at input, Phase 4) — preserved as-is; no new interpolation introduced |
| email HTML → message-viewer | Sanitized render container must stay free of app chrome classes (cross-phase invariant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Tampering | class/color interpolation (V5) | high | mitigate | All new glass/badge markup uses static class constants; label chips keep their existing validated palette mechanism (no new interpolation, no new inline styles); task verifiers assert static patterns only |
| T-06-03 | Tampering | email HTML reusing app chrome classes | high | mitigate | message-viewer untouched in files_modified both waves; UiPolishTest method 6 negative-guards glass-/bg-linear in its markup in every task verify |
| T-06-04 | Info disclosure | inline script addition | medium | mitigate | Zero new inline scripts in any restyled view; all behavior remains in bundled app.js; CSP nonce emission re-verified via UiPolishTest method 7 in the full sweep |
| T-06-05 | Tampering | contrast/focus regressions (blob opacity, badge stops) | medium | mitigate | 600-level badge/action stops per research contrast table on cream; focus-visible rings on all restyled interactive elements; reduced-transparency fallback added in task 1 |
| T-06-06 | Tampering | blob layer click interception | low | mitigate | pointer-events-none + aria-hidden mandated; full-width rows do not scale (Pitfall 6) |
</threat_model>

<verification>
- `php artisan test --filter=UiPolishTest` — methods 2 (send/archive gradient), 4 (glass), 5 (soft-tag), 6 (no-chrome), 7 (csp nonce) green at plan end; methods 1/3 already green from 06-02. Full UiPolishTest green except nothing — all seven green once Send/Archive land. (Method 1 compose gradient was green in wave 1; method 3 x-cloak too.)
- `php artisan test --filter=AppearanceTest` green (density/theme persistence intact through the restyle).
- `npm run build` green each task.
- `rg -l "glass-card" public/build/assets/*.css` non-empty (task 1).
</verification>

<success_criteria>
- Mailbox chrome fully restyled on the token system: glass sidebar/panels/inputs over blobs, soft-tag badges at AA stops, gradient Send/Archive with glow+scale+focus, AA-safed destructive and neutral controls.
- Every UiPolishTest method green; AppearanceTest green; csp.php diff still empty.
</success_criteria>

<output>
Create `.planning/phases/06-6-ui-ux-polish/06-03-SUMMARY.md` when done
</output>
