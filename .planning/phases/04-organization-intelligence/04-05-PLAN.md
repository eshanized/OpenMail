---
phase: 04-organization-intelligence
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/Models/MessageMetadata.php
  - app/Livewire/Mailbox/MessageList.php
  - resources/views/livewire/mailbox/thread-row.blade.php
  - resources/views/livewire/mailbox/message-list.blade.php
  - tests/Feature/ThreadUITest.php
autonomous: true
requirements: [THR-01, THR-02, THR-03, THR-04, THR-05]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Thread rows render in message list with expand/collapse via Alpine.js (D-03, D-04)"
    - "Thread ordering sorts by latest message date descending (THR-05)"
    - "Thread toggle button in toolbar switches between threaded/flat modes (D-18)"
    - "Thread mode persists per folder in localStorage (D-18)"
  artifacts:
    - "app/Services/ThreadBuilder.php (from Plan 1)"
    - "app/Livewire/Mailbox/MessageList.php (thread integration)"
    - "resources/views/livewire/mailbox/thread-row.blade.php"
    - "resources/views/livewire/mailbox/message-list.blade.php (conditional render)"
  key_links:
    - "MessageMetadata → ThreadBuilder (headers fetched via ImapMailboxService)"
    - "ThreadBuilder → MessageList Livewire (thread tree passed to view)"
    - "thread-row.blade.php → Alpine.js expansion state (client-side only per D-03)"
    - "Thread toggle → localStorage key openmail:threadMode:{folderPath}"

---

<objective>
Integrate the ThreadBuilder into MessageList to render threaded view for Inbox with working expand/collapse. Implements thread-row component per UI-SPEC §2.1-2.2 and thread toggle button per D-18/UI-SPEC §2.3. This is the threading UI tracer — one folder (Inbox) renders threaded end-to-end.
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-organization-intelligence/04-CONTEXT.md
@.planning/phases/04-organization-intelligence/04-RESEARCH.md
@.planning/phases/04-organization-intelligence/04-UI-SPEC.md
@.planning/phases/02-mailbox-core/02-CONTEXT.md
@.planning/phases/03-compose-send/03-CONTEXT.md
@app/Services/ImapMailboxService.php
@app/Models/MessageMetadata.php
@app/Livewire/Mailbox/MessageList.php
@resources/views/layouts/mailbox.blade.php
@config/openmail.php
@app/Services/ThreadBuilder.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: MessageMetadata thread accessors + MessageList thread mode integration</name>
  <files>
    app/Models/MessageMetadata.php
    app/Livewire/Mailbox/MessageList.php
    tests/Feature/ThreadUITest.php
  </files>
  <action>
    1. Extend MessageMetadata: add `labels()` belongsToMany, `children` accessor for thread tree, `latestDate`, `unreadCount`, `from_display`, `formatted_date` accessors. Add `thread_id` virtual attribute for Alpine.js tracking.

    2. In MessageList.php:
       - Add `threadMode` property (default from localStorage per folder, default 'threaded' for Inbox per D-18/UI-SPEC). In `mount()`, load thread mode from session/localStorage via JavaScript dispatch.
       - In `getMessagesProperty()`: when threadMode === 'threaded', fetch message headers for current page + thread roots via ImapMailboxService, pass to ThreadBuilder, return thread roots. When 'flat', return existing flat messages.
       - Add `toggleThreadMode()`: switches mode, saves to localStorage via JS dispatch, triggers re-render.
       - Add listener for 'toggle-thread-mode' from Alpine.js keyboard shortcut.

    3. Wire IMAP header fetch for threading: In MessageList, when threadMode === 'threaded', call ImapMailboxService->getThreadHeaders(folderPath, pageUids) which fetches Message-ID, In-Reply-To, References, Subject, Date for visible page UIDs + any thread root UIDs from References. Populate thread_header_cache with results.

    4. Write feature test: ThreadUITest::testThreadedViewLoads — visits Inbox, verifies thread rows render (not flat), thread toggle button visible.
  </action>
  <verify>
    <automated>cd /home/snigdha/Desktop/OpenMail && ./vendor/bin/pest tests/Feature/ThreadUITest.php::testThreadedViewLoads 2>&1 | tail -15</automated>
    <fails_when>non-zero exit, or "0 passed" in summary, or test shows "FAIL"</fails_when>
  </verify>
  <done>
    MessageMetadata has thread accessors. MessageList loads threaded view for Inbox by default. ThreadBuilder receives headers and returns thread roots. thread_header_cache populated during fetch.
  </done>
</task>

<task type="tracer">
  <name>Task 2: ThreadRow component + thread toggle button + expand/collapse (tracer)</name>
  <files>
    resources/views/livewire/mailbox/thread-row.blade.php
    resources/views/livewire/mailbox/message-list.blade.php
    tests/Feature/ThreadUITest.php
  </files>
  <action>
    Wire the tracer: Inbox folder renders threaded view end-to-end with expand/collapse.

    1. Create thread-row.blade.php per UI-SPEC §2.1-2.2: 56px height (py-3), checkbox, star, sender+unread badge, subject+label chips (max 3 + overflow), date, attachment icon, chevron.
       - Alpine.js x-data for expanded state, x-transition for smooth expand/collapse (150ms ease-out per UI-SPEC)
       - Click row (except interactive elements: checkbox, star, label chips) toggles expansion
       - Keyboard: Space/Enter toggles, Arrow Right expands, Arrow Left collapses
       - Focus management: when expanded, first child focusable
       - Thread children rendered recursively with depth indentation (pl-3 border-l per depth)

    2. Update message-list.blade.php:
       - Conditional render: if threadMode === 'threaded', loop thread roots with `<x-mailbox.thread-row>`, else loop flat messages with existing message-row
       - Add thread toggle button in toolbar per D-18/UI-SPEC §2.3:
         - Icon: stacked chat bubbles (threaded) ↔ single chat bubble (flat)
         - Active state: blue-600 + bg-blue-50, Inactive: gray-600
         - wire:click="toggleThreadMode"
         - Tooltip: "Threaded view (t)"

    3. Add feature tests:
       - ThreadUITest::testExpandCollapse — clicks expand, verifies children visible, clicks collapse, verifies children hidden
       - ThreadUITest::testThreadTogglePersistence — switches to Inbox, toggles to Flat, navigates to Sent (verifies default Flat), back to Inbox (verifies Threaded persisted)

    4. localStorage persistence: Alpine.js in message-list.blade.php reads/writes `openmail:threadMode:{folderPath}` on toggle. Default 'threaded' for Inbox, 'flat' for Sent/Drafts/Trash/Spam per UI-SPEC.
  </action>
  <verify>
    <automated>cd /home/snigdha/Desktop/OpenMail && ./vendor/bin/pest tests/Feature/ThreadUITest.php::testExpandCollapse tests/Feature/ThreadUITest.php::testThreadTogglePersistence 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or "0 passed" in summary, or any test shows "FAIL"</fails_when>
  </verify>
  <done>
    Inbox folder renders threaded view with expandable/collapsible thread rows. Thread toggle button appears in toolbar. localStorage persists mode per folder. Alpine.js transitions work smoothly (150ms). Keyboard shortcuts work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IMAP Server → Backend | Message headers for threading fetched via IMAP; untrusted input |
| Client → Backend (Livewire) | Thread expansion state, thread mode toggle — validated server-side |
| localStorage → Thread Mode | User-controlled persistence; no security impact (UI preference only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-21 | Tampering | localStorage thread mode manipulation | low | accept | Only affects UI rendering; no data integrity impact |
| T-04-22 | DoS | Keyboard shortcut spam | low | mitigate | Debounce shortcut handlers (100ms); Livewire request deduplication |
| T-04-23 | DoS | Deeply nested thread expansion | medium | mitigate | ThreadBuilder max depth guard (50 levels); Alpine.js x-show not x-if to limit DOM |
| T-04-24 | Info Disclosure | Thread view cross-folder | low | mitigate | ThreadBuilder only processes messages user has access to (user_id scoped) |
</threat_model>

<verification>
Run `./vendor/bin/pest tests/Feature/ThreadUITest.php` — all tests must pass.
Manual: Open Inbox, verify threaded view, expand/collapse threads, toggle to flat, verify persistence across folder navigation.
</verification>

<success_criteria>
- Thread rows render in message list with expand/collapse via Alpine.js (THR-04, D-03, D-04)
- Thread ordering sorts by latest message date descending (THR-05)
- Thread toggle button in toolbar switches between threaded/flat modes (D-18)
- Thread mode persists per folder in localStorage (threaded for Inbox, flat for others by default)
- thread_header_cache populated during IMAP header fetch for cross-folder thread roots
- All feature tests pass
</success_criteria>

<output>
Create .planning/phases/04-organization-intelligence/04-05-SUMMARY.md when done
</output>