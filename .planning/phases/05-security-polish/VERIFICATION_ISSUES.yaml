issues:
  - plan: "05-03"
    dimension: "dependency_correctness"
    severity: "blocker"
    description: "depends_on references use '05-01' format but plan identifiers are '01', '02', etc. Dependency resolver will not find referenced plans."
    fix_hint: "Change all depends_on references from ['05-01', '05-02'] to ['01', '02'] to match the plan: frontmatter field."
  - plan: "05-04"
    dimension: "dependency_correctness"
    severity: "blocker"
    description: "depends_on references use '05-02' format but plan identifiers are '01', '02', etc."
    fix_hint: "Change depends_on from ['05-02'] to ['02']."
  - plan: "05-05"
    dimension: "dependency_correctness"
    severity: "blocker"
    description: "depends_on references use '05-02', '05-03', '05-04' format but plan identifiers are '02', '03', '04'."
    fix_hint: "Change depends_on from ['05-02', '05-03', '05-04'] to ['02', '03', '04']."
  - plan: "05-06"
    dimension: "dependency_correctness"
    severity: "blocker"
    description: "depends_on references use '05-01' through '05-05' format but plan identifiers are '01' through '05'."
    fix_hint: "Change depends_on from ['05-01', '05-02', '05-03', '05-04', '05-05'] to ['01', '02', '03', '04', '05']."
  - plan: null
    dimension: "research_resolution"
    severity: "blocker"
    description: "RESEARCH.md has ## Open Questions section without (RESOLVED) suffix and no inline RESOLVED markers on 5 questions."
    fix_hint: "Resolve each question and mark section as ## Open Questions (RESOLVED) with RESOLVED: prefix on each answer."
  - plan: "05-01"
    dimension: "scope_sanity"
    severity: "blocker"
    description: "Plan 01 modifies 15 files, hitting the 15+ file blocker threshold. Risks context exhaustion and quality degradation."
    fix_hint: "Split Plan 01 into two plans: (a) CSP infrastructure (config, middleware, nonce generator, presets, report endpoint), (b) Audit logging (migration, model, service, LoginController integration, prune command)."
  - plan: "05-02"
    dimension: "scope_sanity"
    severity: "warning"
    description: "Plan 02 modifies 11 files, exceeding the 10-file warning threshold."
    fix_hint: "Consider splitting into smaller vertical slices if feasible."
  - plan: "05-03"
    dimension: "scope_sanity"
    severity: "warning"
    description: "Plan 03 modifies 12 files, exceeding the 10-file warning threshold."
    fix_hint: "Consider splitting into smaller vertical slices if feasible."
  - plan: "05-04"
    dimension: "scope_sanity"
    severity: "warning"
    description: "Plan 04 modifies 11 files, exceeding the 10-file warning threshold."
    fix_hint: "Consider splitting into smaller vertical slices if feasible."
  - plan: "05-05"
    dimension: "scope_sanity"
    severity: "warning"
    description: "Plan 05 modifies 13 files, exceeding the 10-file warning threshold (approaching blocker at 15)."
    fix_hint: "Consider splitting into smaller vertical slices (e.g., separate Profile/Mail/Security tabs from Composer signature integration)."
  - plan: "05-06"
    dimension: "scope_sanity"
    severity: "warning"
    description: "Plan 06 modifies 11 files, exceeding the 10-file warning threshold."
    fix_hint: "Consider splitting into smaller vertical slices if feasible."
  - plan: "05-01"
    dimension: "dependency_correctness"
    severity: "warning"
    description: "Plan 01 and Plan 02 are both Wave 1 with no depends_on between them, but both modify routes/web.php. Parallel execution could cause merge conflicts."
    fix_hint: "Either declare depends_on: ['01'] in Plan 02, move Plan 02 to Wave 2, or justify that routes are independent additions (different route paths)."
  - plan: null
    dimension: "task_completeness"
    severity: "warning"
    description: "All 18 tasks across 6 plans use <name>, <files>, <action>, <verify>, <done> but verification schema expects id, type (execute|verify|decision), description, read_first (array), acceptance_criteria (array). Missing explicit id field and read_first/acceptance_criteria elements."
    fix_hint: "Add id attribute to each task; consider aligning field names with verification schema (files→read_first, done→acceptance_criteria) or update verification requirements to match actual plan format."