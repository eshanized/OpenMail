---
phase: 03-compose-send
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - resources/js/components/TiptapEditor.js,
  - resources/views/livewire/mailbox/composer.blade.php,
  - resources/views/components/composer-quote.blade.php,
  - resources/views/components/composer-recipient-chips.blade.php,
  - resources/views/components/undo-send-toast.blade.php,
  - app/Livewire/Mailbox/Composer.php,
  - app/Services/ComposerService.php,
  - app/Services/ContactAutocompleteService.php,
  - app/Console/Commands/ProcessPendingSends.php,
  - routes/console.php,
  - resources/css/app.css,
  - tests/Feature/Composer/TiptapIntegrationTest.php,
  - tests/Feature/Composer/DraftAutosaveTest.php,
  - tests/Feature/Composer/UndoSendTest.php,
  - tests/Feature/Composer/ContactAutocompleteTest.php
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
  - COMP-05
  - COMP-06
  - COMP-07
  - COMP-08
  - COMP-09
  - COMP-10
  - COMP-11
  - COMP-12
  - COMP-13

estimate:
  tokens: 70000
  raw_tokens: 60000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - Rich text editor (Tiptap) renders in composer with full toolbar and syncs content to Livewire per D-02/D-04
    - Draft autosave persists to LocalStorage on keystroke debounce and syncs to IMAP Drafts per D-05/D-06/D-07
    - Undo send delays SMTP delivery by configurable 5-30 seconds with toast notification and cancel per D-21/D-23
    - Contact autocomplete suggests recent recipients from IMAP Sent/Inbox per D-25/D-26/D-27
    - Reply/forward quoted text is collapsible with show/hide toggle per D-09
    - Attachments support drag-and-drop with visual feedback per D-13
    - ProcessPendingSends scheduler command processes queued sends every minute per D-19
  artifacts:
    - resources/js/components/TiptapEditor.js,
    - resources/views/components/composer-quote.blade.php,
    - resources/views/components/composer-recipient-chips.blade.php,
    - resources/views/components/undo-send-toast.blade.php,
    - app/Services/ContactAutocompleteService.php,
    - app/Console/Commands/ProcessPendingSends.php
  key_links:
    - TiptapEditor.js → Livewire Composer via hidden textarea wire:model sync
    - ProcessPendingSends → PendingSend model → ComposerService::sendMessage for retry
    - ContactAutocompleteService → ImapMailboxService::searchRecipients for IMAP data
    - Undo-send toast → Composer Livewire::undoSend() → PendingSend cancel + IMAP move to Drafts
---

<objective>
Layer rich text editing (Tiptap), draft autosave with LocalStorage and IMAP sync, undo-send delay with toast notification, and contact autocomplete from IMAP recipients onto the basic composer from Plan 1. This completes the full compose-send loop with all user-facing features.

Purpose: Plan 1 proved the architecture works end-to-end with a plain textarea. Plan 2 delivers the polished user experience: full formatting toolbar, instant draft saves, safety net via undo send, and smart recipient suggestions. These are the features that make OpenMail feel like a real email client.

Output: Working Tiptap editor with toolbar, draft autosave to LocalStorage + IMAP Drafts, undo-send toast with countdown, ProcessPendingSends scheduler command, contact autocomplete from IMAP, collapsible quoted sections, and comprehensive tests.
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-compose-send/03-CONTEXT.md
@.planning/phases/03-compose-send/03-RESEARCH.md
@.planning/phases/03-compose-send/03-UI-SPEC.md
@.planning/phases/03-compose-send/03-01-SUMMARY.md

@app/Livewire/Mailbox/Composer.php
@app/Services/ComposerService.php
@app/Services/ImapMailboxService.php
@app/Services/MessageSanitizer.php
@app/Models/PendingSend.php
@resources/views/livewire/mailbox/composer.blade.php
@resources/js/components/TiptapEditor.js,
@routes/console.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tiptap editor integration with full toolbar + composer-quote component</name>
  <files>
    resources/js/components/TiptapEditor.js,
    resources/views/livewire/mailbox/composer.blade.php,
    resources/views/components/composer-quote.blade.php,
    resources/css/app.css,
    tests/Feature/Composer/TiptapIntegrationTest.php
  </files>
  <read_first>
    resources/js/components/TiptapEditor.js,
    resources/views/livewire/mailbox/composer.blade.php,
    resources/css/app.css,
    app/Livewire/Mailbox/Composer.php,
    app/Services/MessageSanitizer.php
  </read_first>
  <action>
**Step 1 — TiptapEditor.js Full Implementation (per D-02, D-04):**

Replace the scaffold in `resources/js/components/TiptapEditor.js` with full Tiptap integration:

`initTiptapEditor(element, options)` function:
- Import all Tiptap packages dynamically (using dynamic `import()` to avoid SSR issues)
- Create Editor instance with extensions:
  - StarterKit (heading levels 1-3, bold, italic, strike, code, codeBlock, blockquote, bulletList, orderedList, horizontalRule, history)
  - Link (openOnClick: false, HTMLAttributes: { class: 'text-blue-600 underline cursor-pointer' })
  - Image (inline: false, allowBase64: false)
  - Table (resizable: true), TableRow, TableCell, TableHeader
  - TaskList, TaskItem (nested: true)
  - TextAlign (types: ['heading', 'paragraph'])
  - TextStyle, Color
  - Placeholder (placeholder: 'Start writing...')
  - History (depth: 100)
  - CharacterCount (limit: 100000)
  - Underline
  - Highlight (multicolor: true)
  - Emoji (emoticons: false,悍nemo: { tileMargin: 4 })
- Set editor content from `options.initialContent` (for reply/forward quoted text)
- On `onUpdate`: call `options.onUpdate(editor.getHTML())` to sync to Livewire hidden textarea
- Set `editorProps.attributes.class` to `'prose prose-sm max-w-none focus:outline-none min-h-[300px] p-4'`
- Set `editorProps.attributes.spellcheck` to `'true'`
- Return editor instance

`createToolbar(editor, container)` function:
- Create toolbar DOM element with two rows per UI-SPEC:
  - Row 1: Bold, Italic, Underline, Strikethrough | FontSize (dropdown), TextColor (color picker), Highlight | Heading (H1/H2/H3 dropdown) | Alignment (Left/Center/Right/Justify)
  - Row 2: BulletList, OrderedList, TaskList | Blockquote, CodeBlock, HorizontalRule | Table, Link, Image, Emoji | Undo, Redo
- Each button: `@click` toggles formatting, `:class` shows active state (bg-blue-100 for active), `aria-pressed` for toggle buttons
- Link insertion: prompt for URL via `prompt()`, then `editor.chain().focus().setLink({ href: url }).run()`
- Image insertion: prompt for URL (inline image as file attachment deferred to v1 per RESEARCH.md Open Question 2)
- Table insertion: `editor.chain().focus().insertTable({ rows: 3, cols: 3, withHeaderRow: true }).run()`
- Insert HTML button: `editor.chain().focus().insertContent('<hr>').run()` for horizontal rule

**Step 2 — Livewire Composer Integration:**

Modify `app/Livewire/Mailbox/Composer.php`:
- Add `$bodyHtml = ''` property to store Tiptap HTML output
- Add `$bodyText = ''` property for plain text fallback
- Add `syncBodyFromEditor(string $html)` public method (callable from JS via `$wire.syncBodyFromEditor(html)`)
- Update `getComposerData()` to use `$this->bodyHtml` instead of `$this->body`

Modify `resources/views/livewire/mailbox/composer.blade.php`:
- Replace plain textarea with Tiptap editor container:
  ```html
  <div wire:ignore x-data x-init="$nextTick(() => { initTiptapEditor($refs.editor, { onUpdate: (html) => $wire.syncBodyFromEditor(html), initialContent: @js($body) }) })">
      <div x-ref="toolbar"></div>
      <div x-ref="editor" class="tiptap-editor"></div>
      <textarea wire:model="bodyHtml" class="hidden" aria-hidden="true"></textarea>
  </div>
  ```
- The `wire:ignore` prevents Livewire from re-rendering the Tiptap DOM
- The hidden textarea receives HTML via `$wire.syncBodyFromEditor()` for form submission

**Step 3 — Collapsible Quote Component (per D-09):**

Create `resources/views/components/composer-quote.blade.php`:
- Accept props: `$attribution` (string), `$quotedHtml` (string), `$uid` (string, unique ID)
- Alpine.js `x-data="{ open: false }"` for toggle state
- Structure:
  - Header: attribution text + toggle button ("Show quoted text" / "Hide quoted text")
  - Content: `<blockquote>` with `x-show="open" x-transition` containing sanitized HTML
  - CSS: `border-l-4 border-blue-200 pl-4 ml-4 my-2` per UI-SPEC
- Content is rendered via `{!! $quotedHtml !!}` — must be pre-sanitized by MessageSanitizer before passing to component

Modify `Composer.php::buildQuotedBody()`:
- Use the composer-quote component structure
- Sanitize HTML via `MessageSanitizer::sanitizeHtml()` per D-11
- For forward: prepend `"-------- Forwarded message --------"` line
- Return HTML string that the Blade template renders

**Step 4 — Tiptap CSS:**

Add to `resources/css/app.css`:
```css
.tiptap-editor .ProseMirror {
    min-height: 300px;
    padding: 1rem;
    outline: none;
}
.tiptap-editor .ProseMirror p.is-editor-empty:first-child::before {
    color: #9ca3af;
    content: attr(data-placeholder);
    float: left;
    height: 0;
    pointer-events: none;
}
.tiptap-editor .ProseMirror blockquote {
    border-left: 3px solid #bfdbfe;
    padding-left: 1rem;
    margin-left: 1rem;
    color: #6b7280;
}
.tiptap-editor .ProseMirror table {
    border-collapse: collapse;
    margin: 1rem 0;
}
.tiptap-editor .ProseMirror th,
.tiptap-editor .ProseMirror td {
    border: 1px solid #e5e7eb;
    padding: 0.5rem;
}
.tiptap-editor .ProseMirror code {
    background: #f3f4f6;
    border-radius: 0.25rem;
    padding: 0.125rem 0.25rem;
}
```

**Step 5 — Tests:**

Create `tests/Feature/Composer/TiptapIntegrationTest.php`:
- Test TiptapEditor.js module exists and exports initTiptapEditor
- Test composer Blade template contains Tiptap editor container (wire:ignore, x-ref="editor")
- Test hidden textarea receives content from editor via syncBodyFromEditor
- Test toolbar buttons render with correct aria labels
- Test quote component renders attribution and collapsible content

**What to avoid:**
- Do NOT use `wire:model.live` on Tiptap content — sync via hidden textarea + onUpdate callback
- Do NOT render Tiptap content server-side — it's entirely client-side
- Do NOT use inline `<script>` in Blade for Tiptap — use the JS module imported via Vite
- Do NOT allow base64 image embedding in Tiptap — Image extension configured with allowBase64: false
  </action>
  <verify>
    <automated>cd /home/snigdha/Desktop/OpenMail && npm run build 2>&1 | tail -5; echo "---TESTS---"; php artisan test --filter=TiptapIntegrationTest 2>&1 | tail -5; echo "---ALL---"; php artisan test 2>&1 | tail -10</automated>
    <fails_when>non-zero exit from npm build, or "0 passed" in test summary, or any test failure</fails_when>
  </verify>
  <done>
    - Tiptap editor renders in composer with full toolbar (bold, italic, underline, headings, lists, tables, links, etc.)
    - Editor content syncs to Livewire hidden textarea via onUpdate callback
    - Toolbar buttons show active state when formatting is applied
    - Collapsible quote blocks render in reply/forward with show/hide toggle
    - Tiptap CSS styles editor content with proper typography
    - Vite build includes Tiptap and all extensions
    - Tests pass for Tiptap integration
  </done>
  <reversibility rating="reversible">Extends existing TiptapEditor.js scaffold; modifies composer Blade template; adds CSS</reversibility>
</task>

<task type="auto">
  <name>Task 2: Draft autosave + undo send toast + ProcessPendingSends scheduler</name>
  <files>
    app/Livewire/Mailbox/Composer.php,
    resources/views/livewire/mailbox/composer.blade.php,
    resources/views/components/undo-send-toast.blade.php,
    app/Console/Commands/ProcessPendingSends.php,
    routes/console.php,
    tests/Feature/Composer/DraftAutosaveTest.php,
    tests/Feature/Composer/UndoSendTest.php
  </files>
  <read_first>
    app/Livewire/Mailbox/Composer.php,
    resources/views/livewire/mailbox/composer.blade.php,
    app/Services/ComposerService.php,
    app/Models/PendingSend.php,
    routes/console.php
  </read_first>
  <action>
**Step 1 — Draft Autosave (per D-05, D-06, D-07, D-08):**

Modify `app/Livewire/Mailbox/Composer.php`:

Add `$lastSavedAt` property and `$autosaveStatus` property ('idle', 'saving', 'saved', 'error')

Add `updatedBodyHtml()` listener (fires when Tiptap syncs content):
- Dispatch browser event `autosave-draft` with composition data
- Debounce is handled client-side (1500ms per UI-SPEC)

Add `saveDraftToLocalStorage()` method (called from JS):
- Dispatch browser event `save-to-localstorage` with key `openmail:draft:{compositionId}` and JSON data

Add `loadDraftFromLocalStorage(string $compositionId)` method:
- Dispatch browser event `load-from-localstorage` with key
- Return data to populate form

Add `syncDraftToImap()` method:
- Call `$this->composerService->saveDraft(auth()->id(), $this->getComposerData(), $this->draftUid)`
- Update `$this->draftUid` with returned UID
- Update `$this->autosaveStatus = 'saved'`
- Set `$this->lastSavedAt = now()`

Modify `resources/views/livewire/mailbox/composer.blade.php`:
- Add autosave indicator in toolbar: `<span class="text-xs text-gray-500" x-data="{ status: @entangle('autosaveStatus') }"><span x-show="status === 'saving'">Saving...</span><span x-show="status === 'saved'">Saved</span></span>`
- Add Alpine.js `x-init` on composer modal that:
  - On mount: check LocalStorage for draft, load if exists
  - On `autosave-draft` event: debounce 1500ms, save to LocalStorage, then call `$wire.syncDraftToImap()` on interval (every 30 seconds)
  - On send: clear LocalStorage key, call `$wire.deleteDraftFromImap()`
  - On discard: clear LocalStorage key

**Step 2 — Undo Send Toast (per D-21, D-23):**

Modify `app/Livewire/Mailbox/Composer.php`:
- Add `$pendingSendId = null` property
- Add `$undoSendDelay` property (loaded from config)
- Add `$showUndoToast = false` property
- In `send()` method: after successful send (or queued), set `$this->showUndoToast = true`, `$this->pendingSendId = $result['pending_send_id'] ?? null`
- Add `undoSend()` method:
  - Call `$this->composerService->undoSend($this->pendingSendId, auth()->id())`
  - Set `$this->showUndoToast = false`
  - Dispatch toast 'Message moved to Drafts'

Create `resources/views/components/undo-send-toast.blade.php`:
- Alpine.js component per UI-SPEC and RESEARCH.md Pattern 4
- Fixed bottom-right positioning (`fixed bottom-4 right-4 z-50`)
- Progress bar counting down from 100% to 0%
- "Undo" button that calls `$wire.undoSend()`
- "Dismiss" button (X icon) that hides toast without undo
- Auto-hide when countdown reaches 0
- Entrance animation: slide up from bottom
- `role="alert" aria-live="polite"` for accessibility

Modify composer Blade to include the toast:
```html
@if($showUndoToast)
    <x-undo-send-toast :delay="$undoSendDelay" :pending-send-id="$pendingSendId" />
@endif
```

**Step 3 — ProcessPendingSends Scheduler Command (per D-19):**

Create `app/Console/Commands/ProcessPendingSends.php`:
- `$signature = 'pending-sends:process'`
- `$description = 'Process pending email sends that are due'`
- `handle()` method:
  - Query PendingSend where status='pending' AND send_at <= now(), limit 50
  - For each pending send:
    - Decode message_json
    - Try to send via ComposerService::sendMessage()
    - On success: update status to 'sent'
    - On failure: increment retry_count, if retry_count >= 3 set status='failed', else reschedule with exponential backoff (send_at = now() + 2^retry_count minutes)
  - Return 0

Modify `routes/console.php`:
```php
use App\Console\Commands\ProcessPendingSends;

Schedule::command('pending-sends:process')->everyMinute()->withoutOverlapping(5);
```

**Step 4 — Draft Autosave Tests:**

Create `tests/Feature/Composer/DraftAutosaveTest.php`:
- Test LocalStorage key format matches `openmail:draft:{uuid}`
- Test autosave debounce fires after 1500ms of inactivity
- Test IMAP APPEND to Drafts folder on sync
- Test existing draft UID is used for update (delete old + append new)
- Test discard clears LocalStorage and deletes from IMAP Drafts

**Step 5 — Undo Send Tests:**

Create `tests/Feature/Composer/UndoSendTest.php`:
- Test pending_sends record created on send with correct send_at timestamp
- Test undoSend cancels pending send and updates status
- Test undoSend moves message to Drafts via IMAP APPEND
- Test ProcessPendingSends processes due records
- Test ProcessPendingSends retries failed sends with backoff
- Test ProcessPendingSends marks as failed after 3 retries

**What to avoid:**
- Do NOT use `setInterval` for IMAP sync — use a debounce + interval combination
- Do NOT allow undo after status is 'sent' — only 'pending' status can be undone
- Do NOT process more than 50 pending sends per scheduler run — prevent overload
- Do NOT store IMAP passwords in PendingSend — only message data
  </action>
  <verify>
    <automated>cd /home/snigdha/Desktop/OpenMail && php artisan test --filter=DraftAutosaveTest 2>&1 | tail -5; echo "---UNDO---"; php artisan test --filter=UndoSendTest 2>&1 | tail -5; echo "---SCHEDULER---"; php artisan pending-sends:process 2>&1 | tail -3; echo "---ALL---"; php artisan test 2>&1 | tail -10</automated>
    <fails_when>non-zero exit from test, or "0 passed" in summary, or scheduler command fails</fails_when>
  </verify>
  <done>
    - Draft autosave indicator shows "Saving..." then "Saved" in composer toolbar
    - LocalStorage saves draft on 1500ms debounce after keystroke
    - IMAP Drafts folder receives draft via APPEND every 30 seconds
    - Existing drafts are updated (delete old + append new) not duplicated
    - Undo send toast appears after sending with countdown progress bar
    - Undo button cancels pending send and moves message to Drafts
    - ProcessPendingSends command processes due records and retries failures
    - Scheduler registered in routes/console.php for every-minute execution
    - All tests pass for draft autosave and undo send
  </done>
  <reversibility rating="reversible">Extends Composer component; adds new Blade component; adds Artisan command</reversibility>
</task>

<task type="auto">
  <name>Task 3: Recipient chips with autocomplete + attachment drag-drop + comprehensive tests</name>
  <files>
    app/Livewire/Mailbox/Composer.php,
    resources/views/livewire/mailbox/composer.blade.php,
    resources/views/components/composer-recipient-chips.blade.php,
    app/Services/ContactAutocompleteService.php,
    app/Services/ComposerService.php,
    tests/Feature/Composer/ContactAutocompleteTest.php
  </files>
  <read_first>
    app/Livewire/Mailbox/Composer.php,
    resources/views/livewire/mailbox/composer.blade.php,
    app/Services/ImapMailboxService.php,
    app/Services/ComposerService.php,
    app/Models/PendingSend.php
  </read_first>
  <action>
**Step 1 — ContactAutocompleteService (per D-25, D-26):**

Create `app/Services/ContactAutocompleteService.php`:

Constructor: inject `ImapMailboxService`, `FolderMapper`

`refreshCache(int $userId): int`:
- Call `$this->imapService->searchRecipients(100)` to get recent recipients from IMAP
- For each recipient, upsert to `contact_autocomplete_cache` table:
  - Set `user_id`, `email`, `name`, `frequency` (increment on match), `last_used_at`, `expires_at` (now + 7 days)
- Return count of cached contacts

`search(int $userId, string $query, int $limit = 10): \Illuminate\Support\Collection`:
- Query `contact_autocomplete_cache` where `user_id` matches, `expires_at` > now
- Filter by email or name LIKE `%$query%`
- Order by frequency descending
- Limit to `$limit` results
- Return collection with `email`, `name`, `frequency` fields

`getRecentRecipients(int $userId, int $limit = 10): \Illuminate\Support\Collection`:
- Return top `$limit` contacts by frequency (most-used recipients)

**Step 2 — Recipient Chips Component (per D-27, D-28):**

Create `resources/views/components/composer-recipient-chips.blade.php`:
- Accept props: `$field` (to/cc/bcc), `$value` (string of emails), `$label` (string), `$showSuggestions` (bool)
- Alpine.js `x-data` managing:
  - `$chips`: array of `{email, name}` objects parsed from value
  - `$input`: current typed text
  - `$suggestions`: array from autocomplete API
  - `$showDropdown`: bool
  - `$selectedIndex`: for keyboard navigation
- Parse logic: split value by commas, trim, create chip objects
- Input handling:
  - On comma or Enter: create chip from input text, clear input, trigger autocomplete search
  - On backspace with empty input: remove last chip
  - On input: debounce 200ms, fetch autocomplete suggestions
- Chip display: inline-flex with email/name text + × remove button
- Dropdown: below input, shows suggestions with email + name, keyboard navigable (↑/↓/Enter/Tab/Escape)
- `aria-activedescendant`, `role="listbox"`, `role="option"` for accessibility
- Update parent Livewire value on chip add/remove via `$wire.set('fieldName', newValue)`

**Step 3 — Integrate Chips into Composer:**

Modify `resources/views/livewire/mailbox/composer.blade.php`:
- Replace plain To/CC/BCC text inputs with `<x-composer-recipient-chips>` component
- Each field gets its own chip component instance
- CC/BCC sections hidden by default, expand on toggle per D-03

Modify `app/Livewire/Mailbox/Composer.php`:
- Add `searchContacts(string $query)` Livewire method:
  - Call `ContactAutocompleteService::search(auth()->id(), $query)`
  - Return JSON array of suggestions
- Add `refreshContactCache()` method:
  - Call `ContactAutocompleteService::refreshCache(auth()->id())`
  - Dispatch event when cache is refreshed

**Step 4 — Attachment Drag-and-Drop Enhancement (per D-13):**

Modify `resources/views/livewire/mailbox/composer.blade.php`:
- Enhance the attachment drop zone with Alpine.js drag-and-drop:
  - `@dragover.prevent` and `@drop.prevent` on the drop zone
  - On drop: extract files from `event.dataTransfer.files`, call `$wire.set('attachments', ...)` for each
  - Visual feedback: border color changes on dragover (`border-blue-500 bg-blue-50`)
  - Click to browse: hidden file input triggered by button click
  - Show upload progress per file using Livewire's `wire:loading` and progress events

Modify `app/Livewire/Mailbox/Composer.php`:
- Add `updatedAttachments()` validation:
  - Check each file ≤ 25MB per D-14
  - Check total ≤ 50MB per D-14
  - Validate MIME type against allowlist: images (jpg, png, gif, webp), PDF, Office (doc, docx, xls, xlsx, ppt, pptx), text (txt, csv), zip
  - Add errors for violations

**Step 5 — Comprehensive Phase Tests:**

Create `tests/Feature/Composer/ContactAutocompleteTest.php`:
- Test searchContacts returns matching recipients from cache
- Test searchContacts respects limit parameter
- Test refreshCache populates from IMAP Sent/Inbox
- Test cache expires after 7 days
- Test search works with partial email/name matches

Add to existing test files:
- TiptapIntegrationTest: test editor renders with toolbar, content syncs to Livewire
- DraftAutosaveTest: test LocalStorage and IMAP Drafts integration
- UndoSendTest: test toast appearance, countdown, cancel action
- ComposerIntegrationTest: test full compose-send-receive loop with all features

**What to avoid:**
- Do NOT make autocomplete API calls synchronous — debounce 200ms minimum
- Do NOT allow drag-and-drop of folders — only files
- Do NOT bypass MIME type validation — enforce allowlist strictly
- Do NOT store autocomplete results indefinitely — TTL of 7 days per D-26
  </action>
  <verify>
    <automated>cd /home/snigdha/Desktop/OpenMail && php artisan test --filter=ContactAutocompleteTest 2>&1 | tail -5; echo "---ALL_COMPOSER---"; php artisan test --filter=Composer 2>&1 | tail -5; echo "---FULL_SUITE---"; php artisan test 2>&1 | tail -10; echo "---NPM---"; npm run build 2>&1 | tail -3</automated>
    <fails_when>non-zero exit from any test, or "0 passed" in summary, or npm build fails</fails_when>
  </verify>
  <done>
    - Recipient chips render for To/CC/BCC with add/remove functionality
    - Autocomplete dropdown shows suggestions from IMAP recipients
    - Keyboard navigation works in autocomplete (↑/↓/Enter/Tab/Escape)
    - CC/BCC sections expand/collapse with toggle buttons
    - Attachment drag-and-drop zone accepts files with visual feedback
    - File size and MIME type validation rejects invalid attachments
    - ContactAutocompleteService caches and searches recipients correctly
    - All composer-related tests pass (Tiptap, drafts, undo, autocomplete)
    - Full test suite passes with no regressions
    - npm build succeeds with all assets
  </done>
  <reversibility rating="reversible">New Blade component; extends Composer; adds ContactAutocompleteService</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Tiptap Editor | Rich text content crosses from ProseMirror to Livewire — must sanitize before server processing |
| Browser → Autocomplete API | Search queries cross to server — must validate input, prevent injection |
| ComposerService → IMAP Drafts | Draft content crosses to IMAP server — must use authenticated connection, handle APPEND failures |
| PendingSend → Scheduler | Database queue crosses to background process — must prevent duplicate sends, handle race conditions |
| Attachment Upload → Validation | File uploads cross to server — must enforce size/MIME limits before temp storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering | Tiptap editor content | medium | mitigate | Sanitize editor HTML through MessageSanitizer before sending; strip dangerous attributes |
| T-03-09 | Information Disclosure | Autocomplete cache | low | accept | Cache stores only email/name/frequency; no sensitive data; 7-day TTL |
| T-03-10 | Denial of Service | Autocomplete search | low | mitigate | Limit search results to 10; debounce API calls; cache IMAP results |
| T-03-11 | Tampering | Draft autosave | medium | mitigate | Scope drafts to user_id; validate composition_id UUID format; prevent cross-user draft access |
| T-03-12 | Elevation of Privilege | Undo send | medium | mitigate | Verify PendingSend belongs to authenticated user before cancel; prevent race conditions with status check |
| T-03-13 | Repudiation | Send/draft actions | low | accept | Logging deferred to Phase 5 (SEC-05); current: status tracking in pending_sends table |
</threat_model>

<verification>
- Tiptap editor renders with full toolbar and all formatting options work
- Editor content syncs to Livewire form on every change
- Draft autosave saves to LocalStorage on 1500ms debounce
- Draft syncs to IMAP Drafts folder every 30 seconds
- Existing drafts are updated (not duplicated) on sync
- Undo send toast appears with countdown and Cancel button
- Undo send moves message to Drafts and cancels SMTP delivery
- ProcessPendingSends processes due records every minute
- Recipient chips display for To/CC/BCC with autocomplete suggestions
- Autocomplete searches IMAP recipients and returns results
- Attachment drag-and-drop works with file validation
- All tests pass including new composer feature tests
- Full test suite has no regressions
</verification>

<success_criteria>
- Tiptap editor provides full rich text editing with toolbar per D-04
- Draft autosave works instantly (LocalStorage) and reliably (IMAP sync) per D-05/D-06/D-07
- Undo send provides safety net with configurable delay per D-21/D-23
- Contact autocomplete suggests real recipients from mail history per D-25/D-26/D-27
- Reply/forward quoted text is collapsible per D-09
- Attachments support drag-and-drop with validation per D-13/D-14
- ProcessPendingSends scheduler handles retry with exponential backoff per D-19
- All COMP-01 through COMP-13 requirements are met
- Full test suite passes with no regressions from Plan 1 or prior phases
</success_criteria>

<output>
Create `.planning/phases/03-compose-send/03-02-SUMMARY.md` when done
</output>
