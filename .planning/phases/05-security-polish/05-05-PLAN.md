---
phase: 05-security-polish
plan: 05
type: execute
wave: 4
depends_on:
  - "04"
files_modified:
  - database/migrations/create_signatures_table.php
  - app/Models/Signature.php
  - app/Services/SignatureService.php
  - app/Livewire/Settings/SignaturesTab.php
  - resources/views/livewire/settings/signatures-tab.blade.php
  - resources/views/components/tiptap-editor.blade.php
  - resources/views/components/signature-dropdown.blade.php
  - app/Http/Requests/SignatureRequest.php
  - routes/web.php
autonomous: true
requirements:
  - SET-02
user_setup: []
estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - Users can create multiple rich-text signatures via Tiptap editor in modal
    - One signature can be marked as default per user
    - Default signature auto-inserts on new compose
    - Composer has dropdown to swap/remove signatures
    - Signatures stored as Tiptap JSON, rendered as sanitized HTML
    - Signature CRUD accessible in Settings → Signatures tab
  artifacts:
    - database/migrations/create_signatures_table.php
    - app/Models/Signature.php
    - app/Services/SignatureService.php
    - app/Livewire/Settings/SignaturesTab.php
    - resources/views/livewire/settings/signatures-tab.blade.php
    - resources/views/components/tiptap-editor.blade.php (reusable)
    - resources/views/components/signature-dropdown.blade.php
    - app/Http/Requests/SignatureRequest.php
    - routes/web.php (signature CRUD routes)
  key_links:
    - Signature model → signatures table → user relationship
    - SignatureService → Signature model → default enforcement via boot()
    - SignaturesTab → SignatureService → CRUD operations
    - tiptap-editor component → SignatureService → sanitize via MessageSanitizer
    - signature-dropdown in Composer → SignatureService.getDefault() → auto-insert
---

<objective>
Implement signature management with rich-text editing, multiple signatures, default selection, and composer integration per D-13 through D-16 and SET-02.

Purpose: Allow users to create and manage multiple formatted signatures with a default that auto-inserts on compose, using the existing Tiptap editor configuration from Phase 3.

Output: signatures table migration, Signature model with sanitization, SignatureService, SignaturesTab with Tiptap modal, reusable tiptap-editor component, signature-dropdown for composer, and routes.
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-security-polish/05-CONTEXT.md
@.planning/phases/05-security-polish/05-RESEARCH.md
@.planning/phases/05-security-polish/05-PATTERNS.md
@.planning/phases/05-security-polish/05-01-PLAN.md
@.planning/phases/05-security-polish/05-02-PLAN.md
@.planning/phases/05-security-polish/05-03-PLAN.md
@.planning/phases/05-security-polish/05-04-PLAN.md
</context>

<tasks>

<task id="05-tracer" type="execute">
  <name>End-to-end signature management — migration, model, service, Settings tab, Tiptap modal, composer dropdown</name>
  <read_first>
    - database/migrations/create_signatures_table.php
    - app/Models/Signature.php
    - app/Services/SignatureService.php
    - app/Livewire/Settings/SignaturesTab.php
    - resources/views/livewire/settings/signatures-tab.blade.php
    - resources/views/components/tiptap-editor.blade.php
    - resources/views/components/signature-dropdown.blade.php
    - app/Http/Requests/SignatureRequest.php
    - routes/web.php
  </read_first>
  <action>
    Build signature management vertical slice per D-13 through D-16. Implement: (1) create_signatures_table migration with id, user_id (FK cascade), name, content_json (Tiptap JSON), content_html (cached sanitized), is_default, timestamps; index on [user_id, is_default]; (2) Signature model with fillable, casts (content_json=array, is_default=boolean), user relationship, boot() saving: convert content_json to HTML via tiptap-php, sanitize via MessageSanitizer::sanitizeSignatureHtml(), enforce single default per user; (3) SignatureService with getDefault(), create(), update(), delete(), setDefault() — delete promotes newest to default if deleted was default; (4) SignaturesTab Livewire component: signatures collection, openCreateModal(), openEditModal(signature), saveSignature(), deleteSignature(), setDefaultSignature(), uses SignatureService; (5) signatures-tab.blade.php: list signatures with name, default badge, edit/delete buttons; "New Signature" button opens modal; (6) tiptap-editor.blade.php: reusable Blade component wrapping Tiptap editor (reuse Phase 3 composer config), accepts content_json prop, emits content_json on change, includes @vite for Tiptap CSS; (7) signature-dropdown.blade.php: Alpine.js component with signatures list, default signature display, dropdown to select/remove, "Manage signatures…" link opening Settings modal; (8) SignatureRequest validation: name required|string|max:100, content_json required|array, is_default boolean; (9) Add signature CRUD routes in routes/web.php: GET/POST /settings/signatures, PUT/DELETE /settings/signatures/{id}, POST /settings/signatures/{id}/default.
  </action>
  <verify>
    <automated>php artisan migrate --dry-run 2>&1 | grep -c signatures && php -r "require 'vendor/autoload.php'; $app = require 'bootstrap/app.php'; $app->make('Illuminate\Contracts\Console\Kernel')->bootstrap(); echo 'Signature: ' . (class_exists('App\Models\Signature') ? 'exists' : 'missing') . PHP_EOL; echo 'SignatureService: ' . (class_exists('App\Services\SignatureService') ? 'exists' : 'missing') . PHP_EOL; echo 'SignaturesTab: ' . (class_exists('App\Livewire\Settings\SignaturesTab') ? 'exists' : 'missing') . PHP_EOL;"</automated>
    <fails_when>Migration missing, Signature model missing boot() sanitization, SignatureService missing methods, SignaturesTab missing CRUD actions, tiptap-editor component missing, signature-dropdown missing, routes missing</fails_when>
  </verify>
  <acceptance_criteria>
    - create_signatures_table migration creates table with all columns and [user_id, is_default] index
    - Signature model sanitizes HTML on save via MessageSanitizer and enforces single default
    - SignatureService has getDefault, create, update, delete, setDefault
    - SignaturesTab manages signatures list, modal create/edit, default toggle, delete
    - signatures-tab.blade.php renders list with default badge, edit/delete, new button
    - tiptap-editor.blade.php reusable component works for signatures (reads/writes content_json)
    - signature-dropdown.blade.php Alpine.js component shows default, dropdown to swap, manage link
    - SignatureRequest validates name, content_json, is_default
    - Signature CRUD routes registered under /settings/signatures
  </acceptance_criteria>
  <reversibility rating="costly">Schema change (signatures table); removing requires data migration; model boot() logic couples to MessageSanitizer</reversibility>
</task>

<task id="05-verify-signatures" type="verify">
  <name>Verify signature CRUD, default logic, and composer integration</name>
  <read_first>
    - app/Models/Signature.php
    - app/Services/SignatureService.php
    - app/Livewire/Settings/SignaturesTab.php
    - resources/views/livewire/settings/signatures-tab.blade.php
    - resources/views/components/signature-dropdown.blade.php
    - app/Services/ComposerService.php
  </read_first>
  <action>
    Verify signature functionality: (1) Create signature via modal → verify stored as Tiptap JSON, content_html sanitized; (2) Create second signature → verify first remains default; (3) Set second as default → verify first unset, second default; (4) Delete default signature → verify next newest promoted to default; (5) Open composer → verify default signature auto-inserted at cursor; (6) Click signature dropdown → verify all signatures listed, default badge shown; (7) Select different signature → verify composer content updates; (8) Remove signature in dropdown → verify signature removed from composer; (9) Verify XSS attempt in signature (onload, javascript:) blocked by sanitizer.
  </action>
  <verify>
    <automated>php artisan test --filter=SignatureTest 2>/dev/null || echo "Manual: test signature CRUD in Settings, verify composer auto-insert and dropdown"</automated>
    <fails_when>Signature not sanitized, multiple defaults allowed, default not auto-inserted, dropdown not functional, XSS not blocked</fails_when>
  </verify>
  <acceptance_criteria>
    - Multiple signatures creatable per user with rich text (bold, italic, links)
    - Only one default per user enforced at model and service level
    - Default signature auto-inserted on new compose (not reply/forward unless configured)
    - Signature dropdown in composer shows all signatures with default indicator
    - Switching signature in dropdown updates composer body
    - Deleting default promotes another signature to default
    - Signature HTML sanitized (no scripts, event handlers, javascript: URLs)
    - Tiptap editor in modal reuses Phase 3 configuration
  </acceptance_criteria>
</task>

<task id="05-composer-integration" type="execute">
  <name>Integrate signature dropdown and auto-insert into Composer component</name>
  <read_first>
    - app/Livewire/Mailbox/Composer.php
    - resources/views/livewire/mailbox/composer.blade.php
    - app/Services/ComposerService.php
    - resources/views/components/signature-dropdown.blade.php
  </read_first>
  <action>
    Wire signatures into composer: (1) In Composer.php mount(): load user signatures via SignatureService, set defaultSignature property; (2) In Composer.php: add setSignature(signatureId) method updating body with selected signature HTML; (3) In composer.blade.php: include <x-signature-dropdown :signatures="$signatures" :default-signature="$defaultSignature" /> in toolbar; (4) In ComposerService::buildMimeMessage(): if mode=compose and no signature_id provided, auto-append default signature HTML with <br><br> separator; (5) Ensure reply/forward modes don't auto-insert unless user triggers via dropdown; (6) Verify signature HTML sanitized before insertion (already done in model boot).
  </action>
  <verify>
    <automated>php artisan test --filter=ComposerSignatureTest 2>/dev/null || echo "Manual: open composer, verify default signature inserted, dropdown switches signatures"</automated>
    <fails_when>Composer doesn't load signatures, default not auto-inserted, dropdown missing, setSignature not updating body</fails_when>
  </verify>
  <acceptance_criteria>
    - Composer loads user signatures on mount
    - Default signature auto-appended on new compose (with separator)
    - Signature dropdown in toolbar shows all signatures
    - Clicking signature in dropdown replaces/appends signature in body
    - Reply/forward don't auto-insert (user must select from dropdown)
    - Signature HTML already sanitized from model storage
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → SignaturesTab | Signature content submitted via Tiptap editor |
| Signature model → Database | Sanitized HTML stored, JSON preserved |
| Composer → SignatureService | Default signature fetched for auto-insert |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-17 | Tampering | Signature XSS via Tiptap | critical | mitigate | Store only Tiptap JSON; render via tiptap-php → MessageSanitizer::sanitizeSignatureHtml() (reuses email allowlist) |
| T-05-18 | Tampering | Multiple default signatures | low | mitigate | Model boot() enforces single default; SignatureService.setDefault() unsets others |
| T-05-19 | Information Disclosure | Signature content leakage | low | mitigate | User-scoped queries; signatures only accessible to owner |
| T-05-20 | Denial of Service | Large signature content | low | mitigate | Validate content_json size; Tiptap JSON structure limits |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Package legitimacy audit in RESEARCH.md shows all approved; no new installs this phase |
</threat_model>

<verification>
Overall phase checks:
- Signatures tab in Settings allows create/edit/delete/set-default
- Tiptap editor in modal provides rich text (bold, italic, underline, links)
- Only one default signature per user at any time
- Default signature auto-inserts on new compose with separator
- Composer dropdown lists all signatures, allows switching
- Signature HTML sanitized (no scripts, event handlers, dangerous URLs)
- Deleting default promotes another signature
</verification>

<success_criteria>
Signature management fully functional: CRUD in Settings, rich-text editing, default logic, composer auto-insert and dropdown swap, all content sanitized.
</success_criteria>

<output>
Create .planning/phases/05-security-polish/05-05-SUMMARY.md when done
</output>