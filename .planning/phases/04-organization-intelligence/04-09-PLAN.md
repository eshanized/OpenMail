---
phase: 04-organization-intelligence
plan: 09
type: execute
wave: 4
depends_on: ["04-05", "04-06", "04-07", "04-08"]
files_modified:
  - resources/views/layouts/mailbox.blade.php
  - app/Livewire/Mailbox/FolderSidebar.php
  - resources/views/livewire/mailbox/folder-sidebar.blade.php
  - app/Livewire/Mailbox/MessageList.php
  - resources/views/livewire/mailbox/thread-row.blade.php
  - resources/views/livewire/mailbox/search-bar.blade.php
  - routes/web.php
  - tests/Feature/IntegrationTest.php
  - tests/Feature/SearchResultsPageTest.php
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-06, THR-04, CONT-03, LBL-01, LBL-03, LBL-04, LBL-05]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Tabbed sidebar: Folders | Contacts | Labels with mobile bottom navigation (D-19, UI-SPEC)"
    - "Thread toggle button in toolbar persists per folder in localStorage (D-18)"
    - "Keyboard shortcuts: / focuses search, t toggles thread mode, e archives (UI-SPEC)"
    - "Search highlighting works in results page snippets (SRCH-06)"
    - "All Phase 4 features integrated and tested together"
  artifacts:
    - "resources/views/layouts/mailbox.blade.php (tabbed sidebar)"
    - "app/Livewire/Mailbox/FolderSidebar.php (tabs state)"
    - "resources/views/livewire/mailbox/folder-sidebar.blade.php (tab UI)"
    - "resources/views/livewire/mailbox/thread-row.blade.php (keyboard shortcuts)"
  key_links:
    - "FolderSidebar tabs ↔ ContactSidebar/LabelSidebar panels (shared state)"
    - "MessageList.threadMode ↔ localStorage per folder (D-18)"
    - "SearchBar '/' shortcut → focus input (Alpine.js)"
    - "MessageToolbar 'e' key → archive (from Plan 8)"
    - "Thread row Arrow keys → expand/collapse (from Plan 5)"

---

<objective>
Integrate all Phase 4 features into a cohesive UI: tabbed sidebar (Folders/Contacts/Labels), thread toggle with persistence, keyboard shortcuts, mobile responsive behavior (bottom navigation per Open Question 4), and full integration testing. Resolves mobile sidebar behavior (Open Question 4).
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-organization-intelligence/04-CONTEXT.md
@.planning/phases/04-organization-intelligence/04-RESEARCH.md
@.planning/phases/04-organization-intelligence/04-UI-SPEC.md
@.planning/phases/02-mailbox-core/02-CONTEXT.md
@.planning/phases/03-compose-send/03-CONTEXT.md
@resources/views/layouts/mailbox.blade.php
@app/Livewire/Mailbox/FolderSidebar.php
@app/Livewire/Mailbox/MessageList.php
@app/Livewire/Mailbox/SearchBar.php
@app/Livewire/Mailbox/ContactSidebar.php
@app/Livewire/Mailbox/LabelSidebar.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tabbed sidebar (Folders | Contacts | Labels) + mobile bottom navigation</name>
  <files>
    resources/views/layouts/mailbox.blade.php
    app/Livewire/Mailbox/FolderSidebar.php
    resources/views/livewire/mailbox/folder-sidebar.blade.php
    tests/Feature/IntegrationTest.php
  </files>
  <action>
    Transform existing folder sidebar into tabbed sidebar per D-19, UI-SPEC §3.1, §4.1. Resolves RESEARCH.md Open Question 4: bottom navigation for <768px.

    1. FolderSidebar.php:
       - Add property: activeTab = 'folders' (default). Persist in session/localStorage.
       - Properties for child components: showContactsPanel, showLabelsPanel (computed from activeTab)
       - Method setActiveTab(tab): updates activeTab, emits event for child panels
       - Listen for 'set-active-tab' events from keyboard shortcuts

    2. folder-sidebar.blade.php per UI-SPEC §3.1, §4.1, D-19 (Open Question 4 resolved):
       - Desktop (≥768px): Tab bar at top of sidebar — "Folders", "Contacts", "Labels" with icons. Active tab highlighted (blue-50 bg, blue-700 text, border-l-2).
       - Each tab panel: x-show="activeTab === 'folders'" etc.
       - Folders panel: existing folder tree (FolderSidebar component)
       - Contacts panel: ContactSidebar component (from Plan 7)
       - Labels panel: LabelSidebar component (from Plan 8)
       - Mobile (<768px): Tabs become fixed bottom navigation (fixed bottom-0 left-0 right-0, safe-area-inset-bottom, z-40). 3 equal-width buttons with icons + labels. Panels become full-screen drawers sliding up from bottom (x-transition, fixed inset-0 z-50 bg-white). Click tab → slide up corresponding panel. Backdrop click closes drawer. Active tab highlighted in bottom nav.

    3. layouts/mailbox.blade.php:
       - Update sidebar slot to use new tabbed folder-sidebar
       - Ensure mobile drawer z-index works with search dropdown (z-50) and label popovers
       - Add Alpine.js store for sidebar mobile state (openPanel: 'folders'|'contacts'|'labels'|null)
       - Keyboard shortcut 't' for thread toggle, '/' for search focus (global listeners)

    4. ContactSidebar and LabelSidebar: Ensure they work as panels inside tab (no independent layout). They receive parent tab state via props or events.

    5. Add integration test: testTabbedSidebar — verifies tab switching works, mobile bottom nav appears at <768px, panels render correctly, drawer animation smooth.
  </action>
  <verify>
    <automated>cd /home/snigdha/Desktop/OpenMail && ./vendor/bin/pest tests/Feature/IntegrationTest.php::testTabbedSidebar 2>&1 | tail -15</automated>
    <fails_when>non-zero exit, or "0 passed" in summary, or test shows "FAIL"</fails_when>
  </verify>
  <done>
    Sidebar has 3 tabs: Folders, Contacts, Labels. Desktop: tabs at top, panels switch instantly. Mobile: fixed bottom nav with safe-area-inset, panels slide up as drawers. All three panels (folder tree, contacts, labels) work within tabs. State persists in localStorage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Keyboard shortcuts integration + thread toggle persistence</name>
  <files>
    app/Livewire/Mailbox/MessageList.php
    resources/views/livewire/mailbox/message-list.blade.php
    resources/views/livewire/mailbox/thread-row.blade.php
    resources/views/livewire/mailbox/search-bar.blade.php
    tests/Feature/ThreadUITest.php
    tests/Feature/IntegrationTest.php
  </files>
  <action>
    Implement keyboard shortcuts and thread toggle persistence per D-18, UI-SPEC §2.3, §4.5.

    1. MessageList.php enhancements:
       - threadMode property: loaded from localStorage key `openmail:threadMode:{folderPath}` on mount (default 'threaded' for Inbox, 'flat' for Sent/Drafts/Trash/Spam per UI-SPEC)
       - toggleThreadMode(): switches mode, saves to localStorage via JavaScript dispatch, re-renders message list
       - Add listener for 'toggle-thread-mode' from Alpine.js keyboard shortcut

    2. message-list.blade.php toolbar additions:
       - Thread toggle button next to sort controls per UI-SPEC §2.3:
         - Icon: stacked chat bubbles (threaded) ↔ single chat bubble (flat)
         - Active state: blue-600 + bg-blue-50, Inactive: gray-600
         - wire:click="toggleThreadMode"
         - Tooltip: "Threaded view (t)"
       - Alpine.js keyboard listener on message list container (x-data):
         - '/' → dispatch 'focus-search' to SearchBar
         - 't' → dispatch 'toggle-thread-mode' to MessageList
         - 'e' → archive selected (dispatch to MessageToolbar, already in Plan 8)
         - Arrow Right/Left on thread row → expand/collapse (already in thread-row)

    3. thread-row.blade.php keyboard enhancements (from Plan 5):
       - Ensure Arrow Right expands, Arrow Left collapses (already specified)
       - Enter/Space toggles (already specified)
       - Focus management: when expanded, first child focusable

    4. SearchBar.php: Add listener for 'focus-search' event to focus input.

    5. Add feature tests:
       - testThreadTogglePersistence — switches to Inbox, toggles to Flat, navigates to Sent (verifies default Flat), back to Inbox (verifies Threaded persisted)
       - testKeyboardShortcuts — verifies / focuses search, t toggles thread, e archives
       - testMobileKeyboardShortcuts — verifies shortcuts work on mobile viewport
  </action>
  <verify>
    <automated>cd /home/snigdha/Desktop/OpenMail && ./vendor/bin/pest tests/Feature/ThreadUITest.php::testThreadTogglePersistence tests/Feature/ThreadUITest.php::testKeyboardShortcuts tests/Feature/IntegrationTest.php::testKeyboardShortcuts 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or "0 passed" in summary, or any test shows "FAIL"</fails_when>
  </verify>
  <done>
    Thread toggle button in toolbar works. Mode persists per folder in localStorage (threaded for Inbox, flat for others by default). Keyboard shortcuts: / focuses search, t toggles thread mode, e archives. Thread row Arrow keys expand/collapse. All integrated without conflicts. Works on mobile.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full integration test suite + search highlight verification + polish + asset build</name>
  <files>
    tests/Feature/IntegrationTest.php
    tests/Feature/SearchResultsPageTest.php
    resources/views/livewire/mailbox/search-results-page.blade.php
    resources/views/livewire/mailbox/search-results-dropdown.blade.php
  </files>
  <action>
    Final integration verification and polish across all Phase 4 features.

    1. Search highlight verification (SRCH-06, SEC-01):
       - In search-results-page.blade.php and search-results-dropdown.blade.php: ensure snippets pass through DOMPurify BEFORE highlight wrapping
       - Use MessageSanitizer::sanitizeForDisplay() on snippet, then highlight matches
       - Test: search for term in HTML message, verify <mark> wraps match, no XSS from malicious content

    2. IntegrationTest.php comprehensive tests:
       - testFullSearchFlow: search from toolbar → dropdown → click result → opens message
       - testSearchWithLabelFilter: filter by label in search page, verify results scoped
       - testThreadViewWithLabels: thread row shows label chips, click chip filters to label
       - testContactAutocompleteInComposer: compose → To field → unified autocomplete works
       - testArchiveFromThreadView: expand thread, select message, archive, undo works
       - testMobileResponsive: resize to <768px, verify bottom nav, drawer panels
       - testAllFiltersCombined: search + folder + date + attachment + read/unread + label filters
       - testLabelChipsInThreadRow: thread row shows aggregated label chips from all messages
       - testContactGroupsInSidebar: contact groups display as chips in contact row

    3. UI polish per UI-SPEC:
       - Verify all empty states show correct copy (UI-SPEC Copywriting Contract)
       - Verify loading states: search dropdown skeleton (3 rows), thread expansion spinner, contact/label list skeletons (5 rows), search results page full skeleton
       - Verify error states: inline error banners with retry buttons
       - Verify label color contrast: all 10 palette colors pass WCAG 4.5:1 on white (manual check noted in test)
       - Verify thread row height 56px (py-3), flat row 64px
       - Verify search dropdown max 8 results, "View all results" link works
       - Verify mobile bottom nav safe-area-inset-bottom on notched devices

    4. Run full test suite to ensure no regressions from Phase 1-3.

    5. Build assets: npm run build (verify no TypeScript/JS errors)
  </action>
  <verify>
    <automated>cd /home/snigdha/Desktop/OpenMail && ./vendor/bin/pest tests/Feature/IntegrationTest.php tests/Feature/SearchResultsPageTest.php 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or "0 passed" in summary, or any test shows "FAIL"</fails_when>
  </verify>
  <done>
    All integration tests pass. Search highlighting works after DOMPurify. Empty/loading/error states match UI-SPEC. Mobile responsive works with bottom navigation. Full test suite (Phases 1-4) passes. npm run build succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → Thread Mode | User-controlled persistence; no security impact (UI preference only) |
| Keyboard Events → Actions | Client-side shortcuts dispatch to Livewire; server validates all actions |
| Mobile Drawer State → UI | Alpine.js store; no server trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-21 | Tampering | localStorage thread mode manipulation | low | accept | Only affects UI rendering; no data integrity impact |
| T-04-22 | DoS | Keyboard shortcut spam | low | mitigate | Debounce shortcut handlers (100ms); Livewire request deduplication |
| T-04-23 | XSS | Search highlight injection | high | mitigate | DOMPurify BEFORE mark wrapping (verified in Task 3); CSP headers in Phase 5 |
| T-04-24 | Info Disclosure | Mobile drawer state leakage | low | accept | No sensitive data in drawer state |
| T-04-25 | Tampering | Tab state manipulation | low | accept | Only affects UI panel visibility; no data impact |
</threat_model>

<verification>
Run `./vendor/bin/pest` (full suite) — all tests must pass.
Run `npm run build` — must succeed with no errors.
Manual: Resize browser to <768px, verify bottom nav + drawer panels.
Manual: Verify label color contrast with WebAIM checker (all 10 colors).
</verification>

<success_criteria>
- Tabbed sidebar (Folders/Contacts/Labels) works on desktop and mobile (bottom navigation for <768px)
- Thread toggle persists per folder in localStorage (threaded for Inbox, flat for others)
- Keyboard shortcuts: / (search), t (thread toggle), e (archive) all work
- Search highlighting applied AFTER DOMPurify sanitization
- All empty/loading/error states match UI-SPEC copy
- Mobile: bottom navigation fixed, safe-area-inset, drawer panels slide up
- Full test suite (Phases 1-4) passes
- npm run build succeeds
- Phase 4 complete — ready for Phase 5
</success_criteria>

<output>
Create .planning/phases/04-organization-intelligence/04-09-SUMMARY.md when done
</output>