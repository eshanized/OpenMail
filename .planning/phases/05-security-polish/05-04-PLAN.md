---
phase: 05-security-polish
plan: 04
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - database/migrations/2026_09_08_000003_create_signatures_table.php
  - app/Models/Signature.php
  - app/Services/SignatureService.php
  - app/Http/Requests/SignatureRequest.php
  - app/Livewire/Settings/SignaturesTab.php
  - app/Livewire/Settings/SignatureModal.php
  - resources/views/livewire/settings/signatures-tab.blade.php
  - resources/views/livewire/settings/signature-modal.blade.php
  - resources/views/components/tiptap-editor.blade.php
  - resources/js/components/TiptapEditor.js
  - app/Models/User.php
autonomous: true
requirements:
  - SET-02
estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - User can create, edit, delete signatures with rich text (Tiptap) in Settings > Signatures
    - One signature marked as default; default promotion on delete works
    - Signatures are sanitized via MessageSanitizer before storage
  artifacts:
    - signatures table with user_id, name, content_json, content_html, is_default, timestamps
    - Signature model with sanitization hook and default enforcement
    - SignatureService with CRUD, default logic, default promotion on delete
    - SignaturesTab with list/modal UI
    - SignatureModal with Tiptap editor
    - Tiptap editor configuration shared between composer and signatures
  key_links:
    - SignatureService → signatures table → SignatureModal → Tiptap editor
    - Signature model saving hook → MessageSanitizer → sanitized HTML storage
    - Default signature logic → ComposerService (Plan 05-05) → auto-insert on compose

---

<objective>
Build complete signature management system: database schema, model with sanitization, service with CRUD and default logic, Settings tab with list view, and modal with Tiptap rich text editor. This plan delivers SET-02 and provides the foundation for composer integration in Plan 05-05.
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-security-polish/05-CONTEXT.md
@.planning/phases/05-security-polish/05-RESEARCH.md
@.planning/phases/03-compose-send/03-CONTEXT.md
@.planning/phases/05-security-polish/05-02-PLAN.md
@app/Services/MessageSanitizer.php
@app/Services/ComposerService.php
@resources/views/components/tiptap-editor.blade.php
@resources/js/components/TiptapEditor.js
@app/Models/User.php
</context>

<tasks>

<task type="tracer">
  <name>End-to-End Signature CRUD with Tiptap Editor</name>
  <files>
    database/migrations/2026_09_08_000003_create_signatures_table.php
    app/Models/Signature.php
    app/Services/SignatureService.php
    app/Http/Requests/SignatureRequest.php
    app/Livewire/Settings/SignaturesTab.php
    app/Livewire/Settings/SignatureModal.php
    resources/views/livewire/settings/signatures-tab.blade.php
    resources/views/livewire/settings/signature-modal.blade.php
    resources/views/components/tiptap-editor.blade.php
    resources/js/components/TiptapEditor.js
    app/Models/User.php
  </files>
  <action>
    Build complete signature management system (per D-13, D-14, D-16):

    1. **Signatures Table Migration** (per RESEARCH.md Pattern 4):
       - Create migration: id, user_id (FK cascade), name (string), content_json (JSON), content_html (text, nullable), is_default (boolean, default false), timestamps
       - Index on [user_id, is_default] for default lookup
       - No pivot needed — signatures belong to one user

    2. **Signature Model** (app/Models/Signature.php):
       - $fillable: name, content_json, content_html, is_default
       - $casts: content_json => 'array', is_default => 'boolean'
       - user() belongsTo User
       - Booted: saving event — sanitize HTML via MessageSanitizer (reuse existing); enforce single default per user (if is_default, unset others)

    3. **SignatureService** (app/Services/SignatureService.php, per RESEARCH.md Pattern 4):
       - Constructor injection: protected MessageSanitizer $sanitizer
       - getDefault(User): returns default signature or null
       - create(array $data, User): creates signature
       - update(Signature, array $data): updates, returns fresh
       - delete(Signature): deletes; if was default, promote newest remaining to default
       - setDefault(Signature): unsets all user's defaults, sets this one

    4. **Validation Request** (app/Http/Requests/SignatureRequest.php):
       - name: required, string, max:100
       - content_json: required, array (Tiptap JSON structure)
       - is_default: sometimes, boolean

    5. **SignaturesTab Livewire Component** (app/Livewire/Settings/SignaturesTab.php):
       - Properties: signatures (collection), showModal (bool), editingSignature (Signature|null)
       - mount(): load user signatures via SignatureService
       - create(): showModal=true, editingSignature=null
       - edit(Signature): showModal=true, editingSignature=signature
       - delete(Signature): confirm → SignatureService::delete()
       - setDefault(Signature): SignatureService::setDefault()
       - render(): signatures-tab.blade.php

    6. **SignatureModal Livewire Component** (app/Livewire/Settings/SignatureModal.php):
       - Properties: name, content_json, is_default
       - mount(Signature|null): if editing, populate from signature
       - save(): validate via SignatureRequest → create or update via SignatureService → close modal
       - Uses Tiptap editor component for content_json editing

    7. **Views**:
       - signatures-tab.blade.php: list signatures with name, default badge, edit/delete buttons; "Create Signature" button; @livewire('settings.signature-modal') for modal
       - signature-modal.blade.php: modal with Tiptap editor (reuse tiptap-editor.blade.php), name input, default checkbox, save/cancel buttons
       - tiptap-editor.blade.php: shared component for composer + signatures (verify compatibility with Phase 3)
       - TiptapEditor.js: ensure it works in modal context (mount/unmount cleanup)

    8. **Tiptap Configuration Reuse** (per D-13):
       - Verify resources/js/components/TiptapEditor.js exports config usable by both composer and signatures
       - Extensions: StarterKit, Placeholder, Link, Image (disabled for signatures), TextAlign, TextStyle, Color, Highlight, Underline, History, CharacterCount
       - Sanitization: transformPastedHTML to strip unsafe tags; output JSON only

    9. **User Model Relation**:
       - Add signatures() hasMany relation to User model
  </action>
  <verify>
    <automated>php artisan migrate --force && php artisan test --filter=SignatureTest</automated>
    <fails_when>non-zero exit, or signature CRUD fails, or default logic broken</fails_when>
    <automated>php artisan test --filter=SignatureTest::test_sanitization</automated>
    <fails_when>non-zero exit, or XSS payload in signature content not sanitized</fails_when>
    <automated>php artisan test --filter=SignatureTest::test_default_promotion</automated>
    <fails_when>non-zero exit, or deleting default doesn't promote next signature</fails_when>
    <automated>php artisan test --filter=SettingsTest::test_signatures_tab</automated>
    <fails_when>non-zero exit, or Signatures tab not accessible, or modal not working</fails_when>
  </verify>
  <done>
    Signatures table migrated; Signature model with sanitization hook; SignatureService with full CRUD + default logic; SignaturesTab with list/modal; SignatureModal with Tiptap editor; signatures sanitized via MessageSanitizer; default promotion on delete works.
  </done>
  <reversibility>
    rating: costly
    rationale: Schema change (signatures table); removing requires data migration; but feature is self-contained and additive
  </reversibility>
</task>

<task type="auto">
  <name>Signature Sanitization Hardening & Tiptap Modal Polish</name>
  <files>
    app/Models/Signature.php
    app/Services/SignatureService.php
    resources/views/components/tiptap-editor.blade.php
    resources/js/components/TiptapEditor.js
    resources/views/livewire/settings/signature-modal.blade.php
  </files>
  <action>
    Harden signature sanitization pipeline and polish the Tiptap modal experience:

    1. **Signature Sanitization Pipeline** (SEC-01, SEC-02, D-13):
       - In Signature::booted() saving event: render Tiptap JSON to HTML via tiptap-php Editor → getHTML(), then pass through MessageSanitizer::sanitizeHtml()
       - Verify MessageSanitizer blocks scripts, event handlers, javascript: URLs, iframes, forms, CSS injection
       - Store sanitized HTML in content_html for fast rendering; keep content_json as source of truth

    2. **Tiptap Modal Polish**:
       - Ensure modal focus trapping (Alpine.js focus management)
       - Add character count indicator (CharacterCount extension)
       - Add placeholder text "Add your signature..."
       - Keyboard shortcut: Cmd/Ctrl+Enter to save, Escape to cancel
       - Loading state on save button during SignatureService call

    3. **Tiptap Editor Shared Configuration**:
       - Verify TiptapEditor.js exports getExtensions() function returning configured extensions
       - Disable Image extension for signatures (no image uploads in signatures per D-13)
       - Link extension: allow only http/https/mailto schemes, add rel="noopener noreferrer"
       - TextStyle/Color/Highlight: ensure output sanitized by MessageSanitizer

    4. **Signature List UX**:
       - Default badge styling (green pill)
       - Hover actions: edit/delete buttons appear on row hover
       - Empty state: "No signatures yet. Create your first signature." with CTA button
       - Confirmation modal for delete (Alpine.js x-data with confirm())
  </action>
  <verify>
    <automated>php artisan test --filter=SignatureTest::test_sanitization_pipeline</automated>
    <fails_when>non-zero exit, or sanitized HTML still contains blocked elements</fails_when>
    <automated>php artisan test --filter=SettingsTest::test_signature_modal</automated>
    <fails_when>non-zero exit, or modal focus trapping broken, or save/cancel not working</fails_when>
    <automated>php artisan test --filter=SignatureTest::test_image_disabled</automated>
    <fails_when>non-zero exit, or Image extension enabled in signature Tiptap config</fails_when>
  </verify>
  <done>
    Signature sanitization pipeline hardened (Tiptap JSON → HTML → MessageSanitizer); modal polished with focus trapping, keyboard shortcuts, loading states; Tiptap config shared and signature-appropriate (no images); list UX complete with empty state and confirmations.
  </done>
</task>

<task type="auto">
  <name>Signatures Tab Integration & Settings Page Completion</name>
  <files>
    app/Livewire/Settings/SettingsPage.php
    app/Livewire/Settings/SignaturesTab.php
    resources/views/livewire/settings/settings-page.blade.php
    resources/views/livewire/settings/signatures-tab.blade.php
    routes/web.php
  </files>
  <action>
    Integrate Signatures tab into Settings page and verify end-to-end flow:

    1. **Settings Page Tab Activation**:
       - Update SettingsPage.php: ensure 'signatures' tab is active and functional (remove placeholder)
       - Update settings-page.blade.php: signatures tab panel renders @livewire('settings.signatures-tab')
       - Verify tab navigation works: profile → mail → appearance → security → signatures

    2. **Signatures Tab Deep Linking**:
       - Support ?tab=signatures URL parameter
       - On SettingsPage mount: if tab parameter present and valid, set activeTab
       - On tab switch: update URL with wire:navigate

    3. **Settings Page Consistency**:
       - Ensure all tabs have consistent header styling (title, description)
       - Add "Signatures" tab to tab navigation with proper ARIA attributes
       - Verify mobile responsiveness: signatures tab scrollable, modal full-screen on mobile

    4. **Integration Test**:
       - Full flow: Settings → Signatures tab → Create signature → Set as default → Edit signature → Delete signature → Verify default promotion
       - Test multiple signatures: create 3, set middle as default, delete default → verify promotion to newest
  </action>
  <verify>
    <automated>php artisan test --filter=SettingsTest::test_signatures_integration</automated>
    <fails_when>non-zero exit, or Signatures tab not integrated, or deep linking broken</fails_when>
    <automated>php artisan test --filter=SignatureTest::test_multi_signature_flow</automated>
    <fails_when>non-zero exit, or create/edit/delete/default flow broken</fails_when>
    <automated>php artisan test --filter=SettingsTest::test_all_tabs_accessible</automated>
    <fails_when>non-zero exit, or any tab navigation broken</fails_when>
  </verify>
  <done>
    Signatures tab fully integrated into Settings page; tab navigation consistent across all 5 tabs; deep linking works; end-to-end signature management flow verified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Settings UI | Authenticated user modifies own signatures |
| Signature Content → Renderer | User-created signature HTML rendered in composer/emails |
| Tiptap Editor → Server | User input (Tiptap JSON) sent to server for storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-18 | Tampering | Signature XSS | high | mitigate | Tiptap JSON stored; rendered via tiptap-php → HTMLPurifier → DOMPurify pipeline (reuse MessageSanitizer) |
| T-05-19 | Elevation of Privilege | Signature access control | low | mitigate | Signatures scoped to user_id; SignatureService checks ownership; Livewire components auth-gated |
| T-05-20 | Tampering | Default signature logic bypass | low | mitigate | Signature::booted() enforces single default at model level; SignatureService::setDefault() atomic |
| T-05-21 | Information Disclosure | Signature content in logs | low | accept | Signature content not logged; only metadata (name, is_default) in audit if needed |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Package legitimacy audit completed; all packages [OK] |

</threat_model>

<verification>
Overall phase verification for Plan 4:
- Signatures table created with proper indexes and foreign keys
- Signature model: sanitization hook runs on save; single default enforced
- SignatureService: CRUD operations work; default promotion on delete verified
- SignaturesTab: list displays signatures with default badge; create/edit/delete modal works
- SignatureModal: Tiptap editor loads; content saved as JSON; sanitized HTML stored
- Tiptap config: shared with composer; Image extension disabled for signatures
- All signatures sanitized via MessageSanitizer pipeline
</verification>

<success_criteria>
- All SignatureTest, SettingsTest pass
- Signatures tab accessible and functional in Settings page
- Signature CRUD with rich text editing works end-to-end
- Default signature logic correct (create, set, delete with promotion)
- Sanitization prevents XSS in signature content
</success_criteria>

<output>
Create .planning/phases/05-security-polish/05-04-SUMMARY.md when done
</output>