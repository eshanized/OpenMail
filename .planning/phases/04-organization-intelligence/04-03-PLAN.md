---
phase: 04-organization-intelligence
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/Models/Contact.php
  - app/Models/ContactGroup.php
  - app/Services/ContactService.php
  - app/Services/ContactAutocompleteService.php
  - app/Services/VCardService.php
  - tests/Feature/ContactTest.php
  - tests/Feature/ContactAutocompleteTest.php
  - tests/Feature/VCardTest.php
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05, CONT-06]
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 17500
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Local contacts stored with name, email, phone, notes, groups (D-10)"
    - "Unified autocomplete merges local contacts + IMAP recipient cache (D-09)"
    - "Local contacts rank higher than IMAP cache; deduplication by email (case-insensitive)"
    - "vCard 3.0 import/export with conflict resolution (skip/update/duplicate) (D-11)"
    - "Contact avatar color deterministic from email hash (CRC32 modulo 10) per Open Question 3"
  artifacts:
    - "app/Models/Contact.php"
    - "app/Models/ContactGroup.php"
    - "app/Services/ContactService.php"
    - "app/Services/ContactAutocompleteService.php (extended)"
    - "app/Services/VCardService.php"
  key_links:
    - "Contact model → ContactService (CRUD) → ContactSidebar (Plan 7)"
    - "ContactAutocompleteService.searchUnified() → Composer autocomplete dropdown (Plan 7 Task 3)"
    - "VCardService.import()/export() → ContactImportModal → Contact model"
    - "Contact avatar: deterministic color from email hash (UI-SPEC, Open Question 3 resolved)"

---

<objective>
Implement the complete contact management backend: Contact/ContactGroup models, ContactService CRUD, unified autocomplete merging local contacts with IMAP cache, and vCard 3.0 import/export using sabre/vobject. Resolves contact avatar generation (Open Question 3).
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-organization-intelligence/04-CONTEXT.md
@.planning/phases/04-organization-intelligence/04-RESEARCH.md
@.planning/phases/04-organization-intelligence/04-UI-SPEC.md
@.planning/phases/03-compose-send/03-CONTEXT.md
@app/Services/ContactAutocompleteService.php
@app/Models/ContactAutocompleteCache.php
@app/Livewire/Composer.php
@config/openmail.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contact/ContactGroup models + ContactService + VCardService</name>
  <files>
    app/Models/Contact.php
    app/Models/ContactGroup.php
    app/Services/ContactService.php
    app/Services/VCardService.php
    tests/Feature/ContactTest.php
    tests/Feature/VCardTest.php
  </files>
  <behavior>
    - Test 1: Contact::create() validates email format, unique per user, sets avatar_color from deterministic hash (CRC32 modulo 10 palette)
    - Test 2: Contact::update() validates email unique (excluding self), updates usage_count on autocomplete selection
    - Test 3: Contact::delete() removes contact, syncs groups
    - Test 4: ContactGroup CRUD with user scoping
    - Test 5: ContactService.getGroups() returns user's groups with contact counts
    - Test 6: VCardService.import() parses vCard 3.0 with sabre/vobject, handles FN/EMAIL/TEL/NOTE/CATEGORIES, conflict strategies (skip/update/duplicate)
    - Test 7: VCardService.export() generates valid vCard 3.0 with all contact fields + groups as CATEGORIES
    - Test 8: Contact deduplication: exact case-insensitive email match (Pitfall 4)
    - Test 9: avatar_color deterministic: same email always produces same color from 10-color palette (Open Question 3)
  </behavior>
  <action>
    1. Contact model: fillable [name, email, phone, notes, avatar_color, usage_count]. Relationships: groups() belongsToMany ContactGroup, user() belongsTo. Boot: on create, set avatar_color = deterministic hash from email using static `colorFromEmail(string $email): string`. Algorithm: `$palette = ['bg-blue-500','bg-green-600','bg-red-600','bg-yellow-600','bg-purple-600','bg-pink-600','bg-orange-600','bg-teal-600','bg-indigo-600','bg-gray-500']; return $palette[crc32(strtolower($email)) % 10];` Resolves RESEARCH.md Open Question 3.

    2. ContactGroup model: fillable [name, color]. Relationships: contacts() belongsToMany Contact, user() belongsTo. Scope for user.

    3. ContactService: create(), update(), delete(), getForUser(), search(), incrementUsage(). All scoped to authenticated user.

    4. VCardService per RESEARCH.md Pattern 3 using sabre/vobject:
       - import(content, userId, conflictStrategy): Reader::read(), iterate VCards, extract FN, EMAIL (first), TEL, NOTE, CATEGORIES. Conflict: skip (default), update, duplicate. Sync groups from CATEGORIES.
       - export(userId): Writer::write() all user contacts with groups as CATEGORIES.

    5. Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd /home/snigdha/Desktop/OpenMail && ./vendor/bin/pest tests/Feature/ContactTest.php::testCrudAndAvatarColor tests/Feature/VCardTest.php 2>&1 | tail -25</automated>
    <fails_when>non-zero exit, or "0 passed" in summary, or any test shows "FAIL"</fails_when>
  </verify>
  <done>
    Contact/ContactGroup models work. ContactService CRUD works with avatar_color deterministic hash. VCardService imports/exports vCard 3.0 with groups. All unit/feature tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ContactAutocompleteService unified search merge</name>
  <files>
    app/Services/ContactAutocompleteService.php
    tests/Feature/ContactAutocompleteTest.php
  </files>
  <behavior>
    - Test 1: searchUnified() merges local contacts + IMAP cache, local wins on exact email, ranked by usage_count/frequency (D-09)
    - Test 2: Local contacts: exact match > prefix match on email/name, order by usage_count desc
    - Test 3: IMAP cache: frequency desc
    - Test 4: Merge: concat, unique by email (strict, local wins), sort by frequency desc, take limit
    - Test 5: IMAP cache auto-expires (existing TTL logic in ContactAutocompleteCache)
    - Test 6: Returns format compatible with Composer dropdown: [name, email, avatar, frequency, source]
  </behavior>
  <action>
    Extend ContactAutocompleteService (existing from Phase 3): add searchUnified(userId, query, limit) per RESEARCH.md Pattern 3. Merges Contact::search() + ContactAutocompleteCache::search().

    Local contacts query: where email LIKE query% OR name LIKE query%, order by usage_count desc.
    IMAP cache query: existing search() method, frequency desc.

    Merge logic: $local->concat($imap)->unique('email', true)->sortByDesc('frequency')->take($limit)->values().

    Per D-09: Local contacts rank higher (exact match > prefix). IMAP cache auto-expires (existing TTL logic).

    Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd /home/snigdha/Desktop/OpenMail && ./vendor/bin/pest tests/Feature/ContactAutocompleteTest.php::testUnifiedAutocomplete 2>&1 | tail -15</automated>
    <fails_when>non-zero exit, or "0 passed" in summary, or test shows "FAIL"</fails_when>
  </verify>
  <done>
    ContactAutocompleteService.searchUnified() returns merged deduped results (local first, ranked by usage). All tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Input → Contact CRUD | Name, email, phone, notes from user; validated server-side |
| vCard File → Import Parser | Uploaded .vcf files parsed by sabre/vobject; malformed files handled gracefully |
| Composer Autocomplete → Backend | Search queries from composer; sanitized before query |
| IMAP Recipient Cache → Autocomplete | Cached recipients from sent mail; read-only merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Injection | vCard import parsing | medium | mitigate | sabre/vobject handles parsing; wrap in try/catch; validate parsed email format before DB insert |
| T-04-12 | Info Disclosure | Contact autocomplete cross-user | low | mitigate | searchUnified() scopes to authenticated user_id; IMAP cache also user-scoped |
| T-04-13 | Tampering | Contact email uniqueness bypass | medium | mitigate | Unique index on (user_id, email) at DB level; server-side validation before insert/update |
| T-04-14 | DoS | Large vCard import | low | mitigate | Limit import to 1000 contacts per request; chunk processing; file size validation (max 2MB) |
| T-04-15 | Spoofing | IMAP recipient cache pollution | low | accept | Cache populated only from sent mail (user's own actions); not from received mail |
</threat_model>

<verification>
Run `./vendor/bin/pest tests/Feature/ContactTest.php tests/Feature/ContactAutocompleteTest.php tests/Feature/VCardTest.php` — all tests must pass.
Verify vCard export: `php artisan tinker --execute="echo strlen(app(App\Services\VCardService::class)->export(1));"` returns >0.
</verification>

<success_criteria>
- Contact/ContactGroup models with deterministic avatar_color from email hash (CRC32 modulo 10)
- ContactService CRUD works (create, read, update, delete)
- Contact groups supported (many-to-many)
- vCard 3.0 import parses FN/EMAIL/TEL/NOTE/CATEGORIES with 3 conflict strategies
- vCard 3.0 export generates valid .vcf with all fields
- Unified autocomplete merges local contacts + IMAP cache (local wins, ranked by usage)
- All tests pass
</success_criteria>

<output>
Create .planning/phases/04-organization-intelligence/04-03-SUMMARY.md when done
</output>