---
phase: 06-6-ui-ux-polish
plan: 04
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified:
  - resources/views/livewire/mailbox/message-list.blade.php
  - resources/views/livewire/mailbox/folder-sidebar.blade.php
  - resources/views/livewire/mailbox/search-results-dropdown.blade.php
  - resources/views/livewire/mailbox/composer.blade.php
  - resources/views/livewire/mailbox/message-toolbar.blade.php
autonomous: true
requirements: [D-11]
user_setup: []

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - Folder switches, sort, and thread-mode toggles in the message list show shimmer skeleton rows instead of the full-viewport white splash, with no layout shift — the loading region targets the three list actions and the old splash class string no longer appears in the view (D-11, red test flipped green)
    - Folders-panel refresh and the search dropdown's in-flight state use the same shimmer treatment as the list, sharing the animate-shimmer token from 06-02 with zero new CSS (D-11)
    - The Send button keeps its gradient fill from 06-03 while its loading spans, the toolbar Archive/Delete/Spam disable states, and the sidebar Refresh swap all run on the Livewire 4 native data-loading attribute system (research: Livewire is 4.4.x — wire:loading is the deprecated alias and new code uses data-loading; existing compiled markup keeps working)
    - All skeletons and shimmer blocks are static markup — zero user input, zero inline styles, motion-guarded by the 06-02 prefers-reduced-motion rule
  artifacts:
    - message-list.blade.php — skeleton loading region (shimmer rows) replacing the splash block; wire:target list on the region
    - folder-sidebar.blade.php — refresh-busy skeleton rows in the folders panel; data-loading conversion of the Refresh button state swap
    - search-results-dropdown.blade.php — loading + empty panels on the glass treatment with shimmer rows (replacing animate-pulse gray blocks)
    - composer.blade.php, message-toolbar.blade.php — data-loading attribute conversions on loading-state switches
  key_links:
    - The MessageListLoadingTest contains-needle `wire:target="onFolderChanged` is satisfied by the comma-separated wire:target on the new skeleton region, and its does-not-contain needle (old splash class string) by the removal — the RED test from 06-01 flips GREEN here and nothing else may satisfy it
    - Shimmer relies on the --animate-shimmer token + keyframes that 06-02 committed — 06-04 adds NO css changes anywhere (any need for a new CSS rule is a scope leak to stop and report)
    - The data-loading conversion must preserve each attribute's pairing — the disabled-state attribute pairs with the same button's action, the remove/visible pair stays on the same target, and the inline display:none initial-hide remains on every converted region
---

<objective>
D-11 loading skeletons arrive as polished, shimmer-driven replacement of the raw splash/spinner states, and every loading-switch in the touched views migrates to the Livewire 4 native data-loading attribute system — keeping the gradient/glass chrome from plans 06-02/06-03 intact.

Purpose: D-11's skeleton loading is the last surface-level visual decision. The splash block is the worst remaining UX artifact (full-viewport white cover) and the search/folder refresh states still use raw gray pulse blocks that clash with the new surface language. The data-loading migration keeps the loading UX correct on Livewire 4.4.x (deprecated alias → native), scoped strictly to loading-state attributes so no wire:click/wire:model behavior changes.
Output: Skeleton loading in three surfaces (list, sidebar refresh, search dropdown), loading-state attributes migrated in five views, all wave-0/1/2 tests still green, and zero CSS or JS changes (token + utility reuse only).
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-6-ui-ux-polish/06-CONTEXT.md
@.planning/phases/06-6-ui-ux-polish/06-RESEARCH.md
@.planning/phases/06-6-ui-ux-polish/06-UI-SPEC.md
@.planning/phases/06-6-ui-ux-polish/06-VALIDATION.md
@resources/views/livewire/mailbox/message-list.blade.php
@resources/views/livewire/mailbox/folder-sidebar.blade.php
@resources/views/livewire/mailbox/search-results-dropdown.blade.php
@resources/views/livewire/mailbox/composer.blade.php
@resources/views/livewire/mailbox/message-toolbar.blade.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Message list skeleton loading — replace the full-viewport white splash with shimmer rows (D-11)</name>
  <files>resources/views/livewire/mailbox/message-list.blade.php</files>
  <read_first>
    The loading block sits at the end of message-list.blade.php — the wire:loading div at line 203 with the fixed full-viewport translucent white cover, inline display:none initial-hide, and the spinning refresh icon (lines 202-207). It is UNTARGETED: it shows on any MessageList request. The row markup to mimic lives in message-row.blade.php (avatar circle, sender/subject/time lines, density-driven message-row custom properties). The animate-shimmer utility and keyframes already exist from 06-02's tokens — no CSS changes are permitted in this plan (that token is the single source for shimmer; any need for a new rule is a scope leak — stop and report).
  </read_first>
  <action>
    Replace the splash loading block (the wire:loading div at lines 202-207, identified by its fixed full-viewport translucent white cover and inline display:none) with a skeleton region that keeps the same wire:loading + inline style="display: none;" initial-hide onset but changes everything visible (D-11):
    (1) Add a comma-separated target list that pins the region to the three full-list re-render actions — folder switches, sort changes, and thread-mode toggles so the request list reads exactly: wire:target="onFolderChanged,setSort,toggleThreadMode". (This satisfies the 06-01 test contains-needle verbatim — keep the leading order and comma spacing as written so the literal substring matches.) Do not alter these action names — they are the real public methods on App\Livewire\Mailbox\MessageList.
    (2) Render five skeleton rows that mirror the real row geometry: per row a leading avatar block (rounded-full w-9 h-9), a subject block (h-3.5 rounded w-3/4) and an underlined sender strip (h-3 rounded w-32) on one line, and a time block (w-12 h-3 rounded) aligned right. Decorate every block with the animate-shimmer utility plus the static shimmer fill classes — a light-theme linear wash built from near-invisible black stops and mirror dark-theme white stops (use the Tailwind v4 native linear-gradient utility with black 4%/8%/4% stops in light and white 6%/12%/6% in dark, resolved through the custom dark variant). Static class strings only, no inline styles beyond the initial-hide pattern, no user-controlled values (V5/T-06-02).
    (3) Delete the old spinning icon markup entirely — the skeleton region replaces the whole block; the old splash class string must no longer appear anywhere in the view (the 06-01 does-not-contain needle).
    (4) Leave the visible message list, pagination, empty state, and all wire:click handlers untouched — including the x-cloak regions and the density-driven row custom properties.
  </action>
  <verify>
    <automated>php artisan test --filter=MessageListLoadingTest && php artisan test --filter=UiPolishTest::test_compose_button_has_gradient_classes && npm run build</automated>
    <fails_when>MessageListLoadingTest fails (the targeted skeleton region is missing the exact target string or the old splash class string still appears); the compose-gradient method regresses (row/button markup damaged); npm run build exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    MessageListLoadingTest green (RED→GREEN from wave 0); the loading region targets onFolderChanged plus the sort and thread toggles; five shimmer rows render in place of the splash; zero CSS and zero JS files changed in this task.
  </acceptance_criteria>
  <done>
    A folder switch, sort change, or thread toggle shows shimmer skeleton rows in the list area (no full-viewport cover), verified by the flipped-green test and a build-successful check — committed with no CSS/JS diff.
  </done>
  <reversibility rating="reversible">Single-view markup swap; the old block is recoverable from git history.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Sidebar refresh skeleton + search dropdown shimmer/glass panels (D-11)</name>
  <files>resources/views/livewire/mailbox/folder-sidebar.blade.php, resources/views/livewire/mailbox/search-results-dropdown.blade.php</files>
  <read_first>
    folder-sidebar refresh controls (lines 170-177): the Refresh button swaps its icon and label via wire:loading remove spans and disables itself, all targeted on the refreshFolders action — the folders list itself has no busy treatment today. The folders panel (x-show="activeTab === 'folders'" x-cloak, around lines 52-90) lists folder rows with icon, name, and count. search-results-dropdown already has a "Loading skeleton" block (lines 55-75) of animate-pulse bg-gray-200 gray blocks inside a white panel, plus the empty-state panel (lines 48-53) on the same white panel classes — 06-03 restyled the results panel to glass-card and this task brings the loading and empty panels to the same glass surface. The animate-shimmer token + glass-card utility both already exist — zero new CSS for this task.
  </read_first>
  <action>
    Apply skeleton + consistent surface treatment (D-11, reusing 06-02/06-03 work):
    (1) folder-sidebar.blade.php — while the refreshFolders action is in flight, overlay the folders panel itself with three shimmer skeleton rows that mimic folder-row geometry (leading folder icon block w-4 h-4 rounded, a name block h-3 rounded flex-1 max-w-40, a count block w-8 rounded) using the same animate-shimmer + linear gradient wash classes as task 1 (light black 4/8/4%, dark white 6/12/6%). Scope it with the target attribute on the refresh action, keep the inline display:none initial-hide, add pointer-events-none so the skeleton never intercepts clicks. Keep the existing Refresh-button label swap exactly as it is (disabled + "Refreshing..." retained — the button state and the panel skeleton are complementary, not competing).
    (2) search-results-dropdown.blade.php — give the loading panel and the empty-state panel the same glass-card surface as the 06-03 results panel (swap the plain white classes for the glass utility + the border/shadow stack the results panel now uses). Restyle the three loading rows from the old gray pulse classes to the animate-shimmer wash (same class vocabulary as the message-list skeleton — avatar circle, then two lines of shimmer text blocks).
    (3) No new CSS, no new JS, no new inline scripts; static classes only (V5).
  </action>
  <verify>
    <automated>php artisan test --filter='UiPolishTest::(test_glass_surface_classes_present|test_status_badges_use_soft_tag_style)' && php artisan test --filter=MessageListLoadingTest && npm run build</automated>
    <fails_when>the glass or soft-tag UiPolishTest methods fail (panel/row markup drifted from the 06-03 contract); MessageListLoadingTest regresses (folder-sidebar/dropdown edits must not touch message-list); build non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    Folder refresh shows shimmer rows over the folders panel; search loading + empty panels are glass; shimmer blocks use the token-driven classes; the 06-03 test contracts still green.
  </acceptance_criteria>
  <done>
    A user clicking Refresh sees skeleton folder rows while folders reload, and search typing shows a glass shimmer dropdown instead of gray pulse blocks — both verified by the pinned tests and a green build.
  </done>
  <reversibility rating="reversible">Two-view class/template swaps; revert is local.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Livewire 4 native data-loading migration + regression sweep (research: Livewire 4.4.x)</name>
  <files>resources/views/livewire/mailbox/composer.blade.php, resources/views/livewire/mailbox/message-toolbar.blade.php, resources/views/livewire/mailbox/folder-sidebar.blade.php</files>
  <read_first>
    Composer Send button (composer.blade.php lines 208-221): the gradient-primary button from 06-03 keeps wire:click="send", disables via wire:loading.attr="disabled", and swaps its label through the wire:loading.remove / wire:loading span pair with the spinner. message-toolbar Archive (line 38-48), Delete (51-61), Spam (64-73) and the secondary actions at lines 103/135/148/160/172 each carry wire:loading.attr="disabled". folder-sidebar Refresh (170-177) uses the wire:loading remove spans + wire:target="refreshFolders" + wire:loading.attr="disabled". Livewire 4.4.x treats wire:loading as the deprecated alias — the native system is the data-loading attribute family (data-loading, data-loading.remove, data-loading.attr, data-loading.target). The magnetic toolbar undo toast at line 203 and every wire:click/wire:model stay untouched; message-row's single wire:loading.attr="disabled" (line 20) and label-modal's instances are out of scope — accepted deprecated usage, note it in the SUMMARY.
  </read_first>
  <action>
    Migrate ONLY the loading-state attribute system in the three views, per the native data-loading attribute family:
    (1) composer.blade.php — the Send button's three loading hooks convert to the native forms: data-loading.attr="disabled" replaces wire:loading.attr="disabled", the label span data-loading.remove replaces wire:loading.remove, and the spinner span data-loading replaces wire:loading — keeping the static "Send Message"/"Sending..." spans, spinner svg, gradients, and wire:click="send" exactly as-is. Keep the inline style="display: none;" initial-hide on the data-loading region. Leave Save Draft / Discard untouched (`wire:loading.attr="disabled"` on Save Draft and the confirm onclick on Discard stay — the deprecated-usage acceptance note covers them).
    (2) message-toolbar.blade.php — convert every wire:loading.attr="disabled" on the action buttons (Archive, Delete, Spam, and the lines-103/135/148/160/172 secondary actions) to data-loading.attr="disabled" on the same element, paired with the same wire:click action. No class changes beyond what 06-03 already landed.
    (3) folder-sidebar.blade.php — convert the Refresh/Refreshing swap pair to data-loading.remove and data-loading with data-loading.target="refreshFolders", and data-loading.attr="disabled" on the button. The task-2 panel skeleton region stays wire:loading-based with wire:target as specified there (it is pinned by the 06-01 test needle — do not convert it).
    (4) Regression gate: full test suite + build + build gate must all stay green; the 06-03 gradient/soft-tag/no-chrome contracts are the canary for accidental markup damage. No new CSS/JS; no new inline scripts.
  </action>
  <verify>
    <automated>php artisan test && npm run build && node scripts/check-fontsource-build.js && rg -c "wire:loading" resources/views/livewire/mailbox/composer.blade.php resources/views/livewire/mailbox/message-toolbar.blade.php 2>/dev/null | grep -v ':0' | wc -l</automated>
    <fails_when>the full test suite fails (any regression from the attribute migration); build or build gate exits non-zero; or the residual wire:loading count in the two views is not zero (every instance in composer and message-toolbar must convert — the grep counts files still containing any occurrence)</fails_when>
  </verify>
  <acceptance_criteria>
    Zero wire:loading occurrences left in composer.blade.php and message-toolbar.blade.php; folder-sidebar's button swap uses data-loading with its target intact; the task-2 skeleton region still carries the pinned wire:target needled by the 06-01 test; full suite + build + gate green.
  </acceptance_criteria>
  <done>
    All loading-state switches in the three views run on the Livewire 4 native attribute system while the gradient/glass chrome and the pinned skeleton target stay intact — verified by the full suite, the residual-count grep, and the build gate.
  </done>
  <reversibility rating="reversible">Attribute-only swaps; the deprecated alias works identically, so any revert is stylistic.</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces

| Artifact | Type | Consumed By |
|----------|------|-------------|
| message-list.blade.php skeleton region (shimmer rows, wire:target on folder switch + sort + thread toggle) | view | flips 06-01 MessageListLoadingTest green; the only list-level D-11 surface |
| folder-sidebar.blade.php refresh skeleton + data-loading swap | view | sidebar busy states; consumed by 06-05's final integration sweep |
| search-results-dropdown.blade.php glass shimmer loading + glass empty states | view | search flyout consistency with 06-03 |
| composer.blade.php, message-toolbar.blade.php data-loading conversions | views | Livewire 4 native loading states; gradients from 06-03 preserved |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Livewire request → loading UI | Loading-state attributes drive visibility of skeleton/spinner regions based on network state; markup is static server-rendered |
| skeleton/shimmer classes → viewport | Decorative busy indicators must not intercept clicks or flash content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Tampering | class interpolation (V5) | high | mitigate | All skeleton/shimmer markup uses static class strings; no user-controlled values enter class lists; shimmer fills are hard-coded wash stops |
| T-06-03 | Tampering | email HTML reusing app chrome classes | medium | mitigate | message-viewer not in files_modified; UiPolishTest no-chrome guard stays in every full-suite regression run |
| T-06-04 | Info disclosure | inline script / attribute mishandling | medium | mitigate | Zero new inline scripts; data-loading conversions are attribute-level only, no new JS; CSP nonce emission re-checked in the full suite (UiPolishTest method 7) |
| T-06-05 | Tampering | motion/accessibility regressions (skeleton flicker) | low | mitigate | Shimmer rides the 06-02 prefers-reduced-motion guard (near-zero animation); skeleton regions are display:none outside busy states; pointer-events-none on the overlay rows |
</threat_model>

<verification>
- `php artisan test --filter=MessageListLoadingTest` green (wave-0 RED test flipped by task 1).
- `php artisan test` full suite green (task 3 regression gate) — includes AppearanceTest, UiPolishTest (methods 1-7), and the phase test scaffolding from 06-01.
- `rg -c "wire:loading"` on composer + message-toolbar == 0 after task 3; folder-sidebar keeps only its pinned wire:target skeleton region plus the converted button pair.
- `npm run build && node scripts/check-fontsource-build.js` green (no CSS/JS drift).
</verification>

<success_criteria>
- D-11 shipped on all three surfaces with shimmer tokens and zero new CSS/JS.
- Every loading-state switch in the five touched views runs on the native data-loading family, with accepted-deprecation notes for out-of-scope views recorded in the SUMMARY.
- The complete wave-0 through wave-3 test scaffolding is green in one sweep.
</success_criteria>

<output>
Create `.planning/phases/06-6-ui-ux-polish/06-04-SUMMARY.md` when done
</output>