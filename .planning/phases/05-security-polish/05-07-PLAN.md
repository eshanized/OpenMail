---
phase: 05-security-polish
plan: 07
type: execute
wave: 6
depends_on:
  - "06"
files_modified:
  - resources/css/app.css
  - resources/js/app.js
  - resources/views/layouts/mailbox.blade.php
  - resources/views/layouts/app.blade.php
  - app/Livewire/Mailbox/MessageList.php
  - app/Livewire/Mailbox/Sidebar.php
  - config/csp.php
  - .env.example
  - routes/web.php
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - SET-03
  - SET-06
user_setup: []
estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - Density setting applies consistently across mailbox (message list, sidebar, composer, settings)
    - Theme setting applies consistently across all pages (login, mailbox, settings, setup)
    - No layout shifts or flash when theme/density change
    - CSP violation report review completed, ready for enforcement toggle
    - All SEC-01 through SEC-10 and SET-01 through SET-06 requirements satisfied
    - Cross-browser testing verified (Chrome, Firefox, Safari)
  artifacts:
    - resources/css/app.css (refined density utilities)
    - resources/js/app.js (theme/density sync)
    - resources/views/layouts/mailbox.blade.php (density class binding)
    - resources/views/layouts/app.blade.php (theme initializer)
    - app/Livewire/Mailbox/MessageList.php (density-responsive)
    - app/Livewire/Mailbox/Sidebar.php (density-responsive)
    - config/csp.php (documented enforcement procedure)
    - .env.example (CSP env vars documented)
    - routes/web.php (final route verification)
  key_links:
    - CSS density utilities → all mailbox components via :class="densityClass"
    - Alpine.js themeInitializer → all pages via app.blade.php
    - CSP config → enforcement decision → production deployment
    - Settings preferences → mailbox/composer behavior
---

<objective>
Final cross-cutting polish: verify density/theme consistency across all mailbox views, prepare CSP for enforcement, validate all Phase 5 requirements complete.

Purpose: Ensure the security and polish features work cohesively across the entire application, with no regressions in mailbox UX, and CSP ready for production enforcement after violation review.

Output: Verified density/theme application everywhere, CSP enforcement readiness documentation, final requirement traceability confirmation.
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-security-polish/05-CONTEXT.md
@.planning/phases/05-security-polish/05-RESEARCH.md
@.planning/phases/05-security-polish/05-PATTERNS.md
@.planning/phases/05-security-polish/05-01-PLAN.md
@.planning/phases/05-security-polish/05-02-PLAN.md
@.planning/phases/05-security-polish/05-03-PLAN.md
@.planning/phases/05-security-polish/05-04-PLAN.md
@.planning/phases/05-security-polish/05-05-PLAN.md
@.planning/phases/05-security-polish/05-06-PLAN.md
</context>

<tasks>

<task id="07-tracer" type="execute">
  <name>End-to-end cross-mailbox density/theme verification and CSP enforcement preparation</name>
  <read_first>
    - resources/css/app.css
    - resources/js/app.js
    - resources/views/layouts/mailbox.blade.php
    - resources/views/layouts/app.blade.php
    - app/Livewire/Mailbox/MessageList.php
    - app/Livewire/Mailbox/Sidebar.php
    - config/csp.php
    - .env.example
    - routes/web.php
  </read_first>
  <action>
    Perform final polish vertical slice: (1) Verify density CSS vars apply to ALL mailbox components — MessageList (message-row, message-preview), Sidebar (folder items, label chips), Composer (toolbar, editor), Settings tabs; add missing @apply density-* utilities where components use fixed padding; (2) Verify theme consistency — login page, setup wizard, mailbox, settings, error pages all respect dark class and system preference; fix any hardcoded bg-gray-50 to use CSS vars or dark: variants; (3) Test density/theme changes via Settings → verify instant live preview in Appearance tab, immediate application to mailbox on save, persistence across browser sessions; (4) CSP enforcement prep: review storage/logs/security.log for violations, document any legitimate violations needing policy adjustment, update OpenMailPreset if needed, document CSP_ENABLED=true switch procedure in config/csp.php comments; (5) Verify all routes accessible: /settings (5 tabs), /csp-report, /settings/signatures/*, /settings/sessions/revoke, mailbox with all density/theme combos; (6) Cross-browser smoke test: Chrome, Firefox, Safari — verify CSP headers, theme/density, Livewire hydration, Alpine.js interactions.
  </action>
  <verify>
    <automated>php artisan test --filter=FinalPolishTest 2>/dev/null || echo "Manual: full cross-mailbox verification across theme/density combos, CSP log review, route verification"</automated>
    <fails_when>Density not applied to any mailbox component, theme flash on any page, CSP violations unreviewed, routes missing, Livewire/Alpine broken in any browser</fails_when>
  </verify>
  <acceptance_criteria>
    - MessageList, Sidebar, Composer, Settings all use density CSS vars (no fixed px/rem padding)
    - Login, setup, mailbox, settings, error pages all respect dark mode
    - Density change in Settings → instant mailbox update (message spacing, sidebar density)
    - Theme change in Settings → instant application-wide update
    - No layout shift or flash on any page load or preference change
    - CSP security.log reviewed; violations categorized (noise vs real); policy adjusted if needed
    - config/csp.php has enforcement switch documentation
    - .env.example has CSP_ENABLED, CSP_REPORT_ONLY, CSP_REPORT_URI documented
    - All Phase 5 routes functional
    - Chrome/Firefox/Safari: no CSP errors, Livewire works, Alpine works
  </acceptance_criteria>
  <reversibility rating="reversible">CSS/JS refinements; CSP enforcement toggle via env; no schema changes</reversibility>
</task>

<task id="07-requirement-trace" type="verify">
  <name>Verify all Phase 5 requirements (SEC-01 through SEC-10, SET-01 through SET-06) satisfied
</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/05-security-polish/05-CONTEXT.md
  </read_first>
  <action>
    Trace each requirement to implementation: (1) SEC-01: MessageSanitizer HTMLPurifier config blocks scripts/event handlers/dangerous URLs/frames — verify in MessageSanitizer; (2) SEC-02: HTMLPurifier CSS.AllowedProperties allowlist + DOMPurify client — verify config; (3) SEC-03: CSP headers via spatie/laravel-csp with report-only, nonce, Vite integration — verify headers; (4) SEC-04: Rate limiting on login (Phase 1) + API endpoints (Plan 04) — verify 429 responses; (5) SEC-05: Audit logging for login/send — verify audit_logs entries; (6) SEC-06: Session hardening (timeout, secure, httponly, samesite, rotation) — verify config/session.php and LoginController; (7) SEC-07: SSRF-safe URLs via SsrfProtection + MessageSanitizer.sanitizeUrls — verify blocked links; (8) SEC-08: Attachment UUID filenames + MIME validation — verify AttachmentRequest and storage; (9) SEC-09: MIME allowlist in config/openmail.php — verify File::types validation; (10) SEC-10: SecurityHeaders middleware global — verify headers on all responses; (11) SET-01: Profile tab name/email — verify ProfileTab; (12) SET-02: Signature management — verify SignaturesTab + Composer; (13) SET-03: Theme selection — verify AppearanceTab + app.blade.php initializer; (14) SET-04: Mail preferences — verify MailTab + MessageList/Composer integration; (15) SET-05: Session/logout controls — verify SecurityTab + LogoutController; (16) SET-06: Density settings — verify AppearanceTab + CSS vars + mailbox components.
  </action>
  <verify>
    <automated>php artisan test --filter=RequirementTraceTest 2>/dev/null || echo "Manual: checklist each requirement against implementation"</automated>
    <fails_when>Any SEC-01 through SEC-10 or SET-01 through SET-06 not verifiably implemented</fails_when>
  </verify>
  <acceptance_criteria>
    - All 10 SEC requirements have working implementation with verification
    - All 6 SET requirements have working implementation with verification
    - No requirement marked "Pending" in REQUIREMENTS.md for Phase 5
    - Traceability documented in SUMMARY.md
  </acceptance_criteria>
</task>

<task id="07-documentation" type="execute">
  <name>Update documentation and create Phase 5 summary</name>
  <read_first>
    - .planning/phases/05-security-polish/05-CONTEXT.md
    - config/csp.php
    - .env.example
  </read_first>
  <action>
    Finalize documentation: (1) Update config/csp.php with clear enforcement procedure: "After 2 weeks report-only with zero unexplained violations, set CSP_ENABLED=true, CSP_REPORT_ONLY=false"; (2) Ensure .env.example has all Phase 5 env vars with comments; (3) Create Phase 5 SUMMARY.md with: what was built, key decisions, verification results, CSP enforcement timeline, known limitations; (4) Update ROADMAP.md Phase 5 plans to completed status.
  </action>
  <verify>
    <automated>grep -c "CSP_ENABLED\|CSP_REPORT_ONLY\|CSP_REPORT_URI" .env.example && test -f .planning/phases/05-security-polish/05-07-SUMMARY.md && echo "SUMMARY exists"</automated>
    <fails_when>Env vars missing, SUMMARY.md not created, enforcement procedure not documented</fails_when>
  </verify>
  <acceptance_criteria>
    - config/csp.php has enforcement procedure comments
    - .env.example has CSP_ENABLED, CSP_REPORT_ONLY, CSP_REPORT_URI with descriptions
    - Phase 5 SUMMARY.md created with build summary, decisions, verification, CSP timeline
    - ROADMAP.md updated (orchestrator will handle)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → All Pages | CSP headers, security headers, theme/density classes |
| CSP Violations → Logs | Reviewed for enforcement decision |
| User Preferences → All Components | Density CSS vars, theme classes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-25 | Tampering | Premature CSP enforcement | high | mitigate | Mandatory 2-week report-only review; documented switch procedure; config toggle |
| T-05-26 | Denial of Service | Density/theme breaking layout | low | mitigate | CSS vars with fallbacks; comprehensive cross-component testing; @apply utilities |
| T-05-27 | Information Disclosure | CSP violation log noise | low | accept | Logs reviewed manually; no sensitive data in violation reports |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Package legitimacy audit in RESEARCH.md shows all approved; no new installs this phase |
</threat_model>

<verification>
Overall phase checks:
- All mailbox components (MessageList, Sidebar, Composer, MessageView, Settings) respect density CSS vars
- All pages (login, setup, mailbox, settings, errors) respect theme (dark/system/light)
- CSP violation log reviewed; policy adjusted if needed; enforcement procedure documented
- All SEC-01 through SEC-10 and SET-01 through SET-06 requirements verified complete
- Cross-browser compatibility confirmed
- Phase 5 SUMMARY.md created
</verification>

<success_criteria>
Phase 5 complete: security hardening (CSP, rate limiting, audit logging, session hardening, SSRF/MIME/attachment validation) and polish (theme, density, signatures, profile, mail preferences, session controls) all functional and verified; CSP ready for enforcement after monitoring period.
</success_criteria>

<output>
Create .planning/phases/05-security-polish/05-07-SUMMARY.md when done
</output>