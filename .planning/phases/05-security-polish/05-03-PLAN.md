---
phase: 05-security-polish
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/Providers/AppServiceProvider.php
  - config/session.php
  - app/Http/Kernel.php
  - routes/web.php
  - app/Services/MessageSanitizer.php
  - app/Http/Requests/AttachmentRequest.php
  - config/openmail.php
  - app/Services/ComposerService.php
  - app/Http/Controllers/ComposerController.php
  - tests/Unit/MessageSanitizerTest.php
  - tests/Unit/AttachmentTest.php
  - tests/Feature/RateLimitTest.php
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-04
  - SEC-06
  - SEC-07
  - SEC-08
  - SEC-09
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - Login and API endpoints enforce rate limiting with progressive delays
    - Session regeneration occurs on login; session timeout configured; fixation prevention active
    - HTML email sanitization blocks scripts, event handlers, dangerous URLs, embedded frames
    - CSS injection prevented via allowlist in HTMLPurifier
    - Outbound URLs in message content validated (SSRF prevention: block javascript:, data:, private IPs)
    - Attachment filenames UUID-prefixed with MIME-derived extensions; path traversal blocked
    - MIME type validation rejects non-allowlist types using content inspection
  artifacts:
    - Extended AppServiceProvider.php with API rate limiters (compose, search, settings, authenticated)
    - Updated config/session.php with secure cookie config, lifetime, regeneration
    - Extended MessageSanitizer.php with sanitizeUrls() for SSRF-safe link handling
    - AttachmentRequest.php with File::types() validation against MIME allowlist
    - config/openmail.php with attachments.allowed_mimes and max_size configuration
    - ComposerService/Controller using UUID-prefixed filenames for attachments
  key_links:
    - RateLimiter config → route middleware → per-endpoint enforcement
    - Session config → auth.session middleware → fixation prevention
    - MessageSanitizer → email renderer → composer → SSRF/MIME/filename safety
    - Attachment validation → ComposerService → secure storage

---

<objective>
Complete security hardening: extend rate limiting to API endpoints, finalize session hardening, implement SSRF-safe URL handling in message content, and enforce attachment filename sanitization with MIME type validation.
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-security-polish/05-CONTEXT.md
@.planning/phases/05-security-polish/05-RESEARCH.md
@.planning/phases/05-security-polish/05-01-PLAN.md
@app/Providers/AppServiceProvider.php
@app/Services/MessageSanitizer.php
@app/Services/ComposerService.php
@app/Http/Controllers/ComposerController.php
@config/openmail.php
@config/session.php
</context>

<tasks>

<task type="auto">
  <name>API Rate Limiting + Session Hardening</name>
  <files>
    app/Providers/AppServiceProvider.php
    config/session.php
    app/Http/Kernel.php
    routes/web.php
    app/Http/Middleware/VerifyCsrfToken.php
  </files>
  <action>
    Extend rate limiting to API endpoints and complete session hardening (SEC-04, SEC-06):

    1. **API Rate Limiters in AppServiceProvider** (per D-08 scope, RESEARCH.md Pattern 4):
       - Add RateLimiter::for('api.compose', ...) — 30 req/min per user/IP
       - Add RateLimiter::for('api.search', ...) — 60 req/min per user/IP
       - Add RateLimiter::for('api.settings', ...) — 120 req/min per user/IP
       - Add RateLimiter::for('api.authenticated', ...) — combined per-minute (100) + per-hour (1000) per user
       - Use database cache driver (ensure config/cache.php has 'limiter' => 'database')

    2. **Apply Rate Limiters to Routes** (routes/web.php):
       - Wrap authenticated API routes with throttle:api.authenticated middleware
       - Specific endpoints: POST /composer/send, POST /composer/save-draft, GET /search, PUT /settings/*, POST /settings/signatures

    3. **Session Hardening** (SEC-06, Laravel built-ins):
       - Update config/session.php: 'lifetime' => env('SESSION_LIFETIME', 120), 'expire_on_close' => false, 'encrypt' => true, 'secure' => env('SESSION_SECURE_COOKIE', true), 'http_only' => true, 'same_site' => 'lax'
       - Verify auth.session middleware is in web middleware group (bootstrap/app.php) — enables session regeneration on password change
       - Ensure LoginController calls $request->session()->regenerate() on successful login (already in Laravel's AuthenticatesUsers trait, verify)
       - Add session fixation test: verify session ID changes after login

    4. **CSRF Protection Verification** (SEC-10 related):
       - Verify VerifyCsrfToken middleware protects all POST/PUT/DELETE routes
       - Ensure Livewire components use @csrf or wire:csrf tokens
       - API endpoints using Sanctum tokens are exempt (not applicable — no Sanctum)

    5. **Login Throttling Enhancement** (existing + progressive delays):
       - Verify existing login limiter (per-IP 10/min, per-email 5/5min) works
       - Add escalating lockout: after 5 failures → 5min, after 10 → 15min, after 20 → 1hr
       - Implement via custom LoginController logic checking RateLimiter::tooManyAttempts()
  </action>
  <verify>
    <automated>php artisan test --filter=RateLimitTest::test_api_limits</automated>
    <fails_when>non-zero exit, or API endpoints not rate limited, or limits not per-user</fails_when>
    <automated>php artisan test --filter=RateLimitTest::test_login_throttle</automated>
    <fails_when>non-zero exit, or login throttling not enforcing progressive delays</fails_when>
    <automated>php artisan test --filter=SessionTest::test_regeneration</automated>
    <fails_when>non-zero exit, or session ID not regenerated on login</fails_when>
    <automated>php artisan test --filter=SessionTest::test_timeout</automated>
    <fails_when>non-zero exit, or session lifetime not respected</fails_when>
    <automated>php artisan test --filter=SessionTest::test_fixation_prevention</automated>
    <fails_when>non-zero exit, or session fixation not prevented</fails_when>
  </verify>
  <done>
    API rate limiters configured and applied to compose, search, settings endpoints; login throttling with progressive delays verified; session regeneration on login confirmed; secure cookie flags set; session fixation prevention via auth.session middleware active.
  </done>
</task>

<task type="auto">
  <name>SSRF-Safe URL Handling + HTML/CSS Sanitization Hardening</name>
  <files>
    app/Services/MessageSanitizer.php
    resources/views/components/email-renderer.blade.php
    tests/Unit/MessageSanitizerTest.php
  </files>
  <action>
    Harden MessageSanitizer for SSRF prevention and verify HTML/CSS sanitization (SEC-01, SEC-02, SEC-07):

    1. **SSRF-Safe URL Sanitization** (per RESEARCH.md Pattern 5, D-08 scope):
       - Add sanitizeUrls(string $html): string method to MessageSanitizer
       - Parse HTML with DOMDocument, iterate <a> tags
       - For each href, call isDangerousUrl() blocking: javascript:, data:, vbscript:, file: schemes; localhost/127.0.0.1/10.x/192.168.x/169.254.x/::1 private IPs
       - Replace dangerous href with '#', store original in data-original-href, add class='link-blocked'
       - Integrate into existing sanitizeHtml() pipeline: run sanitizeUrls() after HTMLPurifier, before DOMPurify client-side

    2. **Remote Image Blocking Enhancement** (existing blockRemoteImages + SEC-07):
       - Verify blockRemoteImages() rewrites <img src="http://..."> to data-src with user opt-in
       - Extend to also handle <source>, <video>, <audio> tags with src attributes
       - Ensure CSP img-src 'self' data: https: allows user-opted-in images

    3. **HTML Sanitization Verification** (SEC-01):
       - Verify HTMLPurifier config in MessageSanitizer blocks: <script>, <iframe>, <object>, <embed>, <form>, <input>, event handlers (on*), javascript: URLs, CSS expression()
       - Test with XSS payloads: <img src=x onerror=alert(1)>, <svg onload=alert(1)>, <a href="javascript:alert(1)">link</a>
       - Confirm DOMPurify client-side provides defense-in-depth

    4. **CSS Injection Prevention** (SEC-02):
       - Verify HTMLPurifier CSS.AllowedProperties allowlist configured (only safe properties: color, background, font, margin, padding, border, display, etc.)
       - Block: behavior, expression, @import, url() with javascript:, -moz-binding
       - Test: <div style="behavior:url(xss.htc)">, <div style="width:expression(alert(1))">

    5. **Test Coverage** (Wave 0 gaps from RESEARCH.md):
       - Create tests/Unit/MessageSanitizerTest.php with tests for: script blocking, event handler blocking, CSS allowlist, SSRF URL blocking, remote image rewrite
  </action>
  <verify>
    <automated>php artisan test --filter=MessageSanitizerTest</automated>
    <fails_when>non-zero exit, or any XSS payload passes sanitization, or SSRF URLs not blocked</fails_when>
    <automated>php artisan test --filter=MessageSanitizerTest::test_css_allowlist</automated>
    <fails_when>non-zero exit, or dangerous CSS properties not blocked</fails_when>
    <automated>php artisan test --filter=MessageSanitizerTest::test_ssrf_urls</automated>
    <fails_when>non-zero exit, or javascript:/data:/private IPs not blocked in links</fails_when>
  </verify>
  <done>
    MessageSanitizer blocks all XSS vectors (scripts, event handlers, javascript: URLs, CSS injection); sanitizeUrls() rewrites dangerous links with data-original-href; remote images blocked by default with data-src rewrite; DOMPurify client-side reinforcement active; unit tests cover all vectors.
  </done>
</task>

<task type="auto">
  <name>Attachment Filename Sanitization + MIME Validation</name>
  <files>
    config/openmail.php
    app/Http/Requests/AttachmentRequest.php
    app/Services/ComposerService.php
    app/Http/Controllers/ComposerController.php
    tests/Unit/AttachmentTest.php
  </files>
  <action>
    Implement secure attachment handling with UUID filenames and MIME validation (SEC-08, SEC-09):

    1. **MIME Allowlist Configuration** (config/openmail.php):
       - Add 'attachments' array with 'allowed_mimes' (images: jpeg/png/gif/webp/svg; documents: pdf/doc/docx/xls/xlsx/csv/txt; archives: zip/rar/7z)
       - Add 'max_size' (25MB) and 'max_total_size' (50MB)

    2. **Attachment Validation Request** (app/Http/Requests/AttachmentRequest.php):
       - Use Illuminate\Validation\Rules\File::types(config('openmail.attachments.allowed_mimes'))->min(1)->max(max_size_kb)
       - This validates actual file content (finfo), not just extension

    3. **Secure Filename Generation in ComposerService** (per D-08, RESEARCH.md code example):
       - In ComposerService attachment handling: $uuid = Str::uuid(); $extension = $file->extension(); $safeName = "{$uuid}.{$extension}"
       - Store via $file->storeAs('attachments', $safeName, 'private') (private disk = storage/app/private)
       - Never use $file->getClientOriginalName() or $file->getClientOriginalExtension()

    4. **Controller Integration**:
       - Update ComposerController to use AttachmentRequest for validation
       - Pass validated file to ComposerService::addAttachment()

    5. **Path Traversal Prevention Verification**:
       - Test: filename with ../../etc/passwd → stored as UUID only, no directory traversal
       - Test: null bytes in filename → handled by Laravel's validation
       - Test: double extensions (file.php.jpg) → MIME validation catches PHP content

    6. **Test Coverage** (Wave 0 gaps):
       - Create tests/Unit/AttachmentTest.php with tests for: MIME validation rejects non-allowlist, UUID filename generated, path traversal blocked, max size enforced
  </action>
  <verify>
    <automated>php artisan test --filter=AttachmentTest</automated>
    <fails_when>non-zero exit, or MIME validation allows non-allowlist type, or filename not UUID-prefixed</fails_when>
    <automated>php artisan test --filter=AttachmentTest::test_mime_validation</automated>
    <fails_when>non-zero exit, or File::types() not validating actual content</fails_when>
    <automated>php artisan test --filter=AttachmentTest::test_path_traversal</automated>
    <fails_when>non-zero exit, or ../ sequence affects storage path</fails_when>
    <automated>grep -r "getClientOriginalName\|getClientOriginalExtension" app/Services/ComposerService.php app/Http/Controllers/ComposerController.php</automated>
    <fails_when>non-zero exit (any match found)</fails_when>
  </verify>
  <done>
    Attachment uploads validated against MIME allowlist using content inspection; files stored with UUID-prefixed names derived from MIME type; path traversal impossible; ComposerService/Controller integration complete; unit tests verify all vectors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API Endpoints | Authenticated user requests to compose, search, settings APIs |
| Browser → Attachment Upload | User-uploaded files processed by server |
| Message Content → Renderer | Untrusted email HTML/links processed by MessageSanitizer |
| Laravel → File Storage | Attachments saved to private disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10 | Denial of Service | API rate limiting bypass | medium | mitigate | Per-user + per-IP segmented limits; database cache driver; authenticated middleware |
| T-05-11 | Spoofing | Session fixation | medium | mitigate | session()->regenerate() on login; auth.session middleware; secure HttpOnly SameSite cookies |
| T-05-12 | Tampering | XSS via email HTML | high | mitigate | Dual sanitization: HTMLPurifier (server allowlist) → DOMPurify (client) → sandboxed iframe |
| T-05-13 | Tampering | CSS injection via email | medium | mitigate | HTMLPurifier CSS.AllowedProperties allowlist; DOMPurify strips dangerous CSS |
| T-05-14 | Tampering | SSRF via message links/images | high | mitigate | MessageSanitizer::sanitizeUrls() blocks javascript:/data:/private IPs; blockRemoteImages() rewrites external images |
| T-05-15 | Tampering | Path traversal via attachment filename | high | mitigate | UUID prefix + MIME-derived extension; never trust user filename; private disk storage |
| T-05-16 | Tampering | MIME confusion attack | medium | mitigate | File::types() validates actual file content (finfo), not extension |
| T-05-17 | Information Disclosure | Session ID in logs/URLs | low | mitigate | Secure cookies, HttpOnly, SameSite=lax; no session ID in URLs; encryption enabled |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Package legitimacy audit completed; all packages [OK] |

</threat_model>

<verification>
Overall phase verification for Plan 3:
- API rate limiting: compose (30/min), search (60/min), settings (120/min) per user enforced
- Login rate limiting: progressive delays (5s, 30s, 5min, 15min) on repeated failures
- Session: regenerates on login; 120min lifetime; secure/HttpOnly/SameSite cookies; fixation prevention
- HTML sanitization: blocks scripts, event handlers, javascript: URLs, iframes, forms, inputs
- CSS sanitization: allowlist only safe properties; blocks expression, behavior, @import
- SSRF prevention: links with javascript:/data:/vbscript:/file: schemes blocked; private IPs blocked
- Remote images: rewritten to data-src by default; user opt-in to load
- Attachments: MIME validated via File::types(); UUID filenames; stored on private disk; max 25MB/file, 50MB total
</verification>

<success_criteria>
- All RateLimitTest, SessionTest, MessageSanitizerTest, AttachmentTest pass
- API endpoints return 429 when limits exceeded with Retry-After header
- Session ID changes after login
- No XSS payloads render in message viewer
- Dangerous links rewritten in message content
- Attachment uploads with malicious filenames/types rejected
</success_criteria>

<output>
Create .planning/phases/05-security-polish/05-03-SUMMARY.md when done
</output>