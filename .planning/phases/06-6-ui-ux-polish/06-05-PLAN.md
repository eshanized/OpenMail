---
phase: 06-6-ui-ux-polish
plan: 05
type: execute
wave: 4
depends_on: [06-02, 06-03, 06-04]
files_modified:
  - resources/js/app.js
  - resources/views/livewire/settings/appearance-tab.blade.php
  - resources/views/livewire/settings/settings-page.blade.php
  - resources/views/livewire/settings/profile-tab.blade.php
  - resources/views/livewire/settings/mail-tab.blade.php
  - resources/views/livewire/settings/security-tab.blade.php
  - resources/views/livewire/settings/signatures-tab.blade.php
autonomous: true
requirements: [D-01, D-04, D-05, D-06, D-07, D-15]
user_setup: []

estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - Settings loads with zero inline scripts: both the appearance preview and the settings-tab controller register as Alpine components in the Vite bundle (registered on the alpine:init event), so no CSP-relevant inline script remains anywhere in the settings pages (T-06-04; complements 06-02's nonce posture)
    - The appearance theme/density pickers rest on glass cards with the gradient accent only on the selected state (ring on the primary stop), matching the mailbox chrome from 06-03 (D-06, D-07, D-01)
    - All five settings tabs present consistent glass-card form surfaces, glass text inputs, a gradient primary Save button per tab, AA-safe secondary actions, and uniform section spacing per the balanced-spacing decision (D-06, D-07, D-01, D-05, D-15)
    - The full phase is integration-green: complete test suite (wave-0 through wave-4 test scaffolding), production build, fontsource build gate, and the manual visual QA checklist from VALIDATION.md (contrast re-check both themes, axe scan, reduced-motion/-transparency, x-cloak no-flash, route-overlay fade, skeletons, three density modes) with evidence committed in the SUMMARY
  artifacts:
    - resources/js/app.js (+Alpine.data registrations: appearancePreview, settingsTabs — no global function declarations remain in any settings view)
    - appearance-tab.blade.php (glass pickers, gradient selected-state ring, x-data passes server-rendered theme/density via props)
    - settings-page.blade.php (glass tab panel container, x-data passes the tab keys via the existing @js expression), profile-tab/mail-tab/security-tab/signatures-tab (glass forms + gradient Save, unified spacing)
  key_links:
    - The alpine:init registration must be the only mechanism wiring x-data names — if window.Alpine is not yet defined at bundle eval time, the alpine:init listener resolves it; the prop-passing x-data expressions (server-rendered initialTheme/initialDensity and the tab-key array) replace the blade interpolations that currently live inside the two inline script bodies
    - The appearance preview behavior contract must survive the move byte-for-byte: updatedTheme/updatedDensity still dispatch the theme-changed/density-changed window events that app.js's existing listeners consume (app.js lines 51-66), and the file-save persistence through AppearanceTab's updatedTheme/updatedDensity remains wire:model.live-driven
---

<objective>
Finish phase 06 where the mailbox left off: the settings surface gets the same glass/gradient/spacing language (D-06, D-07, D-01, D-05), both remaining inline scripts migrate into the Vite bundle as Alpine components (closing the last CSP-relevant inline-script surface outside the pre-existing nonce'd initializer), and the phase's integration gate produces the verification evidence set.

Purpose: The decisions must hold on every authenticated surface, not just the mailbox. Settings is where wire:model density/theme previews and the tab controller interact with the 06-02/06-03 theme — the last integration risk of the phase. This plan also runs the phase-level gate that proves all sixteen decisions together in both themes.
Output: A fully polished settings surface with zero inline scripts, and a committed evidence set (tests + build + gate + manual QA checklist) that closes 06-VALIDATION.md.
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-6-ui-ux-polish/06-CONTEXT.md
@.planning/phases/06-6-ui-ux-polish/06-RESEARCH.md
@.planning/phases/06-6-ui-ux-polish/06-UI-SPEC.md
@.planning/phases/06-6-ui-ux-polish/06-VALIDATION.md
@.planning/phases/05-security-polish/05-03-SUMMARY.md
@resources/js/app.js
@resources/views/livewire/settings/appearance-tab.blade.php
@resources/views/livewire/settings/settings-page.blade.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Appearance tab — glass pickers + Alpine.data migration of appearancePreview (D-06, D-07, D-01, T-06-04)</name>
  <files>resources/js/app.js, resources/views/livewire/settings/appearance-tab.blade.php</files>
  <read_first>
    appearance-tab.blade.php: the root div binds x-data="appearancePreview()" with x-init="init()"; the inline script block at the end (lines 90-122) declares a global function appearancePreview() whose state holds previewDensityClass (seeded with a blade interpolation of the server density value), and whose updatedTheme/updatedDensity handlers toggle the html class list, write localStorage, and dispatch the theme-changed/density-changed CustomEvents. app.js already listens for those two events (lines 51-66) and Livewire renders AppearanceTab with theme/density props (validateTheme/validateDensity snap invalid values back on persisted state — that server contract stays untouched). No Alpine.data registrations exist in app.js yet — the alpine:init listener is the canonical Alpine 3 registration point and works with the Livewire-bundled Alpine instance.
  </read_first>
  <action>
    Migrate the appearance preview off the inline script and onto the token system:
    (1) resources/js/app.js — append an Alpine component registration inside a document-level alpine:init listener. Register appearancePreview as a local component factory receiving a props argument with shape { initialTheme, initialDensity }: seed the preview state from props (the density class becomes props.initialDensity with a density- prefix, falling back to density-regular when absent), and port the init() + updatedTheme/updatedDensity bodies from the current inline script byte-for-byte in behavior (class toggling on documentElement, localStorage writes, dispatch of the same two window CustomEvents). No other app.js listener may change — the existing DOMContentLoaded block and the theme/density event listeners stay untouched.
    (2) appearance-tab.blade.php — remove the entire inline script block (lines 90-122) and delete the global function. Change the root div binding to pass the server-rendered values through the x-data expression: x-data="appearancePreview({ initialTheme: '{{ $theme }}', initialDensity: '{{ $density }}' })" keeping x-init="init()", the wire:model.live radios, aria-describedby ids, and descriptions byte-identical.
    (3) Restyle the pickers: each theme/density option card becomes a glass-card surface (the 06-03 utility) with a static selected-state ring on the primary stop and neutral text on the cream/soft-dark tokens; keep the live preview box functional as-is (it is the D-15/16 density demonstration). Static classes only; do not interpolate user input into class lists (V5); no new inline scripts anywhere in this view.
  </action>
  <verify>
    <automated>rg -c "script" resources/views/livewire/settings/appearance-tab.blade.php | rg -q '^0$' && rg -c "alpine:init" resources/js/app.js | rg -q '^1$' && php artisan test --filter=AppearanceTest && npm run build && node scripts/check-fontsource-build.js</automated>
    <fails_when>any inline script tag still exists in appearance-tab (count not zero — the word script must not appear on any tag line); the alpine:init registration is missing; AppearanceTest fails (theme/density wiring broken by the prop-driven move); build or gate non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    No script element remains in appearance-tab.blade.php; the Alpine.data registration exists in app.js under the alpine:init listener; the preview behaves identically (AppearanceTest green) including invalid-value snap-back through the server validator.
  </acceptance_criteria>
  <done>
    The appearance tab renders glass pickers with a gradient selected ring, and its interactivity is fully bundled — verified by the no-script grep, the registration grep, AppearanceTest, and the build gate.
  </done>
  <reversibility rating="reversible">The inline script is recoverable from the 06-03 SUMMARY commit; the migration is a bundle move, not a behavior change.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Settings page + remaining tabs — glass forms, gradient Save, settingsTabs migration, unified spacing (D-01, D-04, D-05, D-06, D-07, D-15)</name>
  <files>resources/views/livewire/settings/settings-page.blade.php, resources/views/livewire/settings/profile-tab.blade.php, resources/views/livewire/settings/mail-tab.blade.php, resources/views/livewire/settings/security-tab.blade.php, resources/views/livewire/settings/signatures-tab.blade.php, resources/js/app.js</files>
  <read_first>
    settings-page.blade.php: root div x-data="settingsTabs()" (line 1); tab nav (lines 3-25) is a bordered underline tabset with kb arrow handlers calling focusNextTab/focusPrevTab/selectTab via a global function declared in the trailing inline script (lines 57-100) that also owns the toast live-region. The panel container (line 28) is a plain div. Each tab view: profile-tab/mail-tab/security-tab/signatures-tab render forms with standard secondary styling (white/gray-50 cards, gray bordered inputs, solid blue Save buttons, hover states), per Phase 5's work. wire:click/wire:model handlers are Phase-5 contracts — touch classes and scripts only, never handlers.
  </read_first>
  <action>
    (1) settings-page.blade.php — remove the trailing inline script (lines 57-100) and delete the global settingsTabs function. Change the root binding to pass the tab keys through the existing blade-to-js concatenation: x-data="settingsTabs(@js(array_keys($tabs)))" keeping x-init="init()". In app.js, extend the same alpine:init listener from task 1 with a settingsTabs registration whose factory takes the tab-keys array and whose object exposes toastMessage, toastType, the init() toast-event subscription (this.$wire.on('toast', ...)), and the three tab-navigation methods with their exact current bodies.
    (2) Restyle the container and nav: the tab-panel region (line 28 div) becomes a glass-card surface; the tab underline/active treatment moves to the primary stop for the active state and the surface tokens for hover/inactive (06-03 vocabulary); the toast live-region keeps its solid green/red AA colors and role/aria attributes untouched.
    (3) Restyle the four tab views to the same language: each form card becomes a glass-card surface; text inputs become the D-06 glass input treatment from 06-03; the primary Save button per tab becomes the gradient primary (from-blue-600 to-purple-600, rounded-lg, glow shadow, hover scale 1.02, focus-visible ring, motion-reduce suppression) exactly following the 06-02/06-03 button contract; destructive/secondary actions stay solid AA red / neutral gray surface; unify section spacing and card padding across the four tabs to the balanced-spacing rules (D-15) — match the appearance tab's gutter after task 1 so all five tabs share one rhythm.
    (4) Static classes only; no new inline scripts; no handler changes; wire:model/wire:click/wire:keyboard bindings byte-identical.
  </action>
  <verify>
    <automated>rg -c "script" resources/views/livewire/settings/*.blade.php | grep -v ':0' | wc -l | rg -q '^0$' && rg -c "settingsTabs" resources/js/app.js | rg -q '^1$' && php artisan test --filter=AppearanceTest && php artisan test --filter='UiPolishTest::(test_send_and_archive_buttons_have_gradient_classes|test_glass_surface_classes_present)' && npm run build && node scripts/check-fontsource-build.js</automated>
    <fails_when>any settings blade still contains a script element (count of non-zero files not zero); the settingsTabs registration is missing from app.js; AppearanceTest or the two pinned UiPolishTest methods fail; build or gate non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    Zero script elements across all six settings views; settingsTabs registered in the bundle; glass forms + gradient Save on all five tabs with uniform spacing; no handler/attribute changes.
  </acceptance_criteria>
  <done>
    Settings reads as one polished surface — glass pickers, glass forms, gradient Save buttons, one spacing rhythm — with zero inline scripts and the tab controller fully bundled, verified by the no-script sweep across all settings views and a green suite.
  </done>
  <reversibility rating="reversible">View-level class/script moves; all behavior preserved, revert local.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Phase 06 integration gate — full suite, build, gate, and committed QA evidence (closes 06-VALIDATION.md)</name>
  <files>- (verification-only)</files>
  <read_first>
    The wave-0 scaffolding (MessageListLoadingTest, AppearanceTest, UiPolishTest) plus the wave-1/2/3 plan verifiers defined the per-task contracts; 06-VALIDATION.md defines the four wave-0 artifacts, the per-method test map, and the manual review checklist this gate closes out. The csp.php null-diff contract from 06-02 and the glass/gradient/no-chrome guards from UiPolishTest are the security invariants re-proven here. The 06-04 SUMMARY carries the accepted-deprecation notes (out-of-scope wire:loading instances) to carry forward.
  </read_first>
  <action>
    Run the phase integration gate and commit the evidence set (verification-only — no new files expected: any regression fix lands in the owning task's file set from the earlier plans of this phase and is reported in the SUMMARY):
    (1) Full automated gate: php artisan test (entire suite — wave-0 scaffolding plus all phase tests), npm run build, node scripts/check-fontsource-build.js, and git diff --stat config/csp.php asserting an empty diff (CSP policy untouched through the whole phase).
    (2) Manual visual QA per the VALIDATION review checklist, in both themes: contrast re-check on every new stop from the research contrast table (gradient buttons, soft-tag badges, glass on cream/soft-dark, blob layering behind text), axe scan of the mailbox and settings routes with reduced-motion and reduced-transparency enabled plus 200% zoom, x-cloak no-flash check on the folder/label/search panels, route-overlay fade on wire:navigate, skeleton visibility during folder switch/search/refresh, and all three density modes rendering (1.4 line-height regular). Record pass/fail per item.
    (3) Write 06-05-SUMMARY.md (and update the phase STATE) capturing: the dark-token retargeting outcome from 06-02, the data-loading migration coverage + accepted deprecated wire:loading leftovers, the two Alpine.data migrations, any deviating files vs plan files_modified, and the QA evidence — then commit everything for this plan.
  </action>
  <verify>
    <automated>php artisan test && npm run build && node scripts/check-fontsource-build.js && test -z "$(git diff --stat config/csp.php)"</automated>
    <fails_when>any test in the full suite reports failure; build or gate exits non-zero; or git diff shows config/csp.php modified (only the CSP file is checked — a diff anywhere in it breaks the T-06-01 invariant)</fails_when>
  </verify>
  <acceptance_criteria>
    Full suite green, build + gate green, csp.php diff empty, QA checklist fully recorded with no unmitigated high findings, and the SUMMARY committed with evidence.
  </acceptance_criteria>
  <done>
    Phase 06 is integration-green: every wave-0 through wave-4 test passes in one sweep, the build gate passes, the CSP policy is byte-identical, and the manual QA evidence set (both themes, reduced-motion/-transparency, zoom, fade, skeletons, densities) is committed alongside 06-05-SUMMARY.md.
  </done>
  <reversibility rating="reversible">Evidence-only task; fixes to earlier artifacts are already reversible per their plans.</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces

| Artifact | Type | Consumed By |
|----------|------|-------------|
| `resources/js/app.js` — Alpine.data registrations (appearancePreview, settingsTabs) | JS | only JS bundle change of the phase beyond 06-02's hooks; no settings inline script remains |
| appearance-tab.blade.php — glass pickers, prop-driven x-data, no script element | view | final appearance surface consumed by the QA checklist |
| settings-page.blade.php + profile/mail/security/signatures tabs — glass forms, gradient Save, unified spacing | views | phase-complete settings surface |
| 06-05-SUMMARY.md — integration gate evidence + accepted-deviation notes | summary | closes 06-VALIDATION.md; input to phase verification |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-rendered settings state → Alpine component props | theme/density/tab keys flow from Blade into x-data expressions evaluated by Alpine (same trust level as the pre-existing nonce'd inline initializer) |
| bundled Alpine components → DOM | appearance preview toggles html classes and dispatches theme/density events consumed by app.js |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Info disclosure | inline scripts on settings pages | high | mitigate | Both settings scripts migrate into the Vite bundle (nonce-integrated); tasks 1-2 verifiers assert zero script elements remain across all six settings views; app.js additions are static code with no user input |
| T-06-01 | Tampering | config/csp.php | high | mitigate | CSP policy not in files_modified for any plan this phase; task 3 gate asserts an empty diff on the file |
| T-06-02 | Tampering | class/color interpolation (V5) | high | mitigate | All new settings markup uses static class strings; x-data props carry server-rendered scalar values only (validated theme/density from AppearanceTab) |
| T-06-03 | Tampering | email HTML reusing app chrome classes | medium | mitigate | message-viewer untouched again; UiPolishTest no-chrome guard re-run in the full suite |
| T-06-05 | Tampering | contrast/spacing regressions in settings | medium | mitigate | Gradient stops are the AA-passing 600-level pair; QA checklist re-verifies contrast both themes incl. axe; reduced-motion/-transparency guards baked in 06-02 |
</threat_model>

<verification>
- Zero `<script>` elements across all six settings views (grep-count per file == 0).
- `alpine:init` + both Alpine.data registrations present in app.js (grep).
- Full suite `php artisan test` green; `npm run build`; gate exit 0; `git diff --stat config/csp.php` empty.
- Manual QA checklist (VALIDATION.md) fully recorded in the SUMMARY with pass/fail per item.
</verification>

<success_criteria>
- Settings is porcelain: glass pickers/forms, gradient Save on all five tabs, one spacing rhythm, zero inline scripts.
- Phase 06 closes with every D-01..D-16 decision observable in both themes, all test scaffolding green in one sweep, CSP byte-identical, and the evidence set committed.
</success_criteria>

<output>
Create `.planning/phases/06-6-ui-ux-polish/06-05-SUMMARY.md` when done
</output>