---
phase: 06-6-ui-ux-polish
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/Feature/Mailbox/MessageListLoadingTest.php
  - tests/Feature/Settings/AppearanceTest.php
  - tests/Feature/UiPolishTest.php
  - scripts/check-fontsource-build.js
autonomous: true
requirements: [D-01, D-04, D-06, D-07, D-08, D-11, D-13, D-16]
user_setup: []

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - The three Wave 0 test files exist, lint clean, and run under PHPUnit: MessageListLoadingTest and UiPolishTest are intentionally RED against today's markup (TDD contract — their assertions name markup that plans 06-02/06-03/06-04 will deliver), AppearanceTest is GREEN today (protects existing theme/density persistence from the restyle)
    - UiPolishTest gives per-behavior methods (compose gradient, send/archive gradient, x-cloak rule, glass surfaces, soft-tag badges, message-viewer chrome guard, CSP nonce meta) so later plans can run precise single-method filters
    - The build gate script exists in scripts/ and exits non-zero today (fontsource css absent from public/build/manifest.json), providing the objective font-delivery signal plan 06-02 flips to exit 0
    - Tests assert only git-tracked source and static class strings — no user-controlled values are ever asserted or interpolated (V5)
  artifacts:
    - tests/Feature/Mailbox/MessageListLoadingTest.php
    - tests/Feature/Settings/AppearanceTest.php
    - tests/Feature/UiPolishTest.php
    - scripts/check-fontsource-build.js
  key_links:
    - MessageListLoadingTest's wire:target needle matches the real MessageList component methods (onFolderChanged, setSort, toggleThreadMode) so the future skeleton implementation is pinned to actual behavior
    - Build gate exit code is consumed verbatim by plan 06-02's tracer verify — gate passes only when the font bundle reaches the committed build manifest
---

<objective>
Create the Wave 0 validation scaffold the entire phase runs on: three PHPUnit test files (`MessageListLoadingTest`, `AppearanceTest`, `UiPolishTest`) and one build-gate script (`scripts/check-fontsource-build.js`). These are the RED/GREEN anchors required by VALIDATION.md before any restyle lands.

Purpose: Without these tests, every later plan's `<verify>` would fall back to `|| echo "Manual..."` (the Phase 5 pattern that masked missing coverage). With them, each of plans 06-02 through 06-05 can verify real green transitions per behavior.
Output: The four files above, committed in a RED-by-design state for the two implementation-guarding tests and GREEN for the regression-guard tests.

RED is the TDD contract here, not a failure: MessageListLoadingTest and UiPolishTest assert markup that only exists after plans 06-02/06-03/06-04 land. The executor MUST NOT "fix" the RED tests by weakening assertions — if a RED expectation is wrong, the plan's checkpoints below name the exact expected RED needles so a genuinely-broken scaffold (e.g. typo in component name) is distinguishable from an expected-RED assertion.
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-6-ui-ux-polish/06-CONTEXT.md
@.planning/phases/06-6-ui-ux-polish/06-RESEARCH.md
@.planning/phases/06-6-ui-ux-polish/06-VALIDATION.md
@.planning/phases/06-6-ui-ux-polish/06-UI-SPEC.md
@tests/Feature/ThreadUITest.php
@tests/Feature/Mailbox/MailboxIntegrationTest.php
@app/Livewire/Mailbox/MessageList.php
@app/Livewire/Settings/AppearanceTab.php
@resources/views/livewire/mailbox/message-list.blade.php
@resources/css/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MessageListLoadingTest (RED for D-11) and AppearanceTest (GREEN guard for D-16/theme persistence)</name>
  <files>tests/Feature/Mailbox/MessageListLoadingTest.php, tests/Feature/Settings/AppearanceTest.php</files>
  <read_first>
    Per D-11 (skeleton screens) and D-16 (line-height 1.4). Model on ThreadUITest: Mockery mock of App\Services\ImapMailboxService (getCachedFolders, getMessageCount, getFolders, refreshFolderCache, getMessages → empty LengthAwarePaginator, getThreadHeaders), plus FolderMapper and MessageSanitizer instances bound in the container. MessageList component is 'mailbox.message-list' with folderPath prop; its real methods are onFolderChanged, setSort, toggleThreadMode — the skeleton wire:target must name exactly these.
  </read_first>
  <action>
    Create tests/Feature/Mailbox/MessageListLoadingTest.php (namespace Tests\Feature\Mailbox, extends Tests\TestCase, RefreshDatabase, user factory, imap mock per ThreadUITest). Two methods per D-11:
    (1) test_message_list_has_skeleton_loading_guard — Livewire::test('mailbox.message-list', ['folderPath' => 'INBOX']) asserting the rendered HTML contains the comma-joined wire:target needle naming onFolderChanged, setSort, and toggleThreadMode, plus the wire:loading.remove guard on the real-content container. This is RED today — the current view carries no such markup (the research-confirmed temporary overlay at message-list.blade.php sits app-wide).
    (2) test_message_list_has_no_full_screen_spinner_overlay — the same component assertDontSee on the current overlay markup's distinguishing class string (the fixed inset full-screen backdrop with the z-50 spinner host). RED today because that overlay is still in the view; plan 06-04 removes it.
    Do NOT weaken either assertion; both needles are documented here and in 06-04's task 1.
    Create tests/Feature/Settings/AppearanceTest.php (namespace Tests\Feature\Settings, same base/mocking approach but for the settings.appearance-tab Livewire component — no imap dependency). Three GREEN-regression methods that must pass against the CURRENT implementation and stay green through the 06-05 restyle (D-15/D-16 + appearance persistence): (a) theme radios render with wire:model.live="theme" and updating theme to 'dark' persists via a Setting row for the user (assertDatabaseHas on the settings table with key/value relevant columns) and dispatches the theme-changed event; (b) density radios render with wire:model.live="density" and updating density to 'compact' persists likewise; (c) an invalid theme value (e.g. 'pink') snaps back to 'system' via the component's validateTheme guard. Mirror AppearanceTab's real methods (mount reads user setting with defaults 'system'/'regular'; updatedTheme/updatedDensity persist via Setting::setForUser). Use the proven phase-05 test-class bootstrap: actingAs($this->user) + Livewire component test.
    Both files: PHP 8.3 typed syntax, PHPUnit #[Test] attributes, PHPUnit\Framework\Attributes\Test import, tearDown closes Mockery. Per D-16 the density test asserts the component state, not the CSS value (the CSS assertion lives in plan 06-02's verifier).
  </action>
  <verify>
    <automated>php -l tests/Feature/Mailbox/MessageListLoadingTest.php && php -l tests/Feature/Settings/AppearanceTest.php && php artisan test --filter=AppearanceTest && php artisan test --filter=MessageListLoadingTest 2>&1 | rg -F -e 'wire:target="onFolderChanged' -e 'does not contain'</automated>
    <fails_when>php -l exits non-zero; AppearanceTest reports failures (the GREEN guard is broken); the MessageListLoading run exits 0 AND prints OK (both assertions passed — vacuously guarding nothing); or its output lacks both expected RED needles (skeleton-guard assertion missing its "contains" needle and overlay-absence assertion missing its "does not contain" needle)</fails_when>
  </verify>
  <acceptance_criteria>
    Both test files exist, lint clean, and run: AppearanceTest is fully green; MessageListLoadingTest fails with exactly the two documented RED needles in its output (skeleton guard absent today, overlay still present today).
  </acceptance_criteria>
  <done>
    MessageListLoadingTest (2 methods, both RED with documented needles) and AppearanceTest (3 methods, all GREEN) exist in tests/Feature/Mailbox/ and tests/Feature/Settings/ (new directory) and the combined verifier command above passes.
  </done>
  <reversibility rating="reversible">Test scaffolding; assertions can be refined without architectural impact.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Create UiPolishTest (RED for D-01/D-04/D-06/D-07/D-08 + GREEN security/CSP guards)</name>
  <files>tests/Feature/UiPolishTest.php</files>
  <read_first>
    Per D-01/D-04 (gradient primary actions, accessibility-adjusted stops from-blue-600 to-purple-600 per research's Contrast Table — white text on 600-level stops passes AA 5.17:1/5.38:1), D-06 (glass inputs backdrop-blur-sm), D-07 (glass cards glass-card), D-08 (soft-tag badges), plus the CSP nonce invariant (spatie/laravel-csp emits @cspNonceMetaTag in layouts/app.blade.php — plan 06-02 must not remove it) and the cross-phase security invariant (message-viewer render container carries no app-chrome classes).
  </read_first>
  <action>
    Create tests/Feature/UiPolishTest.php (namespace Tests\Feature). One class with seven methods, each independently filterable:
    (1) test_compose_button_has_gradient_classes — Livewire::test('mailbox.message-list', ['folderPath' => 'INBOX']) asserting the rendered HTML contains the contiguous class substring from-blue-600 to-purple-600 (this needle lands in plan 06-02's tracer on the Compose button). RED today.
    (2) test_send_and_archive_buttons_have_gradient_classes — source-level: read resources/views/livewire/mailbox/composer.blade.php and resources/views/livewire/mailbox/message-toolbar.blade.php and assertStringContainsString the same gradient substring in each (lands in plan 06-03). RED today.
    (3) test_x_cloak_css_rule_exists — source-level: read resources/css/app.css and assert it contains the bare x-cloak attribute selector (this exact selector has no rule today — the research-verified app-wide flash bug; plan 06-02 adds it). RED today.
    (4) test_glass_surface_classes_present — source-level: assertStringContainsString backdrop-blur-sm in folder-sidebar and search-results-dropdown blade markup, and the glass-card utility class name in mailbox layout markup (lands 06-03). RED today.
    (5) test_status_badges_use_soft_tag_style — source-level: folder-sidebar unread-count pill keeps the tinted-bg + colored-text soft-tag pattern (assert the bg-*-100 tinted pill fill and text-*-600 colored text pair — per D-08 and the contrast table where red-600/gray-500 are the AA floor on cream). GREEN today (pill already matches); regression guard for 06-03.
    (6) test_message_viewer_has_no_chrome_classes — source-level: resources/views/livewire/mailbox/message-viewer.blade.php must NOT contain the glass- prefix on any class and must NOT contain the gradient utility prefix bg-linear anywhere (cross-phase invariant: email HTML must never reuse app chrome styling). GREEN today and MUST stay green — 06-03/06-04 do not touch message-viewer.
    (7) test_layout_emits_csp_nonce_meta — HTTP GET /mailbox behind actingAs(user) + session seeding (imap mock as in ThreadUITest), asserting the response contains the csp-nonce meta tag name (spatie output). GREEN today; guards plan 06-02's app.blade.php edits.
    Static class strings only — never assert a class derived from user input (V5).
  </action>
  <verify>
    <automated>php -l tests/Feature/UiPolishTest.php && php artisan test --filter=UiPolishTest 2>&1 | rg -F -e 'from-blue-600' -e '\[x-cloak\]' -e 'backdrop-blur-sm'</automated>
    <fails_when>php -l exits non-zero; the filter exits 0 AND prints OK (every method passed — the RED expectations are vacuous); or the output lacks at least one of the three RED needles (gradient, x-cloak selector, glass blur) meaning that future implementation would not be observationally pinned</fails_when>
  </verify>
  <acceptance_criteria>
    UiPolishTest exists with 7 independently-filterable methods; methods (1)-(4) are RED with their documented needles; methods (5)-(7) are GREEN (nonce meta emitted, soft-tag pill intact, message-viewer chrome-free).
  </acceptance_criteria>
  <done>
    All seven methods run under php artisan test --filter=UiPolishTest with the five-known-state split (4 RED / 3 GREEN) and the verifier command above passes.
  </done>
  <reversibility rating="reversible">Test file only; no production impact.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Create build-gate script for font delivery (D-13)</name>
  <files>scripts/check-fontsource-build.js</files>
  <read_first>
    Per D-13 (Plus Jakarta Sans self-hosted via @fontsource-variable/plus-jakarta-sans, legitimacy-approved in the research Package Legitimacy Audit) and the research Pitfall 7 (font css must land in public/build/manifest.json after npm run build; JS-side import is the guaranteed path). scripts/ is a new directory — the script is a tracked source artifact, while public/build/ is gitignored build output the script merely verifies.
  </read_first>
  <action>
    Create scripts/check-fontsource-build.js as an ES module (package.json type:module) with no third-party imports (node:fs only). Behavior: read public/build/manifest.json; fail with a clear message if the file is missing; parse entries and require an entry whose emitted file path contains the jakarta font family token (any entry whose src or file references fontsource or jakarta); additionally require the app.css and app.js entries to exist so the gate cannot pass on a stale partial build; print an OK line and exit 0 when all three conditions hold, otherwise print a FAIL line and exit 1. Exit code 1 is the expected state TODAY (font not yet installed — plan 06-02 flips it to 0). Do not weaken the gate to pass on an empty manifest; the manifest sanity check for app assets is part of the gate. Keep the file free of any dynamic user input — it validates build output only.
  </action>
  <verify>
    <automated>node -e "import('node:fs').then(fs=>{if(!fs.existsSync('scripts/check-fontsource-build.js'))process.exit(2)})" && node scripts/check-fontsource-build.js; test $? -eq 1</automated>
    <fails_when>the script is missing (exit 2 from the existence probe); the script exits 0 (font already in manifest — vacuous gate) or any exit code other than the expected 1 (including a crash with a stack trace that masks the intended state)</fails_when>
  </verify>
  <acceptance_criteria>
    scripts/check-fontsource-build.js exists, runs without crashing, and exits 1 today with a message naming the missing fontsource entry.
  </acceptance_criteria>
  <done>
    The gate script is committed, executes cleanly on today's build manifest, and its exit-1 state is the documented precondition plan 06-02's tracer turns into exit-0.
  </done>
  <reversibility rating="reversible">Independent script; trivially adjustable.</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces

| Artifact | Type | Consumed By |
|----------|------|-------------|
| `tests/Feature/Mailbox/MessageListLoadingTest.php` — 2 methods (skeleton guard RED, overlay-absence RED) | test | plan 06-04 task 1 (flips green) |
| `tests/Feature/Settings/AppearanceTest.php` — 3 methods (theme/density persistence + invalid-value guard, all GREEN) | test | plans 06-02 / 06-05 (regression guard) |
| `tests/Feature/UiPolishTest.php` — 7 methods (4 RED: compose gradient, send/archive gradient, x-cloak, glass; 3 GREEN: soft-tag badge, message-viewer chrome guard, CSP nonce) | test | plans 06-02 task 1, 06-03 tasks 1-3, 06-04 (partial green per wave) |
| `scripts/check-fontsource-build.js` — manifest gate (needs fontsource+app.css+app.js entries; exit 1 today) | script | plan 06-02 task 1 (flips to exit 0) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → asserted source | Test assertions read git-tracked source and rendered HTML; no user-controlled runtime input crosses into assertions (V5) |
| build output → gate | scripts/ reads gitignored public/build/manifest.json output only — never a source of truth for planning, only a delivery signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | test assertions on user-controlled values | low | mitigate | All UiPolishTest assertions use static class strings only; the message-viewer chrome guard is a static negative assertion with no interpolation |
| T-06-02 | Tampering | gate script trusting arbitrary manifest content | low | mitigate | Gate validates structure (entries present) plus the font token; exits non-zero on parse failure; no shell eval/exec of manifest content |
| T-06-SC | Tampering | npm package legitimacy (D-13 dependency) | high | mitigate | Package Legitimacy Audit completed in RESEARCH.md: @fontsource-variable/plus-jakarta-sans v5.3.0 [OK]/Approved, no postinstall scripts; plan 06-02 installs verbatim from the audited registry entry — no checkpoint required (no [ASSUMED]/[SUS] verdicts) |
</threat_model>

<verification>
- `php -l` on all three test files: clean.
- `php artisan test --filter=AppearanceTest`: fully green.
- `php artisan test --filter=MessageListLoadingTest` and `--filter=UiPolishTest`: fail with exactly the documented RED needles (this is wave 0's contract — later plans flip specific methods green; the executor halts and reports if a RED needle is missing because that means the assertion no longer pins future work).
- `node scripts/check-fontsource-build.js`: exit 1 with the missing-fontsource message.
</verification>

<success_criteria>
- All four Wave 0 artifacts from VALIDATION.md exist and are committed (test scaffold + build gate).
- Red/green state per artifact is recorded in the SUMMARY so every later plan knows its starting signal.
- No production file was modified — wave 0 is scaffolding only (git diff confirmed).
</success_criteria>

<output>
Create `.planning/phases/06-6-ui-ux-polish/06-01-SUMMARY.md` when done
</output>