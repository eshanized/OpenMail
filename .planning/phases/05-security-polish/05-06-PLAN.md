---
phase: 05-security-polish
plan: 06
type: execute
wave: 5
depends_on:
  - "05"
files_modified:
  - app/Livewire/Settings/ProfileTab.php
  - app/Livewire/Settings/MailTab.php
  - app/Livewire/Settings/SecurityTab.php
  - resources/views/livewire/settings/profile-tab.blade.php
  - resources/views/livewire/settings/mail-tab.blade.php
  - resources/views/livewire/settings/security-tab.blade.php
  - app/Http/Requests/SettingsRequest.php
  - app/Models/User.php
  - app/Models/Setting.php
  - routes/web.php
autonomous: true
requirements:
  - SET-01
  - SET-04
  - SET-05
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 3
  confidence: med
must_haves:
  truths:
    - Profile tab shows name and email, allows editing with validation
    - Mail tab configures page size, default folder, reply behavior (reply vs reply all)
    - Security tab shows active sessions with revoke-other-sessions action
    - All preferences persist to database via Setting model (user-scoped)
    - User model has signatures() and auditLogs() relationships
    - Setting model has user_id and user-scoped getForUser/setForUser methods
    - Session revocation deletes other sessions from sessions table
  artifacts:
    - app/Livewire/Settings/ProfileTab.php
    - app/Livewire/Settings/MailTab.php
    - app/Livewire/Settings/SecurityTab.php
    - resources/views/livewire/settings/profile-tab.blade.php
    - resources/views/livewire/settings/mail-tab.blade.php
    - resources/views/livewire/settings/security-tab.blade.php
    - app/Http/Requests/SettingsRequest.php
    - app/Models/User.php (modified with relationships and setting accessor)
    - app/Models/Setting.php (modified with user_id and user-scoped methods)
    - routes/web.php (modified with session revocation route)
  key_links:
    - ProfileTab/MailTab/SecurityTab → SettingsRequest validation → Setting::setForUser()
    - SecurityTab → LogoutController::revokeOtherSessions() → sessions table
    - User::setting() accessor → Setting::getForUser() → cached preferences
    - Setting::setForUser() → cache invalidation → immediate UI update
---

<objective>
Complete remaining Settings tabs (Profile, Mail, Security) and wire User/Setting models for preference persistence per SET-01, SET-04, SET-05.

Purpose: Deliver complete Settings page with all five tabs functional, user preferences persisted per-user, and session management controls for security.

Output: Three tab components with views, validation request, User model extensions (relationships, setting accessor), Setting model extensions (user-scoped methods), and session revocation route.
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-security-polish/05-CONTEXT.md
@.planning/phases/05-security-polish/05-RESEARCH.md
@.planning/phases/05-security-polish/05-PATTERNS.md
@.planning/phases/05-security-polish/05-01-PLAN.md
@.planning/phases/05-security-polish/05-02-PLAN.md
@.planning/phases/05-security-polish/05-03-PLAN.md
@.planning/phases/05-security-polish/05-04-PLAN.md
@.planning/phases/05-security-polish/05-05-PLAN.md
</context>

<tasks>

<task id="06-tracer" type="execute">
  <name>End-to-end Settings tabs — Profile, Mail, Security with User/Setting model extensions</name>
  <files>
    - app/Livewire/Settings/ProfileTab.php
    - app/Livewire/Settings/MailTab.php
    - app/Livewire/Settings/SecurityTab.php
    - resources/views/livewire/settings/profile-tab.blade.php
    - resources/views/livewire/settings/mail-tab.blade.php
    - resources/views/livewire/settings/security-tab.blade.php
    - app/Http/Requests/SettingsRequest.php
    - app/Models/User.php
    - app/Models/Setting.php
    - routes/web.php
  </files>
  <read_first>
    - app/Livewire/Settings/ProfileTab.php
    - app/Livewire/Settings/MailTab.php
    - app/Livewire/Settings/SecurityTab.php
    - resources/views/livewire/settings/profile-tab.blade.php
    - resources/views/livewire/settings/mail-tab.blade.php
    - resources/views/livewire/settings/security-tab.blade.php
    - app/Http/Requests/SettingsRequest.php
    - app/Models/User.php
    - app/Models/Setting.php
    - routes/web.php
  </read_first>
  <action>
    Build remaining Settings tabs per SET-01, SET-04, SET-05. Implement: (1) ProfileTab: name/email properties with #[Validate], mount() loads from auth()->user(), save() validates and updates User model, dispatches toast; (2) MailTab: page_size (10-100), default_folder (string), reply_behavior (reply|reply_all) with validation, mount() loads from Setting::getForUser(), save() persists via Setting::setForUser(); (3) SecurityTab: sessions collection loaded from DB sessions table (user_id + last_activity), revokeOtherSessions() calls LogoutController@revokeOtherSessions via route, shows current session indicator; (4) Views for each tab: profile-tab.blade.php (name/email inputs), mail-tab.blade.php (number input, folder select, radio group), security-tab.blade.php (session list with device/IP/last active, revoke button); (5) SettingsRequest: validation rules for all three tabs (name, email, page_size, default_folder, reply_behavior); (6) Extend User model: add signatures() HasMany, auditLogs() HasMany, setting($key, $default) accessor calling Setting::getForUser(); (7) Extend Setting model: add user_id to fillable, user() BelongsTo, static getForUser()/setForUser() with cache (key: setting:user:{id}:{key}, TTL 1hr); (8) Add POST /settings/sessions/revoke route in routes/web.php pointing to LogoutController@revokeOtherSessions.
  </action>
  <verify>
    <automated>php -r "require 'vendor/autoload.php'; $app = require 'bootstrap/app.php'; $app->make('Illuminate\Contracts\Console\Kernel')->bootstrap(); echo 'ProfileTab: ' . (class_exists('App\Livewire\Settings\ProfileTab') ? 'exists' : 'missing') . PHP_EOL; echo 'MailTab: ' . (class_exists('App\Livewire\Settings\MailTab') ? 'exists' : 'missing') . PHP_EOL; echo 'SecurityTab: ' . (class_exists('App\Livewire\Settings\SecurityTab') ? 'exists' : 'missing') . PHP_EOL; echo 'User::setting: ' . (method_exists('App\Models\User', 'setting') ? 'exists' : 'missing') . PHP_EOL; echo 'Setting::getForUser: ' . (method_exists('App\Models\Setting', 'getForUser') ? 'exists' : 'missing') . PHP_EOL;" && php artisan route:list | grep -c 'sessions/revoke'</automated>
    <fails_when>Any tab component missing, views missing, SettingsRequest missing rules, User::setting() missing, Setting::getForUser/setForUser missing, revoke route missing</fails_when>
  </verify>
  <done>
    - ProfileTab exists with name/email validation, mount, save
    - MailTab exists with page_size/default_folder/reply_behavior validation, mount, save
    - SecurityTab exists with sessions list, revokeOtherSessions action
    - All three tab views render correctly in Settings page
    - SettingsRequest validates all tab fields
    - User model has signatures(), auditLogs(), setting() accessor
    - Setting model has user_id, user(), getForUser(), setForUser() with cache
    - POST /settings/sessions/revoke route exists
  </done>
  <reversibility rating="reversible">Tab components additive; User/Setting model extensions backward compatible; cache invalidation on set</reversibility>
</task>

<task id="06-verify-settings" type="verify">
  <name>Verify all Settings tabs functional with persistence and session management</name>
  <files>
    - app/Livewire/Settings/ProfileTab.php
    - app/Livewire/Settings/MailTab.php
    - app/Livewire/Settings/SecurityTab.php
    - resources/views/livewire/settings/profile-tab.blade.php
    - resources/views/livewire/settings/mail-tab.blade.php
    - resources/views/livewire/settings/security-tab.blade.php
    - app/Models/User.php
    - app/Models/Setting.php
  </files>
  <read_first>
    - app/Livewire/Settings/ProfileTab.php
    - app/Livewire/Settings/MailTab.php
    - app/Livewire/Settings/SecurityTab.php
    - resources/views/livewire/settings/profile-tab.blade.php
    - resources/views/livewire/settings/mail-tab.blade.php
    - resources/views/livewire/settings/security-tab.blade.php
    - app/Models/User.php
    - app/Models/Setting.php
  </read_first>
  <action>
    Verify Settings tabs: (1) Profile: edit name/email → save → verify User updated, toast shown, persists on refresh; (2) Mail: change page size (e.g., 25→50) → save → verify message list pagination updates; change default folder → save → verify mailbox redirects to new default; change reply behavior → verify composer default changes; (3) Security: open in two browsers → verify both sessions listed with device/IP/last active; click "Revoke Other Sessions" → verify other browser logged out, toast shown, only current session remains; (4) Verify Setting cache: change theme in Appearance → verify mailbox updates immediately (cache invalidated); (5) Verify User::setting() accessor works in Blade (e.g., @json(auth()->user()->setting('theme'))).
  </action>
  <verify>
    <automated>php artisan test --filter=SettingsTabsTest 2>/dev/null || echo "Manual: test all three tabs, verify persistence, test session revocation across browsers"</automated>
    <fails_when>Profile/Mail/Security tab save not persisting, page size not affecting pagination, session revocation not working, Setting cache not invalidating</fails_when>
  </verify>
  <done>
    - Profile tab updates name/email with validation (email unique, name required)
    - Mail tab page_size persists and controls message list pagination
    - Mail tab default_folder persists and sets mailbox landing folder
    - Mail tab reply_behavior persists and sets composer default
    - Security tab lists all active sessions with identifiable info
    - Revoke Other Sessions logs out all other browsers/devices
    - Current session marked and cannot be revoked
    - Setting::getForUser()/setForUser() cache works and invalidates on change
    - User::setting() accessor works in Blade and Livewire
  </done>
</task>

<task id="06-integration" type="execute">
  <name>Wire mail preferences into mailbox and composer behavior</name>
  <files>
    - app/Livewire/Mailbox/MessageList.php
    - app/Livewire/Mailbox/Composer.php
    - app/Models/User.php
    - app/Models/Setting.php
  </files>
  <read_first>
    - app/Livewire/Mailbox/MessageList.php
    - app/Livewire/Mailbox/Composer.php
    - app/Models/User.php
    - app/Models/Setting.php
  </read_first>
  <action>
    Apply mail preferences: (1) In MessageList.php: use auth()->user()->setting('page_size', 25) for pagination perPage; (2) In mailbox route/controller: redirect to auth()->user()->setting('default_folder', 'INBOX') on initial load; (3) In Composer.php: default reply behavior from auth()->user()->setting('reply_behavior', 'reply') for reply vs reply-all default; (4) Ensure Setting cache invalidation triggers live updates (wire:navigate or broadcast).
  </action>
  <verify>
    <automated>php artisan test --filter=MailPreferencesTest 2>/dev/null || echo "Manual: change page size/default folder/reply behavior in Settings, verify mailbox/composer behavior"</automated>
    <fails_when>MessageList not using page_size setting, mailbox not redirecting to default_folder, composer not using reply_behavior</fails_when>
  </verify>
  <done>
    - MessageList pagination respects user's page_size setting
    - Mailbox initial load redirects to user's default_folder
    - Composer reply default respects reply_behavior setting
    - Changes in Settings apply immediately without full reload
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Settings tabs | Profile, mail, security preferences submitted |
| Setting model → Database | User-scoped preferences with cache |
| SecurityTab → Sessions table | Session revocation deletes other sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-21 | Tampering | Profile email change | medium | mitigate | Email validation + uniqueness check; current password not required for profile (separate from auth) |
| T-05-22 | Elevation | Session revocation | medium | mitigate | User can only revoke own sessions; current session protected; audit log entry |
| T-05-23 | Information Disclosure | Session list exposure | low | mitigate | Only shows user's own sessions; no sensitive data in session payload |
| T-05-24 | Denial of Service | Cache stampede on Setting | low | mitigate | Cache TTL 1hr; single-flight via remember(); cache key per user+setting |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Package legitimacy audit in RESEARCH.md shows all approved; no new installs this phase |
</threat_model>

<verification>
Overall phase checks:
- All 5 Settings tabs functional (Profile, Mail, Appearance, Security, Signatures)
- Profile: name/email editable with validation
- Mail: page size, default folder, reply behavior persist and apply
- Security: active sessions listed, revoke other sessions works
- Appearance: theme/density persist and apply (from Plan 03)
- Signatures: CRUD works, composer integration works (from Plan 05)
- User::setting() and Setting::getForUser()/setForUser() operational
</verification>

<success_criteria>
Complete Settings page with all 5 tabs; user preferences persisted and applied application-wide; session management allows revocation; User and Setting models support preference access patterns.
</success_criteria>

<output>
Create .planning/phases/05-security-polish/05-06-SUMMARY.md when done
</output>