---
phase: 05-security-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/create_audit_logs_table.php
  - app/Models/AuditLog.php
  - app/Services/AuditService.php
  - app/Http/Controllers/Auth/LoginController.php
  - app/Http/Controllers/Auth/LogoutController.php
  - app/Console/Commands/PruneAuditLogs.php
  - config/logging.php
autonomous: true
requirements:
  - SEC-05
user_setup: []
estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - Audit logs capture login success, login failure, lockout, and send actions
    - Each audit entry includes user_id, event_type, description, IP, user agent, metadata, timestamp
    - Audit logs are pruned after 90 days via scheduler
    - LoginController and LogoutController call AuditService explicitly
    - Security log channel exists for CSP violations and audit events
  artifacts:
    - database/migrations/create_audit_logs_table.php
    - app/Models/AuditLog.php
    - app/Services/AuditService.php
    - app/Http/Controllers/Auth/LoginController.php (modified)
    - app/Http/Controllers/Auth/LogoutController.php (modified)
    - app/Console/Commands/PruneAuditLogs.php
    - config/logging.php (modified)
  key_links:
    - AuditService → AuditLog model → audit_logs table
    - LoginController/LogoutController → AuditService convenience methods
    - PruneAuditLogs command → scheduler → 90-day retention
    - config/logging.php security channel → CSP violation logging
---

<objective>
Implement audit logging for authentication events and send actions with 90-day retention per D-05 through D-08 and SEC-05.

Purpose: Create tamper-evident audit trail of security-relevant events (login success/failure, lockout, send/reply/forward) for compliance and incident investigation.

Output: audit_logs table migration, AuditLog model, AuditService with explicit logging, controller integrations, prune command, and security log channel.
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-security-polish/05-CONTEXT.md
@.planning/phases/05-security-polish/05-RESEARCH.md
@.planning/phases/05-security-polish/05-PATTERNS.md
</context>

<tasks>

<task id="02-tracer" type="execute">
  <name>End-to-end audit logging — migration, model, service, controller integration, prune command</name>
  <files>
    - database/migrations/create_audit_logs_table.php
    - app/Models/AuditLog.php
    - app/Services/AuditService.php
    - app/Http/Controllers/Auth/LoginController.php
    - app/Http/Controllers/Auth/LogoutController.php
    - app/Console/Commands/PruneAuditLogs.php
    - config/logging.php
  </files>
  <read_first>
    - database/migrations/create_audit_logs_table.php
    - app/Models/AuditLog.php
    - app/Services/AuditService.php
    - app/Http/Controllers/Auth/LoginController.php
    - app/Http/Controllers/Auth/LogoutController.php
    - app/Console/Commands/PruneAuditLogs.php
    - config/logging.php
  </read_first>
  <action>
    Create audit logging vertical slice per D-05 through D-08. Implement: (1) create_audit_logs_table migration with id, user_id (nullable FK), event_type (indexed), description, ip, user_agent, metadata (JSON), created_at (indexed); composite indexes on [user_id, created_at] and [event_type, created_at] for prune/query performance; (2) AuditLog model with fillable fields, metadata cast to array, user relationship; (3) AuditService with static log() method and convenience methods loginSuccess(), loginFailed(), lockout(), send() per D-06/D-08; (4) Extend LoginController (Livewire LoginForm) to call AuditService::loginSuccess() on success, AuditService::loginFailed() on failure, AuditService::lockout() at 10+ attempts; (5) Extend LogoutController to call AuditService::log('auth.logout') and add revokeOtherSessions() for SET-05; (6) PruneAuditLogs command deleting entries older than 90 days; (7) Add 'security' log channel in config/logging.php using daily driver for CSP violations and audit events.
  </action>
  <verify>
    <automated>php artisan migrate --dry-run 2>&1 | grep -c audit_logs && php -r "require 'vendor/autoload.php'; $app = require 'bootstrap/app.php'; $app->make('Illuminate\Contracts\Console\Kernel')->bootstrap(); echo 'AuditLog model: ' . (class_exists('App\Models\AuditLog') ? 'exists' : 'missing') . PHP_EOL; echo 'AuditService: ' . (class_exists('App\Services\AuditService') ? 'exists' : 'missing') . PHP_EOL;"</automated>
    <fails_when>Migration missing, AuditLog model missing, AuditService missing convenience methods, LoginController/LogoutController not calling AuditService, prune command missing, security log channel missing</fails_when>
  </verify>
  <done>
    - create_audit_logs_table migration creates table with all required columns and indexes
    - AuditLog model has fillable, casts, user relationship
    - AuditService has log() + loginSuccess/Failed/lockout/send() methods
    - LoginController calls AuditService on success, failure, lockout
    - LogoutController calls AuditService on logout and has revokeOtherSessions()
    - PruneAuditLogs command deletes entries older than 90 days
    - config/logging.php has 'security' channel configured
  </done>
  <reversibility rating="reversible">Migration can be rolled back; service calls are local to controllers; prune job removable via scheduler</reversibility>
</task>

<task id="02-verify-audit" type="verify">
  <name>Verify audit logging captures all SEC-05 events correctly</name>
  <files>
    - database/migrations/create_audit_logs_table.php
    - app/Models/AuditLog.php
    - app/Services/AuditService.php
    - app/Http/Controllers/Auth/LoginController.php
    - app/Http/Controllers/Auth/LogoutController.php
    - app/Console/Commands/PruneAuditLogs.php
  </files>
  <read_first>
    - database/migrations/create_audit_logs_table.php
    - app/Models/AuditLog.php
    - app/Services/AuditService.php
    - app/Http/Controllers/Auth/LoginController.php
    - app/Http/Controllers/Auth/LogoutController.php
    - app/Console/Commands/PruneAuditLogs.php
  </read_first>
  <action>
    Verify audit logging: (1) Run migration and inspect audit_logs schema; (2) Simulate login success → verify audit_logs entry with event_type=auth.login.success; (3) Simulate login failure → verify auth.login.failed with reason in metadata; (4) Simulate 10+ failures → verify auth.lockout; (5) Simulate logout → verify auth.logout; (6) Simulate send via ComposerService → verify mail.send.send; (7) Run prune command → verify only >90 day entries deleted; (8) Check security log channel writes.
  </action>
  <verify>
    <automated>php artisan test --filter=AuditLogTest 2>/dev/null || echo "Test file not yet created - manual verification: php artisan migrate; php artisan tinker --execute=\"App\Services\AuditService::loginSuccess(); App\Models\AuditLog::latest()->first()->event_type\""</automated>
    <fails_when>Any SEC-05 event type not logged, migration fails, prune command deletes wrong entries, security channel not writing</fails_when>
  </verify>
  <done>
    - Migration creates audit_logs table with correct schema
    - Login success creates audit_logs row with event_type=auth.login.success
    - Login failure creates row with event_type=auth.login.failed and reason in metadata
    - Lockout at 10 attempts creates auth.lockout entry
    - Send/reply/forward creates mail.send.{action} entries
    - Logout creates auth.logout entry
    - Prune command removes only entries older than 90 days
    - Security log channel receives CSP violations and audit events
  </done>
</task>

<task id="02-scheduler" type="execute">
  <name>Register audit log prune command in scheduler</name>
  <files>
    - app/Console/Commands/PruneAuditLogs.php
    - app/Console/Kernel.php
  </files>
  <read_first>
    - app/Console/Commands/PruneAuditLogs.php
    - app/Console/Kernel.php
  </read_first>
  <action>
    Register PruneAuditLogs command in app/Console/Kernel.php schedule() method to run daily at 02:00. Verify command signature is 'audit:prune'. Add command to $commands array if not auto-discovered.
  </action>
  <verify>
    <automated>php artisan schedule:list | grep -c "audit:prune" && php artisan audit:prune --dry-run 2>&1 | head -5</automated>
    <fails_when>Command not scheduled, schedule:list doesn't show audit:prune, command fails to execute</fails_when>
  </verify>
  <done>
    - PruneAuditLogs command registered in Kernel.php schedule() running daily
    - Command signature is 'audit:prune'
    - php artisan schedule:list shows audit:prune scheduled
    - Command executes without error (dry-run)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller → AuditService | Auth controllers explicitly call audit service |
| AuditService → Database | Audit logs written to audit_logs table |
| Scheduler → Database | Prune command deletes old entries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering | Audit log integrity | high | mitigate | Append-only writes; no update/delete via application; DB-level constraints |
| T-05-06 | Information Disclosure | Audit log metadata | medium | mitigate | Metadata JSON excludes passwords, tokens; only emails, IPs, user agents |
| T-05-07 | Denial of Service | Audit log table growth | medium | mitigate | 90-day retention via daily prune; indexes for efficient deletion |
| T-05-08 | Repudiation | Missing audit events | high | mitigate | Explicit AuditService calls at every auth/send action; convenience methods prevent omission |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Package legitimacy audit in RESEARCH.md shows all approved; no new installs this phase |
</threat_model>

<verification>
Overall phase checks:
- All SEC-05 event types logged (login success, failure, lockout, send, reply, forward, logout)
- Audit entries contain required fields (user_id, event_type, description, ip, user_agent, metadata, created_at)
- 90-day prune job registered and functional
- Security log channel operational for CSP violations
</verification>

<success_criteria>
Audit logging captures all SEC-05 events with complete metadata; 90-day retention enforced; security log channel ready for CSP violations.
</success_criteria>

<output>
Create .planning/phases/05-security-polish/05-02-SUMMARY.md when done
</output>