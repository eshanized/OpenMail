---
phase: 04-organization-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_09_06_000001_create_labels_table.php
  - database/migrations/2026_09_06_000002_create_message_labels_table.php
  - database/migrations/2026_09_06_000003_create_contacts_table.php
  - database/migrations/2026_09_06_000004_create_contact_groups_table.php
  - database/migrations/2026_09_06_000005_create_contact_group_contact_table.php
  - database/migrations/2026_09_06_000006_add_scout_columns_to_message_metadata.php
  - app/Models/Label.php
  - app/Models/Contact.php
  - app/Models/ContactGroup.php
  - app/Services/ThreadBuilder.php
  - app/Livewire/Mailbox/MessageList.php
  - resources/views/livewire/mailbox/thread-row.blade.php
  - resources/views/livewire/mailbox/message-list.blade.php
  - tests/Unit/ThreadBuilderTest.php
  - tests/Feature/ThreadUITest.php
autonomous: true
requirements: [THR-01, THR-02, THR-03, THR-04, THR-05]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Thread tree builds from Message-ID/In-Reply-To/References headers using JWZ algorithm"
    - "Subject normalization fallback groups orphaned messages by normalized subject ±2 day window"
    - "Thread rows render in message list with expand/collapse via Alpine.js"
    - "Thread ordering sorts by latest message date descending"
    - "Database schema supports labels, contacts, groups, and Scout searchable MessageMetadata"
  artifacts:
    - "database/migrations/2026_09_06_000001_create_labels_table.php"
    - "database/migrations/2026_09_06_000002_create_message_labels_table.php"
    - "database/migrations/2026_09_06_000003_create_contacts_table.php"
    - "database/migrations/2026_09_06_000004_create_contact_groups_table.php"
    - "database/migrations/2026_09_06_000005_create_contact_group_contact_table.php"
    - "database/migrations/2026_09_06_000006_add_scout_columns_to_message_metadata.php"
    - "app/Services/ThreadBuilder.php"
    - "resources/views/livewire/mailbox/thread-row.blade.php"
  key_links:
    - "MessageMetadata → ThreadBuilder (headers fetched via ImapMailboxService)"
    - "ThreadBuilder → MessageList Livewire (thread tree passed to view)"
    - "thread-row.blade.php → Alpine.js expansion state (client-side only per D-03)"
---

<objective>
Establish the foundational database schema for Phase 4 features and implement the core threading engine (JWZ algorithm) with a working thread view in the message list. This is the tracer plan — it wires one complete path from IMAP header fetch through thread reconstruction to rendered thread rows with expand/collapse.
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-organization-intelligence/04-CONTEXT.md
@.planning/phases/04-organization-intelligence/04-RESEARCH.md
@.planning/phases/04-organization-intelligence/04-UI-SPEC.md
@.planning/phases/02-mailbox-core/02-CONTEXT.md
@.planning/phases/03-compose-send/03-CONTEXT.md
@app/Services/ImapMailboxService.php
@app/Models/MessageMetadata.php
@app/Livewire/Mailbox/MessageList.php
@resources/views/layouts/mailbox.blade.php
@config/openmail.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migrations for Phase 4 core tables</name>
  <files>
    database/migrations/2026_09_06_000001_create_labels_table.php
    database/migrations/2026_09_06_000002_create_message_labels_table.php
    database/migrations/2026_09_06_000003_create_contacts_table.php
    database/migrations/2026_09_06_000004_create_contact_groups_table.php
    database/migrations/2026_09_06_000005_create_contact_group_contact_table.php
    database/migrations/2026_09_06_000006_add_scout_columns_to_message_metadata.php
  </files>
  <action>
    Create six migrations per D-10, D-13, D-05. All tables user-scoped (user_id foreign key to users table).

    1. labels: id, user_id, name, color, created_at. Unique index on (user_id, name). Color stores Tailwind class (e.g., bg-blue-500).
    2. message_labels: message_metadata_id, label_id. Composite PK. Index on (user_id, label_id, message_metadata_id) for filter performance per Pitfall 5.
    3. contacts: id, user_id, name, email, phone, notes, avatar_color, usage_count, created_at, updated_at. Unique index on (user_id, email). avatar_color from deterministic hash.
    4. contact_groups: id, user_id, name, color, created_at. Unique on (user_id, name).
    5. contact_group_contact: contact_id, contact_group_id. Composite PK.
    6. Add Scout columns to message_metadata: body_text (TEXT, nullable), searchable_as (string for per-user index isolation). Add FULLTEXT index on (subject, from_address, from_name, to_address, snippet, body_text).

    Run migrations after creation.
  </action>
  <verify>
    <automated>cd /home/snigdha/Desktop/OpenMail && php artisan migrate --force 2>&1 | tail -10</automated>
    <fails_when>non-zero exit, or "Migration table not found" in output, or any migration shows "Failed"</fails_when>
  </verify>
  <done>
    All 6 migrations run successfully. Tables exist with correct indexes. FULLTEXT index created on message_metadata searchable columns.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ThreadBuilder service with JWZ algorithm</name>
  <files>
    app/Services/ThreadBuilder.php
    tests/Unit/ThreadBuilderTest.php
  </files>
  <behavior>
    - Test 1: buildThreads() returns root threads with nested children from Message-ID/In-Reply-To/References (THR-01, THR-02)
    - Test 2: extractParentId() prefers In-Reply-To, falls back to last References entry (THR-02)
    - Test 3: groupBySubject() strips Re:/Fwd:, normalizes whitespace, groups by subject + 2-day window (THR-03, D-02)
    - Test 4: Thread ordering by latest message date descending (THR-05)
    - Test 5: Handles missing parent messages (dummy containers pruned per JWZ step 3)
    - Test 6: Handles circular references gracefully (no infinite loop)
    - Test 7: Empty input returns empty array
  </behavior>
  <action>
    Implement ThreadBuilder per RESEARCH.md Pattern 1 (JWZ algorithm). Key methods: buildThreads(Collection $messages), extractParentId(), cleanMessageId(), groupBySubject(), attachChildren(), getLatestDate(), countUnread(). 

    Input: Collection of MessageMetadata objects with message_id, in_reply_to, references, subject, date, is_seen, uid, folder_path, has_attachments, labels relation loaded.

    Per D-01: Only fetches headers for visible page + thread roots (caller responsibility). Service is pure algorithm — no IMAP calls.

    Per D-02: Subject fallback strips "Re:", "Fwd:" (case-insensitive), trims whitespace, groups by lowercase subject + date window.

    Per D-03: Returns plain arrays/objects (not Livewire components) for Alpine.js consumption. Each thread node gets: children[], latestDate, unreadCount, from_display, subject, formatted_date, has_attachments, labels.

    Write tests FIRST (RED), then implement (GREEN), then refactor if needed.
  </action>
  <verify>
    <automated>cd /home/snigdha/Desktop/OpenMail && ./vendor/bin/pest tests/Unit/ThreadBuilderTest.php 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or "0 passed" in summary, or any test shows "FAIL"</fails_when>
  </verify>
  <done>
    ThreadBuilder class exists with all methods. All 7 unit tests pass. Code coverage >90% on ThreadBuilder.
  </done>
  <reversibility>
    rating: reversible
    rationale: Pure algorithm service; changing threading logic only affects thread view rendering
  </reversibility>
</task>

<task type="tracer">
  <name>Task 3: Thread view integration in MessageList (tracer — one folder end-to-end)</name>
  <files>
    app/Models/MessageMetadata.php
    app/Livewire/Mailbox/MessageList.php
    resources/views/livewire/mailbox/thread-row.blade.php
    resources/views/livewire/mailbox/message-list.blade.php
    tests/Feature/ThreadUITest.php
  </files>
  <action>
    Wire the tracer: one folder (Inbox) renders threaded view end-to-end.

    1. Extend MessageMetadata: add `labels()` belongsToMany, `children` accessor for thread tree, `latestDate`, `unreadCount`, `from_display`, `formatted_date` accessors. Add `thread_id` virtual attribute for Alpine.js tracking.

    2. In MessageList.php: Add `threadMode` property (default from localStorage per folder, default 'threaded' for Inbox per D-18). In `mount()`, load thread mode from session/localStorage. In `getMessagesProperty()`: when threadMode === 'threaded', fetch message headers for current page + thread roots via ImapMailboxService, pass to ThreadBuilder, return thread roots. When 'flat', return existing flat messages.

    3. Create thread-row.blade.php per UI-SPEC §2.1-2.2: 56px height, checkbox, star, sender+unread badge, subject+label chips (max 3 + overflow), date, attachment icon, chevron. Alpine.js x-data for expanded state, x-transition for smooth expand/collapse (150ms per UI-SPEC). Click row (except interactive elements) toggles expansion. Keyboard: Space/Enter toggles, Arrow Right expands, Arrow Left collapses.

    4. Update message-list.blade.php: conditional render — if threadMode === 'threaded', loop thread roots with `<x-mailbox.thread-row>`, else loop flat messages with existing message-row. Add thread toggle button in toolbar per D-18 (icon: stacked chat bubbles ↔ single bubble).

    5. Write feature test: ThreadUITest::testExpandCollapse — visits mailbox, verifies thread rows render, clicks expand, verifies children visible, clicks collapse, verifies children hidden.
  </action>
  <verify>
    <automated>cd /home/snigdha/Desktop/OpenMail && ./vendor/bin/pest tests/Feature/ThreadUITest.php::testExpandCollapse 2>&1 | tail -15</automated>
    <fails_when>non-zero exit, or "0 passed" in summary, or test shows "FAIL"</fails_when>
  </verify>
  <done>
    Inbox folder renders threaded view with expandable/collapsible thread rows. Thread toggle button appears in toolbar. localStorage persists mode per folder. Alpine.js transitions work smoothly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IMAP Server → Backend | Message headers (Message-ID, In-Reply-To, References, Subject) fetched via IMAP; untrusted input |
| User Input → Search/Thread | Search queries, label names, contact data — sanitized at service layer |
| Client → Backend (Livewire) | Thread expansion state, label actions, contact CRUD — validated server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Spoofing | ThreadBuilder | low | accept | Message-ID headers from IMAP server; server trusted per deployment model |
| T-04-02 | Tampering | Message-ID/References headers | medium | mitigate | ThreadBuilder validates Message-ID format (cleanMessageId strips <>); malformed headers ignored |
| T-04-03 | Info Disclosure | Thread view cross-folder | low | mitigate | ThreadBuilder only processes messages user has access to (user_id scoped); no cross-user leakage |
| T-04-04 | DoS | Large thread reconstruction | medium | mitigate | D-01 limits to visible page + thread roots; ThreadBuilder has max depth guard (50 levels) |
| T-04-05 | Elevation | Label/contact CRUD | medium | mitigate | All operations scoped to authenticated user_id; policies enforce ownership |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | Package legitimacy gate in RESEARCH.md; all packages [OK] verdict |
</threat_model>

<verification>
Run `./vendor/bin/pest tests/Unit/ThreadBuilderTest.php tests/Feature/ThreadUITest.php::testExpandCollapse` — all tests must pass.
Run `php artisan migrate --force` — migrations must apply cleanly.
</verification>

<success_criteria>
- Database schema for labels, contacts, groups, pivots, and Scout columns applied
- ThreadBuilder JWZ algorithm passes all unit tests (THR-01, THR-02, THR-03, THR-05)
- MessageList renders threaded view for Inbox with working expand/collapse (THR-04)
- Thread toggle button in toolbar switches between threaded/flat modes
- Thread mode persists per folder in localStorage (D-18)
</success_criteria>

<output>
Create .planning/phases/04-organization-intelligence/04-01-SUMMARY.md when done
</output>