---
phase: 06-6-ui-ux-polish
plan: 02
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - package.json
  - resources/js/app.js
  - resources/css/app.css
  - resources/views/layouts/app.blade.php
  - resources/views/livewire/mailbox/message-list.blade.php
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-09, D-12, D-13, D-14, D-15, D-16]
user_setup: []

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - The app's font is Plus Jakarta Sans Variable, self-hosted through Vite: the compiled manifest contains the fontsource css entry and the build gate script exits 0 (D-13)
    - Light surfaces render warm cream and dark surfaces soft-dark via one semantic surface token with a runtime .dark override; ink/gray text meets AA on both surfaces (D-02, D-03, D-04 contrast-adjusted stops)
    - The Compose primary action carries the gradient bg-linear-to-r from-blue-600 to-purple-600 with glow shadow, rounded-lg, hover scale 1.02, and focus-visible ring (D-01, D-04, D-05, D-10)
    - Every SPA navigation shows the route overlay fade cross and the page region fade replays on livewire:navigated (D-12, D-09)
    - The x-cloak flash bug is dead: the hidden-until-Alpine rule exists in app.css and the six views using x-cloak can no longer flash visible (research Pitfall 5)
    - Density: base font size stays 0.875rem (D-14), regular density line-height is 1.4 (D-16), and all three density modes still render (D-15)
    - CSP posture unchanged: config/csp.php untouched, no new inline script, body/layout still emit the csp-nonce meta (T-06-01, T-06-04)
  artifacts:
    - package.json (+ @fontsource-variable/plus-jakarta-sans dependency)
    - resources/js/app.js (font import + route-overlay + fade-replay hooks)
    - resources/css/app.css (@theme tokens incl. keyframes, x-cloak rule, density 1.4, surface/brand tokens, reduced-motion guard)
    - resources/views/layouts/app.blade.php (semantic surface body, #app-main fade region, #route-overlay div)
    - resources/views/livewire/mailbox/message-list.blade.php (gradient Compose button)
  key_links:
    - app.js hooks fire on livewire:navigating/navigated (Livewire 4 event names) — both the overlay and the fade replay depend on these two events firing on initial load and every swap
    - The runtime .dark override of --color-surface must retarget generated utilities (research Assumption A3, MEDIUM) — verified with a dark-toggle check; if utilities do not retarget, the fallback is explicit dark: variants (documented)
    - package.json lockfile change is the single source of truth that public/build regeneration is required
---

<objective>
The phase tracer: one end-to-end theming slice through every layer the phase touches — dependency (font package), JS bundle (font import + navigation hooks), CSS token system (surfaces, brand, keyframes, x-cloak, density), layout (semantic surface, fade region, route overlay), and one production component (gradient Compose button). Proven by the wave-0 build gate and two UiPolishTest methods before any expansion task starts.

Purpose: Prove the CSS-first theming architecture (tokens + runtime dark override + keyframes-in-theme + bundled hooks) end-to-end on the agent's best early-context tokens. If the token retargeting or the Livewire 4 event wiring is architecturally wrong, it fails here after one commit, not after ten restyled views.
Output: A committed, production-quality foundation the remaining four plans build on — same file types, same patterns (06-03/06-04/06-05 reuse tokens, overlay, guard rules).

Decision reconciliations (traceability, from research Open Questions):
- D-04: locked stops #3b82f6→#8b5cf6 fail WCAG AA for 14px white text (3.68/4.23 vs 4.5). The agent's discretion explicitly covers stops and contrast — implemented as from-blue-600 (#2563eb) to-purple-600 (#9333ea), passing 5.17/5.38. The locked 500-level stops are reserved exclusively for large decorative fills (the blob layer in plan 06-03) where white contrast is not required.
- D-12: true DOM cross-fade is unsupported by Livewire 4's synchronous swap; implemented as a theme-aware opaque route overlay fading in on livewire:navigating and out on livewire:navigated (visually identical to the locked intent, deterministic timing).
- D-16: .density-regular moves from 1.5 to 1.4 (both the class block and the @utility duplicate); .density-comfortable keeps 1.6 so comfortable remains the relaxed option. The prior 1.5 expectation is knowingly updated.
- D-13: variable package registers family "Plus Jakarta Sans Variable" (DevTools-only suffix; visually identical; zero markup changes needed if swapped to the static package later).
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-6-ui-ux-polish/06-CONTEXT.md
@.planning/phases/06-6-ui-ux-polish/06-RESEARCH.md
@.planning/phases/06-6-ui-ux-polish/06-UI-SPEC.md
@.planning/phases/06-6-ui-ux-polish/06-VALIDATION.md
@.planning/phases/05-security-polish/05-03-SUMMARY.md
@resources/css/app.css
@resources/js/app.js
@resources/views/layouts/app.blade.php
@resources/views/livewire/mailbox/message-list.blade.php
@scripts/check-fontsource-build.js
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>End-to-end theming slice — font, tokens, x-cloak, density 1.4, route overlay fade, gradient Compose (one path)</name>
  <files>package.json, resources/js/app.js, resources/css/app.css, resources/views/layouts/app.blade.php, resources/views/livewire/mailbox/message-list.blade.php</files>
  <read_first>
    All patterns verified in RESEARCH.md: Pattern 1 (token architecture — @theme semantic tokens + :where(.dark) runtime overrides + keyframes nested inside @theme), Pattern 3 (route overlay on livewire:navigating/navigated + fade replay via forced reflow on the app-main region), Code Example 1 (JS-side fontsource import in resources/js/app.js — the research-guaranteed path per Pitfall 7), Code Example 2 (gradient button), Pitfall 5 (the single x-cloak rule fix), Open Questions 1-5 (decision reconciliations in the objective). Density blocks to change: the .density-regular class block AND the duplicate @utility density-regular duplicate (line-height 1.5 -> 1.4). Current app.css token block: @theme at top sets --font-sans with Instrument Sans (D-13 replaces it); the density @theme (lines 180-184) already carries --font-size-base: 0.875rem (D-14 satisfied — keep).
  </read_first>
  <action>
    Install the audited font dependency (per D-13). Run npm install with the exact audited package name from the research Package Legitimacy Audit — the only new dependency this phase; adding any other package requires stopping and reporting.
    Then wire the slice end-to-end (D-13, D-02, D-03, D-04, D-09, D-12, D-14, D-16):
    (1) resources/js/app.js — add the fontsource CSS import as the first import statement (before the bootstrap import so Vite emits it deterministically). Append the route-overlay hook: a small handler pair on the livewire:navigating and livewire:navigated document events that toggles an opacity utility class on the route-overlay element (D-12), plus a navigated handler that replays the fade-in animation on the app-main region by removing the animation class, forcing a reflow, and re-adding it (D-09 — works on initial load too because livewire:navigated fires there). All in bundled app.js — no inline script anywhere (T-06-04).
    (2) resources/css/app.css — replace the font family token with the Plus Jakarta Sans Variable stack (D-13). Extend the theme block with semantic tokens: --color-surface warm cream #fafaf9 (D-02), --color-surface-raised, --color-ink; brand stops --color-primary #2563eb and --color-accent #9333ea (the AA-passing implementation of D-04), plus the locked 500-level --color-primary-deep and --color-accent-deep reserved for decorative-large only; effects tokens --shadow-glow and --shadow-glow-strong (D-05/D-10); animation tokens --animate-fade-in (0.18s ease-out) and --animate-shimmer with their nested keyframes fade-in and shimmer (D-09, D-11 foundation). Add the route overlay background custom properties --route-overlay-bg on :root (light) and inside a :where(.dark) block (dark) (D-03/D-12). Add the missing hidden rule for the x-cloak attribute selector (research Pitfall 5 — the exact selector, applied to the digest region of app.css only). Change the .density-regular class block AND the duplicate density-regular utility block line-height to 1.4 (D-16); leave compact (already 1.4) and comfortable (1.6) untouched; do not alter --font-size-base (D-14 already 0.875rem); do not touch --spacing-unit values (D-15 — spacing review happens in plan 06-05).
    (3) resources/views/layouts/app.blade.php — swap the body surface classes to the semantic surface token so light cream and dark soft-dark come from one token (D-02/D-03); add id="app-main" to the main content region and the animate-fade-in class to it (D-09); insert the route overlay div as the first element inside body, before the nav — fixed, pointer-events-none, invisible by default, high z-index, background from the route-overlay custom property, aria-hidden (D-12). Keep the CSP nonce meta tag, nonced inline theme initializer, and @vite tags exactly as they are (T-06-01) — the inline initializer is pre-existing Phase 5 work, not a new inline script.
    (4) resources/views/livewire/mailbox/message-list.blade.php — restyle ONLY the Compose button (the one primary action in this view): gradient fill from-blue-600 to-purple-600 with the rounded-lg shape, glow shadow tokens, hover scale 1.02 plus stronger glow, focus-visible ring with offset (WCAG 2.4.7), and motion-reduce transform/transition suppression (D-01, D-04, D-05, D-10). Use only static utility classes — zero interpolation of user input into class lists (V5/T-06-02). This is the ONLY view edit in this plan; every other component restyle belongs to plans 06-03/06-04.
    (5) Rebuild and run the wave-0 gate: npm run build must succeed and node scripts/check-fontsource-build.js must now exit 0 (font css in manifest — Pitfall 7's signal).
    (6) Verify the runtime dark token retargeting (research Assumption A3): after build, confirm the compiled css contains both surface values, and in the browser dev check toggle dark mode on the mailbox page and confirm the background flips to the soft-dark value with no extra dark: utility needed on the body. If utilities do not retarget (token override loses to use-site resolution), add explicit dark: variants to the body/surface usage and document the fallback in the SUMMARY — do not fight the framework.
  </action>
  <verify>
    <automated>npm install @fontsource-variable/plus-jakarta-sans && npm run build && node scripts/check-fontsource-build.js && php artisan test --filter='UiPolishTest::(test_compose_button_has_gradient_classes|test_x_cloak_css_rule_exists)' && php artisan test --filter=AppearanceTest && rg -l "route-overlay" public/build/assets/*.js</automated>
    <fails_when>npm install or npm run build exits non-zero; the build gate exits non-zero (font not in manifest per D-13); either UiPolishTest method reports failure; AppearanceTest reports failure (density/theme persistence broken by the density edit); the route-overlay grep finds no bundled JS asset containing the overlay id</fails_when>
  </verify>
  <acceptance_criteria>
    Build green, gate exit 0 (fontsource css in manifest), the two pinned UiPolishTest methods green (live-rendered gradient + app.css x-cloak rule), AppearanceTest green (three density modes render, theme/density persist), route-overlay hook present in a built asset, reduced-motion guard present in app.css, CSP nonce meta still emitted (covered by UiPolishTest method 7 in plan 06-02's final wave gate).
  </acceptance_criteria>
  <done>
    A user loading the mailbox sees Plus Jakarta Sans text on cream (light) / soft-dark (dark), a gradient Compose button with glow + hover scale + focus ring, no x-cloak flash on the folder/label panels, a fade-in page region, and a route-overlay fade on every wire:navigate — all verified by the gate + two green test methods + build asset greps above.
  </done>
  <reversibility rating="reversible">Token/class changes are local CSS edits per CONTEXT.md reversibility notes for D-01..D-16.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Reduced-motion global guard (WCAG 2.3.3) + wave verification sweep</name>
  <files>resources/css/app.css</files>
  <read_first>
    Code Example 5 in RESEARCH.md: the global prefers-reduced-motion media block (animation-duration/transition-duration near-zero, single iteration, auto scroll-behavior) and the route-overlay display none inside it. Research Pitfall 6 (never animate backdrop-filter — opacity/transform only) informs why motion-reduce already on the Compose button covers its scale transform.
  </read_first>
  <action>
    Extend app.css (digest region addition, per D-09/D-10/D-12 a11y completeness — the reduced-motion guard is the accessibility counterpart to the fade/scale animations): add a global prefers-reduced-motion media block that collapses animation and transition durations to near-zero for all elements and hides the route overlay element entirely so the D-12 cross-fade never runs for motion-sensitive users (WCAG 2.3.3 intent per research Code Example 5). Keep the block static — no user input, no inline styles. Then run the wave verification sweep for the whole plan: full build, build gate, and the complete UiPolishTest/AppearanceTest sweep. This task adds the second reduced-motion occurrence, so the count check below is only valid after this task completes — task 1's verifier intentionally does not count occurrences.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-fontsource-build.js && php artisan test --filter=AppearanceTest && php artisan test --filter=UiPolishTest && rg -c "prefers-reduced-motion" resources/css/app.css | rg -q '^2$' && rg -l "prefers-reduced-motion" public/build/assets/*.css && git diff --stat config/csp.php</automated>
    <fails_when>build or gate non-zero; AppearanceTest or UiPolishTest fails (UiPolishTest methods 5-7 — soft-tag badge, message-viewer chrome guard, CSP nonce — must be green here; methods 1-4 must now show compose-gradient and x-cloak green with glass/send-archive still RED by design); app.css does not contain exactly two reduced-motion guard occurrences; no built css asset contains the guard; or git diff shows config/csp.php modified (CSP policy must remain untouched per T-06-01)</fails_when>
  </verify>
  <acceptance_criteria>
    prefers-reduced-motion appears exactly twice in app.css (the transition-fastener comment block is not counted — the count targets the two real declaration blocks); every built css asset carries the guard; csp.php has zero diff.
  </acceptance_criteria>
  <done>
    The motion guard is committed in app.css and the wave-1 sweep is green under the exact signal contract in the verifier: gate 0, AppearanceTest green, UiPolishTest at its expected 3-green/2-partially-green state, csp.php untouched.
  </done>
  <reversibility rating="reversible">CSS-only; removable without architecture impact.</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces

| Artifact | Type | Consumed By |
|----------|------|-------------|
| `package.json` — @fontsource-variable/plus-jakarta-sans dependency (+ lockfile) | dependency | all later plans run npm run build against the new manifest |
| `resources/js/app.js` — fontsource import, route-overlay hook (livewire:navigating/navigated), fade replay on app-main | JS | D-12/D-09 runtime behavior; plan 06-05 reuses the same bundle for Alpine.data registration |
| `resources/css/app.css` — tokens (surface/ink/brand/effects/animation), nested keyframes, x-cloak rule, density-regular 1.4, route-overlay-bg, reduced-motion guard | CSS | plan 06-03 (glass utilities, blob layer, badge tints), plan 06-04 (animate-shimmer skeletons), plan 06-05 (appearance pickers) all consume these tokens |
| `resources/views/layouts/app.blade.php` — semantic surface body, #app-main fade region, #route-overlay div | layout | route cross-fade + page fade surfaces; plan 06-03 adds the decorative blob layer here (same file, later wave) |
| `resources/views/livewire/mailbox/message-list.blade.php` — gradient Compose button | view | proves the gradient pattern; plan 06-03 copies it to Send/Archive; plan 06-04 rewrites the loading region in the same view |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → DOM (route overlay, fade replay) | Document-level JS hooks mutate class lists on Livewire's navigation events; overlay/region markup is static server-rendered |
| compiled CSS → browser | Vite-emitted css; token values are compile-time constants, never runtime input |
| dependency registry → build | the one new npm package crosses into the production bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | config/csp.php + font delivery | high | mitigate | Self-hosted @fontsource only — zero external requests, zero style-src/font-src relaxation; task 2 verifier asserts csp.php has zero diff |
| T-06-02 | Tampering | class/color interpolation (V5) | high | mitigate | All new gradient/overlay markup uses static class constants; the Compose restyle adds zero dynamic class interpolation; overlay and hook are hard-coded ids/classes |
| T-06-03 | Tampering | email HTML reusing app chrome classes | high | mitigate | message-viewer render container untouched this plan (not in files_modified); UiPolishTest method 6 keeps the negative chrome-class guard green |
| T-06-04 | Info disclosure | route overlay / JS outside CSP scope | medium | mitigate | Overlay is a static div; all hooks live in Vite-bundled app.js (nonce-integrated); zero new inline script; UiPolishTest method 7 guards the nonce meta on the restyled layout |
| T-06-05 | Tampering | focus/contrast/reduced-motion regressions | medium | mitigate | focus-visible ring on the gradient button, 600-level AA-passing stops, reduced-motion guard (task 2); manual axe pass deferred to plan 06-05 |
| T-06-SC | Tampering | npm install (fontsource) | high | mitigate | RESEARCH.md audit: [OK]/Approved, no postinstall scripts; install pinned to the audited version spec — no human checkpoint needed |
</threat_model>

<verification>
- `npm run build` green; `node scripts/check-fontsource-build.js` exit 0 (D-13 signal).
- `php artisan test --filter='UiPolishTest::(test_compose_button_has_gradient_classes|test_x_cloak_css_rule_exists)'` green (live render + source rule).
- `php artisan test --filter=AppearanceTest` green (D-15/D-16 modes + persistence survive).
- `rg -l "route-overlay" public/build/assets/*.js` non-empty (hook bundled).
- `rg -c "prefers-reduced-motion" resources/css/app.css` == 2 (after task 2).
- `git diff --stat config/csp.php` empty (CSP immutable).
</verification>

<success_criteria>
- The tracer slice is committed and proven: font in manifest, tokens compiled, x-cloak fixed, density 1.4, overlay + fade wired, Compose gradient live.
- UiPolishTest state after this plan: methods 1 (compose gradient) and 3 (x-cloak) GREEN; 2 (send/archive), 4 (glass) still RED by design; 5-7 GREEN.
- SUMMARY records the dark-token retargeting check result (native override vs explicit dark: variant fallback) — the A3 assumption resolution.
</success_criteria>

<output>
Create `.planning/phases/06-6-ui-ux-polish/06-02-SUMMARY.md` when done
</output>