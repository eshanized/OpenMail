---
phase: 04-organization-intelligence
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/Models/Label.php
  - app/Services/LabelService.php
  - tests/Feature/LabelTest.php
autonomous: true
requirements: [LBL-01, LBL-02, LBL-03, LBL-04, LBL-05]
user_setup: []

estimate:
  tokens: 20000
  raw_tokens: 10000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - "Labels stored locally in DB (labels + message_labels pivot), not synced to IMAP (D-13)"
    - "User can create, edit, delete labels with 10-color palette (D-15, D-16)"
    - "Label filter scope: MessageMetadata::whereHas('labels', fn(q) => q->where('labels.id', $labelId))"
    - "Archive action: IMAP MOVE to Archive folder + remove Inbox label + add Archive label"
  artifacts:
    - "app/Models/Label.php"
    - "app/Services/LabelService.php"
  key_links:
    - "LabelService → Label model (CRUD) → LabelSidebar (Plan 8)"
    - "MessageMetadata.labels() → Label chips in message list/thread rows (Plan 6, 8)"
    - "Label filter → MessageList query scope (whereHas labels)"
    - "Archive action → ImapMailboxService.moveMessage() + LabelService sync + undo toast (Plan 8)"

---

<objective>
Implement the Label model and LabelService with full CRUD, message labeling, label counts, and Archive action support. Labels are local-only (D-13) with 10-color palette from UI-SPEC.
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-organization-intelligence/04-CONTEXT.md
@.planning/phases/04-organization-intelligence/04-RESEARCH.md
@.planning/phases/04-organization-intelligence/04-UI-SPEC.md
@.planning/phases/02-mailbox-core/02-CONTEXT.md
@app/Services/ImapMailboxService.php
@app/Models/MessageMetadata.php
@app/Models/Label.php
@app/Livewire/Mailbox/MessageList.php
@config/openmail.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Label model + LabelService CRUD + message labeling + Archive support</name>
  <files>
    app/Models/Label.php
    app/Services/LabelService.php
    tests/Feature/LabelTest.php
  </files>
  <behavior>
    - Test 1: LabelService.create() validates name unique per user (case-insensitive), color from 10-color palette
    - Test 2: LabelService.update() validates name unique (excluding self), color change
    - Test 3: LabelService.delete() removes label from all messages (cascade), deletes label
    - Test 4: LabelService.applyToMessages(labelId, messageMetadataIds) attaches label to messages
    - Test 5: LabelService.removeFromMessages(labelId, messageMetadataIds) detaches label
    - Test 6: LabelService.getForUser() returns labels with unread_count + total_count per label
    - Test 7: Label filter scope: MessageMetadata::whereHas('labels', fn(q) => q->where('labels.id', $labelId))
    - Test 8: Label color palette: 10 predefined colors from UI-SPEC (Blue, Green, Red, Yellow, Purple, Pink, Orange, Teal, Indigo, Gray)
    - Test 9: ensureArchiveLabel() finds/creates "Archive" label with Gray color
    - Test 10: ensureInboxLabel() finds/creates "Inbox" label with Blue color
  </behavior>
  <action>
    1. Label model (migration created in Plan 1): fillable [name, color]. Relationships: messages() belongsToMany MessageMetadata, user() belongsTo. Scope: forUser(userId). Pivot table message_labels has user_id via message_metadata relationship.

    2. LabelService per D-13, D-15, D-16:
       - create(userId, name, color): validates name max 50 chars, unique per user (case-insensitive), color must be from palette
       - update(labelId, userId, name?, color?): same validations
       - delete(labelId, userId): detaches from all messages (cascade on pivot), deletes label
       - applyToMessages(labelId, messageIds): syncWithoutDetaching on pivot
       - removeFromMessages(labelId, messageIds): detach from pivot
       - getForUser(userId): returns labels with withCount(['messages as unread_count' => where is_seen=false], ['messages as total_count'])
       - getPalette(): returns 10-color array from UI-SPEC §Label Color Palette (Blue #2563EB, Green #16A34A, Red #DC2626, Yellow #CA8A04, Purple #9333EA, Pink #DB2777, Orange #EA580C, Teal #0D9488, Indigo #4F46E5, Gray #6B7280)
       - ensureArchiveLabel(userId): finds label named "Archive" (case-insensitive) or creates it with Gray color (#6B7280)
       - ensureInboxLabel(userId): finds label named "Inbox" or creates with Blue color (#2563EB)

    3. Add query scopes to MessageMetadata: scopeWithLabel($query, $labelId) and scopeWithoutLabel($query, $labelId).

    4. Write tests FIRST (RED), then implement (GREEN). Tests cover CRUD, apply/remove, counts, palette, filter scope, Archive/Inbox label helpers.
  </action>
  <verify>
    <automated>cd /home/snigdha/Desktop/OpenMail && ./vendor/bin/pest tests/Feature/LabelTest.php::testCrudAndApply 2>&1 | tail -15</automated>
    <fails_when>non-zero exit, or "0 passed" in summary, or test shows "FAIL"</fails_when>
  </verify>
  <done>
    LabelService CRUD works with validation. Labels applied/removed from messages. Label counts (unread/total) accurate. Filter scope works. Palette returns 10 colors. Archive/Inbox label helpers work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Label Actions → Backend | Create/edit/delete labels, apply/remove from messages; validated server-side |
| Archive Action → IMAP Server | IMAP MOVE command to Archive folder; folder path from FolderMapper heuristic |
| Label Filter → Message List | Label ID from URL/click; verified owned by user before filtering |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-16 | Tampering | Label apply/remove cross-user | medium | mitigate | LabelService scopes all operations to authenticated user_id; pivot table has user_id via message_metadata |
| T-04-17 | DoS | Archive moves large selection | low | mitigate | Bulk archive uses IMAP UID SET (single command); limit selection to 100 messages |
| T-04-18 | Info Disclosure | Label counts cross-folder | low | mitigate | LabelService.getForUser() counts only user's messages; no cross-folder leakage |
| T-04-19 | Spoofing | Fake Archive folder creation | low | accept | FolderMapper heuristic for \Archive SPECIAL-USE; user can configure in settings later |
| T-04-20 | Repudiation | Archive undo not audited | low | accept | Phase 5 adds audit logging; v1 accepts no audit for undo |
</threat_model>

<verification>
Run `./vendor/bin/pest tests/Feature/LabelTest.php` — all tests must pass.
Verify Archive folder detection: `php artisan tinker --execute="echo app(App\Services\FolderMapper::class)->getArchiveFolderPath();"`
</verification>

<success_criteria>
- Label CRUD with 10-color palette (create, edit, delete, color change)
- Labels applied/removed from messages via LabelService
- Label counts (unread/total) accurate per label
- Filter scope works: MessageMetadata::whereHas('labels', ...)
- Archive/Inbox label helpers ensure labels exist
- All tests pass
</success_criteria>

<output>
Create .planning/phases/04-organization-intelligence/04-04-SUMMARY.md when done
</output>