---
phase: 05-security-polish
plan: 04
type: execute
wave: 3
depends_on:
  - "03"
files_modified:
  - app/Providers/AppServiceProvider.php
  - app/Http/Middleware/SsrfProtection.php
  - app/Http/Requests/AttachmentRequest.php
  - config/openmail.php
  - config/session.php
  - app/Services/MessageSanitizer.php
  - routes/web.php
  - app/Http/Controllers/Auth/LoginController.php
  - app/Http/Controllers/Auth/LogoutController.php
autonomous: true
requirements:
  - SEC-04
  - SEC-06
  - SEC-07
  - SEC-08
  - SEC-09
user_setup: []
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med
must_haves:
  truths:
    - API endpoints (compose, search, settings) have rate limiting with per-user and per-IP segments
    - Session configuration enforces timeout, secure cookies, HTTP-only, SameSite=lax
    - Session rotation occurs on login and password change
    - All outbound URLs in message content validated against SSRF patterns
    - Attachment uploads validated against MIME allowlist with UUID-prefixed filenames
    - Dangerous URL schemes (javascript:, data:, file:) blocked in message links
    - Private IP ranges (localhost, 10.x, 192.168.x, etc.) blocked in URLs
  artifacts:
    - app/Providers/AppServiceProvider.php (modified with API rate limiters)
    - app/Http/Middleware/SsrfProtection.php
    - app/Http/Requests/AttachmentRequest.php
    - config/openmail.php (modified with attachments/security config)
    - config/session.php (modified for SEC-06)
    - app/Services/MessageSanitizer.php (modified with sanitizeUrls, sanitizeSignatureHtml)
    - routes/web.php (modified with throttle middleware on API routes)
    - app/Http/Controllers/Auth/LoginController.php (modified for session rotation)
    - app/Http/Controllers/Auth/LogoutController.php (modified for revokeOtherSessions)
  key_links:
    - AppServiceProvider rate limiters → routes/web.php throttle middleware
    - SsrfProtection middleware → MessageSanitizer::isDangerousUrl()
    - AttachmentRequest → config/openmail.php MIME allowlist → UUID filename storage
    - MessageSanitizer.sanitizeUrls() → email renderer → link blocking
    - config/session.php → Laravel session handler → secure cookies
---

<objective>
Implement API rate limiting, session hardening, SSRF-safe URL handling, and MIME/attachment validation per SEC-04, SEC-06, SEC-07, SEC-08, SEC-09.

Purpose: Harden all API endpoints against abuse, secure sessions against fixation/hijacking, prevent SSRF via message content, and validate attachments for type and safe storage.

Output: Rate limiter configs, SSRF middleware, attachment validation request, MIME allowlist config, session hardening config, MessageSanitizer extensions, and route middleware application.
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-security-polish/05-CONTEXT.md
@.planning/phases/05-security-polish/05-RESEARCH.md
@.planning/phases/05-security-polish/05-PATTERNS.md
@.planning/phases/05-security-polish/05-01-PLAN.md
@.planning/phases/05-security-polish/05-02-PLAN.md
@.planning/phases/05-security-polish/05-03-PLAN.md
</context>

<tasks>

<task id="04-tracer" type="execute">
  <name>End-to-end API security — rate limiters, SSRF middleware, attachment validation, session config, MessageSanitizer extensions</name>
  <files>
    - app/Providers/AppServiceProvider.php
    - app/Http/Middleware/SsrfProtection.php
    - app/Http/Requests/AttachmentRequest.php
    - config/openmail.php
    - config/session.php
    - app/Services/MessageSanitizer.php
    - routes/web.php
    - app/Http/Controllers/Auth/LoginController.php
    - app/Http/Controllers/Auth/LogoutController.php
  </files>
  <read_first>
    - app/Providers/AppServiceProvider.php
    - app/Http/Middleware/SsrfProtection.php
    - app/Http/Requests/AttachmentRequest.php
    - config/openmail.php
    - config/session.php
    - app/Services/MessageSanitizer.php
    - routes/web.php
    - app/Http/Controllers/Auth/LoginController.php
    - app/Http/Controllers/Auth/LogoutController.php
  </read_first>
  <action>
    Implement API security vertical slice per SEC-04, SEC-06, SEC-07, SEC-08, SEC-09. Implement: (1) Extend AppServiceProvider::boot() with RateLimiter::for() for 'api.compose' (30/min per user/IP), 'api.search' (60/min per user/IP), 'api.settings' (120/min per user/IP), 'api.authenticated' (100/min + 1000/hr per user) per RESEARCH.md Pattern; (2) Create SsrfProtection middleware using isDangerousUrl() from RESEARCH.md Pattern 5 — block javascript:/data:/vbscript:/file: schemes and private IPs (localhost, 127.x, 10.x, 192.168.x, 169.254.x, ::1); apply globally or to mailbox routes; (3) Create AttachmentRequest with File::types(config('openmail.attachments.allowed_mimes')) validation, max 25MB, using MIME content inspection not extension; (4) Extend config/openmail.php with attachments.allowed_mimes array (images, PDF, Office docs, text, archives) and max_size; security section with csp_enabled, csp_report_only, session_timeout; (5) Extend config/session.php: lifetime=120, expire_on_close=true, secure=true, same_site=lax, http_only=true per SEC-06; (6) Extend MessageSanitizer: add sanitizeSignatureHtml() reusing purifier, add sanitizeUrls() with DOMDocument link iteration calling isDangerousUrl(), add private isDangerousUrl() method; (7) Apply throttle middleware in routes/web.php: Route::middleware(['auth', 'throttle:api.authenticated'])->group() for compose, search, settings POST routes; (8) Ensure LoginController calls $request->session()->regenerate() on login (already in Phase 1); (9) Ensure LogoutController has revokeOtherSessions() deleting other sessions from sessions table.
  </action>
  <verify>
    <automated>php -r "require 'vendor/autoload.php'; $app = require 'bootstrap/app.php'; $app->make('Illuminate\Contracts\Console\Kernel')->bootstrap(); echo 'AppServiceProvider: ' . (class_exists('App\Providers\AppServiceProvider') ? 'exists' : 'missing') . PHP_EOL; echo 'SsrfProtection: ' . (class_exists('App\Http\Middleware\SsrfProtection') ? 'exists' : 'missing') . PHP_EOL; echo 'AttachmentRequest: ' . (class_exists('App\Http\Requests\AttachmentRequest') ? 'exists' : 'missing') . PHP_EOL;" && php artisan route:list | grep -E 'compose|search|settings' | grep -c throttle</automated>
    <fails_when>Rate limiters not in AppServiceProvider, SsrfProtection middleware missing, AttachmentRequest missing File::types validation, config/openmail.php missing attachments config, session config not hardened, throttle middleware not on API routes</fails_when>
  </verify>
  <done>
    - AppServiceProvider has 4 rate limiter configs (api.compose, api.search, api.settings, api.authenticated)
    - SsrfProtection middleware exists with isDangerousUrl() blocking dangerous schemes and private IPs
    - AttachmentRequest uses File::types() with config-driven MIME allowlist and 25MB max
    - config/openmail.php has attachments.allowed_mimes, max_size, security section
    - config/session.php has expire_on_close=true, secure=true, same_site=lax, http_only=true
    - MessageSanitizer has sanitizeSignatureHtml() and sanitizeUrls() with isDangerousUrl()
    - API routes (compose, search, settings) have throttle:api.authenticated middleware
    - LoginController regenerates session on login
    - LogoutController has revokeOtherSessions() method
  </done>
  <reversibility rating="reversible">Rate limiters configurable; middleware removable; session config via env; sanitizer methods additive</reversibility>
</task>

<task id="04-verify-rate-ssrf" type="verify">
  <name>Verify rate limiting, SSRF protection, and attachment validation</name>
  <files>
    - app/Providers/AppServiceProvider.php
    - app/Http/Middleware/SsrfProtection.php
    - app/Http/Requests/AttachmentRequest.php
    - config/openmail.php
    - config/session.php
    - routes/web.php
  </files>
  <read_first>
    - app/Providers/AppServiceProvider.php
    - app/Http/Middleware/SsrfProtection.php
    - app/Http/Requests/AttachmentRequest.php
    - config/openmail.php
    - config/session.php
    - routes/web.php
  </read_first>
  <action>
    Verify security controls: (1) Rate limiting: POST /composer/send 31 times rapidly → 31st returns 429; GET /search 61 times → 61st returns 429; authenticated user limits enforced per-user not per-IP; (2) SSRF: Message with javascript:alert(1) link → link rewritten to # with data-original-href; message with http://localhost/admin link → blocked; message with http://10.0.0.1 link → blocked; (3) Attachment: upload .exe file → rejected; upload valid PDF → accepted with UUID filename; upload 26MB file → rejected; (4) Session: verify secure/httponly/samesite flags on session cookie; verify session expires on browser close; (5) Session rotation: login → session ID changes; password change → session regenerates.
  </action>
  <verify>
    <automated>php artisan test --filter=ApiSecurityTest 2>/dev/null || echo "Manual: test rate limits with curl loop, test SSRF with malicious links, test attachment validation"</automated>
    <fails_when>Rate limits not enforced, SSRF URLs not blocked, invalid MIME accepted, session cookies not secure/httponly, session not rotating</fails_when>
  </verify>
  <done>
    - Compose endpoint returns 429 after 30 requests/minute per user
    - Search endpoint returns 429 after 60 requests/minute per user
    - Settings endpoint returns 429 after 120 requests/minute per user
    - javascript:/data:/file: links rewritten to # with data-original-href
    - Private IP URLs (localhost, 10.x, 192.168.x, etc.) blocked
    - Attachment validation rejects non-allowlisted MIME types
    - Attachment validation accepts allowlisted types with UUID prefix
    - Session cookie has Secure, HttpOnly, SameSite=Lax flags
    - Session regenerates on login and password change
  </done>
</task>

<task id="04-integration" type="execute">
  <name>Integrate SSRF protection and sanitized URLs into message rendering pipeline</name>
  <files>
    - app/Services/MessageSanitizer.php
    - resources/views/components/email-renderer.blade.php
    - app/Http/Controllers/MailboxController.php
  </files>
  <read_first>
    - app/Services/MessageSanitizer.php
    - resources/views/components/email-renderer.blade.php
    - app/Http/Controllers/MailboxController.php
  </read_first>
  <action>
    Wire SSRF protection into message flow: (1) In MailboxController or MessageViewer component, call MessageSanitizer::sanitizeUrls() on message HTML before passing to email-renderer; (2) Ensure email-renderer.blade.php receives sanitized HTML and renders in sandboxed iframe; (3) Add 'link-blocked' CSS style for blocked links (text-decoration: line-through, opacity: 0.5, cursor: not-allowed); (4) Add tooltip on blocked links showing "Link blocked for security" with original URL on hover; (5) Verify CSP allows iframe sandbox for email renderer.
  </action>
  <verify>
    <automated>php artisan test --filter=MessageSanitizerUrlTest 2>/dev/null || echo "Manual: view message with javascript: and private IP links, verify blocked styling"</automated>
    <fails_when>sanitizeUrls not called in message pipeline, blocked links not styled, iframe sandbox broken by CSP</fails_when>
  </verify>
  <done>
    - MessageSanitizer::sanitizeUrls() called on all rendered message HTML
    - Blocked links have .link-blocked class with visual indication
    - Tooltip shows original blocked URL on hover
    - Email renderer iframe still loads with CSP
    - No SSRF-vulnerable links render as clickable
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API Endpoints | Rate limited requests (compose, search, settings) |
| Message Content → Browser | Sanitized URLs prevent SSRF clicks |
| File Upload → Storage | MIME-validated, UUID-named attachments |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-12 | Denial of Service | API rate limiting bypass | high | mitigate | Per-user + per-IP segmented limits; database cache driver; authenticated stricter limits |
| T-05-13 | Spoofing | Session fixation | high | mitigate | session()->regenerate() on login; secure/httponly/samesite cookies; expire_on_close |
| T-05-14 | Tampering | SSRF via message links | critical | mitigate | SsrfProtection middleware + MessageSanitizer::sanitizeUrls() dual layer; block private IPs and dangerous schemes |
| T-05-15 | Elevation | Malicious attachment upload | high | mitigate | File::types() content inspection; UUID filenames; private storage; MIME allowlist config |
| T-05-16 | Information Disclosure | Session ID exposure | medium | mitigate | HTTP-only cookies; HTTPS only; short lifetime (120 min); rotation on auth events |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Package legitimacy audit in RESEARCH.md shows all approved; no new installs this phase |
</threat_model>

<verification>
Overall phase checks:
- All API endpoints return 429 when rate limit exceeded
- Session cookies have Secure, HttpOnly, SameSite=Lax
- Session regenerates on login and password change
- Message links with javascript:/data:/file: schemes neutralized
- Message links to private IPs (localhost, RFC1918) neutralized
- Attachment upload rejects non-allowlisted MIME types
- Attachment storage uses UUID prefix, not user filename
</verification>

<success_criteria>
API rate limiting active on compose/search/settings; sessions hardened per SEC-06; SSRF protection active on all message content; attachment validation enforces MIME allowlist with safe filenames.
</success_criteria>

<output>
Create .planning/phases/05-security-polish/05-04-SUMMARY.md when done
</output>