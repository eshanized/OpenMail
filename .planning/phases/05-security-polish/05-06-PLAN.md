---
phase: 05-security-polish
plan: 06
type: execute
wave: 5
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05"]
files_modified:
  - app/Livewire/Settings/SettingsPage.php
  - app/Livewire/Settings/AppearanceTab.php
  - resources/views/livewire/settings/settings-page.blade.php
  - resources/views/layouts/mailbox.blade.php
  - resources/css/app.css
  - config/openmail.php
  - config/csp.php
  - .env.example
  - app/Console/Commands/ToggleCspReportOnly.php
  - tests/Feature/IntegrationTest.php
  - .planning/phases/05-security-polish/CSP_VIOLATIONS.md
autonomous: true
requirements:
  - SEC-03
  - SET-03
  - SET-06
estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - Settings page fully polished with all 5 tabs functional
    - Density/theme applies consistently across all mailbox components
    - CSP violation log documented; enforcement flip ready
    - All integration tests pass
  artifacts:
    - Polished Settings page with loading states, unsaved changes warning, keyboard shortcuts
    - CSS vars driving all mailbox spacing/typography (no hardcoded light-mode colors)
    - CSP_VIOLATIONS.md documenting report-only violations
    - Artisan command csp:toggle-report-only for easy flip
    - .env.example with CSP production config
  key_links:
    - Density/theme CSS vars → all mailbox components → consistent UI
    - CSP report-only violations → CSP_VIOLATIONS.md → enforcement decision
    - Settings page → all user preferences → mailbox behavior

---

<objective>
Final polish for Settings page, verify density/theme integration across entire mailbox, prepare CSP for enforcement flip, and run full integration tests. This plan completes Phase 5 delivery.
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-security-polish/05-CONTEXT.md
@.planning/phases/05-security-polish/05-RESEARCH.md
@.planning/phases/05-security-polish/05-01-PLAN.md
@.planning/phases/05-security-polish/05-02-PLAN.md
@.planning/phases/05-security-polish/05-03-PLAN.md
@.planning/phases/05-security-polish/05-04-PLAN.md
@.planning/phases/05-security-polish/05-05-PLAN.md
@config/csp.php
@config/openmail.php
@resources/css/app.css
@resources/views/layouts/mailbox.blade.php
</context>

<tasks>

<task type="tracer">
  <name>End-to-End Full Integration Verification</name>
  <files>
    tests/Feature/IntegrationTest.php
    .planning/phases/05-security-polish/CSP_VIOLATIONS.md
  </files>
  <action>
    Run comprehensive integration test covering the full Phase 5 feature set:

    1. **Full Flow Integration Test** (tests/Feature/IntegrationTest.php):
       - Test: Login → verify audit log entry created (login success)
       - Test: Failed login → verify audit log entry (login failed)
       - Test: Lockout → verify audit log entry (lockout)
       - Test: Mailbox → Settings → Appearance → change theme → verify mailbox updates immediately
       - Test: Mailbox → Settings → Appearance → change density → verify mailbox updates immediately
       - Test: Settings → Signatures → create signature → set default → Compose → verify auto-insert
       - Test: Compose → signature dropdown → swap to another signature → verify replacement
       - Test: Compose → signature dropdown → select "None" → verify removal
       - Test: Settings → Profile → update name/email → verify persistence
       - Test: Settings → Mail → change page size → verify MessageList pagination updates
       - Test: Settings → Mail → change default folder → verify redirect behavior
       - Test: Settings → Mail → change reply behavior → verify Composer default
       - Test: Settings → Security → revoke session → verify target session invalidated
       - Test: Send email → verify audit log entry (send action)
       - Test: CSP headers present in all responses (report-only mode)
       - Test: Security headers present in all responses
       - Test: /csp-report endpoint logs violations

    2. **CSP Violation Documentation**:
       - Run application in report-only mode for test suite duration
       - Collect all CSP violations logged to security channel
       - Document each violation in .planning/phases/05-security-polish/CSP_VIOLATIONS.md with: directive, blocked-uri, source-file, line-number, frequency, assessment (expected/needs-fix)
       - Categorize: expected (Livewire/Alpine unsafe-inline fallbacks) vs unexpected (needs fixing before enforcement)

    3. **Performance Smoke Test**:
       - Verify page load times acceptable (<2s for mailbox, <1s for settings tabs)
       - Verify no memory leaks in long-running Livewire components
       - Verify audit_logs query performance with 1000+ records
  </action>
  <verify>
    <automated>php artisan test --filter=IntegrationTest::test_full_settings_flow</automated>
    <fails_when>non-zero exit, or any integration test step fails</fails_when>
    <automated>test -f .planning/phases/05-security-polish/CSP_VIOLATIONS.md</automated>
    <fails_when>non-zero exit (file missing)</fails_when>
    <automated>php artisan test --filter=IntegrationTest --stop-on-failure</automated>
    <fails_when>non-zero exit, or any integration test fails</fails_when>
  </verify>
  <done>
    Full integration test passes; CSP_VIOLATIONS.md documents all report-only violations with assessment; performance acceptable.
  </done>
  <reversibility>
    rating: reversible
    rationale: Integration tests only; documentation only; no code changes
  </reversibility>
</task>

<task type="auto">
  <name>Settings Page Polish + Density/Theme Cross-Mailbox Verification</name>
  <files>
    app/Livewire/Settings/SettingsPage.php
    app/Livewire/Settings/AppearanceTab.php
    resources/views/livewire/settings/settings-page.blade.php
    resources/views/layouts/mailbox.blade.php
    resources/css/app.css
  </files>
  <action>
    Final polish for Settings page and verify density/theme integration across all mailbox components:

    1. **Settings Page UX Polish**:
       - Add loading states to tab switches (wire:loading on tab panels)
       - Add unsaved changes warning (Alpine.js track dirty state, confirm on tab switch/leave)
       - Add keyboard shortcuts: Cmd/Ctrl+S to save active tab; Escape to close modals
       - Ensure mobile responsive: tabs scrollable on small screens; modals full-screen on mobile

    2. **Density/Theme Cross-Mailbox Verification**:
       - Test density classes (density-compact/regular/comfortable) applied to: message list rows, sidebar items, composer, message viewer, search results, contact sidebar, label sidebar
       - Verify CSS vars (--spacing-unit, --font-size-base, --line-height-base) drive all spacing/typography
       - Test dark mode: all components render correctly in dark (Tailwind dark: variants, CSS vars for custom colors)
       - Fix any hardcoded colors that don't adapt (replace with Tailwind semantic colors or CSS vars)

    3. **Settings Persistence Reliability**:
       - Verify Setting::setForUser/getForUser uses cache correctly (Setting model should cache per user)
       - Test: change theme → refresh page → theme persists
       - Test: change density → navigate to mailbox → density persists
       - Test: multiple tabs open → change in one → other tabs receive broadcast event (Livewire dispatch to window)

    4. **Accessibility Final Pass**:
       - All Settings tabs: proper heading hierarchy (h1→h2→h3)
       - Form labels associated with inputs
       - Focus management in modals (trap focus, restore on close)
       - Color contrast ratios meet WCAG AA in both light and dark modes
  </action>
  <verify>
    <automated>php artisan test --filter=SettingsTest</automated>
    <fails_when>non-zero exit, or any Settings tab test fails</fails_when>
    <automated>php artisan test --filter=IntegrationTest::test_density_theme_consistency</automated>
    <fails_when>non-zero exit, or density/theme not consistent across components</fails_when>
    <automated>grep -r "bg-white\|text-gray-900" resources/views/livewire/ | grep -v "dark:" | wc -l</automated>
    <fails_when>output > 10 (allowing some explicit light-mode only elements)</fails_when>
    <automated>php artisan test --filter=SettingsTest::test_accessibility_final</automated>
    <fails_when>non-zero exit, or accessibility issues found</fails_when>
  </verify>
  <done>
    Settings page fully polished with all 5 tabs functional; density/theme applies consistently across mailbox; no hardcoded light-mode colors; accessibility verified; persistence reliable.
  </done>
</task>

<task type="auto">
  <name>CSP Enforcement Preparation + Config Documentation</name>
  <files>
    config/csp.php
    config/openmail.php
    .env.example
    app/Console/Commands/ToggleCspReportOnly.php
  </files>
  <action>
    Prepare CSP for enforcement flip and document all configuration:

    1. **CSP Enforcement Preparation** (per D-01):
       - Review CSP_VIOLATIONS.md from tracer task: all violations assessed
       - Verify all inline scripts/styles have @cspNonceAttribute or are covered by unsafe-inline fallback
       - Create .env.example entries: CSP_ENABLED=true, CSP_REPORT_ONLY=false, CSP_REPORT_URI=/csp-report
       - Add Artisan command csp:toggle-report-only for easy flip:
         - php artisan csp:toggle-report-only --on  (sets CSP_REPORT_ONLY=false)
         - php artisan csp:toggle-report-only --off (sets CSP_REPORT_ONLY=true)
         - Reads/writes .env file using Laravel's Env::set()

    2. **Config Documentation**:
       - Update config/openmail.php with all new settings: attachments, CSP, security, appearance defaults
       - Document each env var with description and default
       - Add comments explaining CSP report-only → enforcement workflow

    3. **Production Deployment Checklist**:
       - Document in CSP_VIOLATIONS.md: "After 1-2 weeks monitoring with zero unexpected violations, run: php artisan csp:toggle-report-only --on"
       - Verify Vite manifest.json in public/build/.vite/manifest.json for production
       - Verify security log channel rotation configured (daily, 30-day retention)

    4. **Final Security Audit**:
       - Verify all SEC requirements have test coverage
       - Verify all STRIDE threats from Plans 1-5 have mitigations implemented
       - Run security-focused test suite: php artisan test --filter=Security
  </action>
  <verify>
    <automated>php artisan test --filter=SecurityTest</automated>
    <fails_when>non-zero exit, or any security test fails</fails_when>
    <automated>php artisan csp:toggle-report-only --off && grep CSP_REPORT_ONLY .env</automated>
    <fails_when>non-zero exit, or CSP_REPORT_ONLY not set to true</fails_when>
    <automated>php artisan csp:toggle-report-only --on && grep CSP_REPORT_ONLY .env</automated>
    <fails_when>non-zero exit, or CSP_REPORT_ONLY not set to false</fails_when>
    <automated>test -f .planning/phases/05-security-polish/CSP_VIOLATIONS.md && grep -c "## Violation" .planning/phases/05-security-polish/CSP_VIOLATIONS.md</automated>
    <fails_when>non-zero exit, or violation log missing or empty</fails_when>
    <automated>npm run build && test -f public/build/.vite/manifest.json</automated>
    <fails_when>non-zero exit, or production build missing manifest</fails_when>
  </verify>
  <done>
    CSP enforcement flip ready with documented violations and toggle command; all config documented in .env.example and config/openmail.php; production build verified; security audit complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → CSP Violation Endpoint | Automated browser reports POST violation JSON (no auth) |
| Admin → CSP Toggle Command | Trusted admin runs command to flip enforcement mode |
| Laravel → Database | Trusted application reads/writes all feature data |
| User → All Features | Authenticated user interacts with all Phase 5 features |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-27 | Tampering | CSP enforcement flip bypass | medium | mitigate | csp:toggle-report-only command requires CLI access; .env write protected on shared hosting |
| T-05-28 | Information Disclosure | CSP violation log exposure | low | mitigate | Violations logged to security channel only; no user-facing display |
| T-05-29 | Denial of Service | Settings page DoS via rapid tab switching | low | accept | Rate limited by api.settings limiter; Livewire request deduplication |
| T-05-30 | Tampering | Density/theme persistence corruption | low | accept | User-scoped settings; cache invalidation on write; localStorage fallback |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Package legitimacy audit completed; all packages [OK] |

</threat_model>

<verification>
Overall phase verification for Plan 6:
- Settings page fully polished with all 5 tabs functional
- Density/theme: applies to all mailbox components; no hardcoded light-mode colors; no flash on load
- Signatures: create → set default → compose auto-insert → dropdown swap works end-to-end
- Session revocation immediately invalidates target session
- Mail preferences affect mailbox behavior (pagination, default folder, reply logic)
- CSP: violation log documented; unsafe-inline exceptions minimal and documented; ready for enforcement flip
- All integration tests pass
- .env.example and config/openmail.php fully documented
</verification>

<success_criteria>
- All SettingsTest, SignatureTest, ComposerTest, IntegrationTest, SecurityTest pass
- Settings page complete with 5 tabs, all functional and polished
- Density/theme consistent across entire mailbox
- CSP_VIOLATIONS.md complete with enforcement decision guide
- csp:toggle-report-only command works
- Production build verified with Vite manifest
- Phase 5 success criteria from ROADMAP.md all met
</success_criteria>

<output>
Create .planning/phases/05-security-polish/05-06-SUMMARY.md when done
</output>