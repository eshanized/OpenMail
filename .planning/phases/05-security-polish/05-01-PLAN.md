---
phase: 05-security-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/csp.php
  - app/Support/Csp/LaravelViteNonceGenerator.php
  - app/Support/Csp/OpenMailPreset.php
  - app/Support/Csp/OpenMailReportOnlyPreset.php
  - app/Http/Middleware/SecurityHeaders.php
  - bootstrap/app.php
  - app/Http/Controllers/CspReportController.php
  - routes/web.php
  - database/migrations/2026_09_08_000001_create_audit_logs_table.php
  - app/Models/AuditLog.php
  - app/Services/AuditService.php
  - app/Http/Controllers/Auth/LoginController.php
  - app/Console/Commands/PruneAuditLogs.php
  - app/Console/Kernel.php
  - app/Livewire/Settings/SettingsPage.php
  - app/Livewire/Settings/AppearanceTab.php
  - resources/views/livewire/settings/settings-page.blade.php
  - resources/views/livewire/settings/appearance-tab.blade.php
  - resources/views/layouts/app.blade.php
  - resources/css/app.css
  - resources/js/app.js
  - config/openmail.php
autonomous: true
requirements:
  - SEC-03
  - SEC-05
  - SEC-10
  - SET-03
  - SET-06
estimate:
  tokens: 65000
  raw_tokens: 32500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - CSP headers are present in HTTP responses with report-only mode enabled
    - CSP violations are POSTed to /csp-report and logged to security channel
    - Security headers (X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy) present on all responses
    - Audit logs record login success, login failure, and lockout events
    - User can toggle theme (light/dark/system) and density (compact/regular/comfortable) in Settings > Appearance
    - Theme/density preferences persist to database and apply immediately without page reload
  artifacts:
    - config/csp.php with report-only config and Vite nonce generator
    - app/Http/Middleware/SecurityHeaders.php applying SEC-10 headers
    - app/Http/Controllers/CspReportController.php handling violation reports
    - database audit_logs table with indexes and 90-day retention
    - app/Services/AuditService.php with explicit logging methods
    - app/Livewire/Settings/SettingsPage.php with tabbed navigation
    - app/Livewire/Settings/AppearanceTab.php with theme/density controls
    - CSS custom properties for density and dark mode in resources/css/app.css
    - Alpine.js theme/density initializer in resources/views/layouts/app.blade.php
  key_links:
    - CSP nonce generation → Vite manifest → production asset loading
    - AuditService → LoginController → security log channel → audit_logs table
    - Theme/density CSS vars on <html> → Livewire component styles → mailbox UI
    - Settings page → Setting model → per-user preferences → immediate UI update

---

<objective>
Establish security infrastructure foundation: CSP headers in report-only mode with Vite nonce integration, security headers middleware, audit logging for authentication events, and a working Settings page with Appearance tab (theme/density controls) that demonstrates end-to-end preference persistence and live UI updates.

Purpose: This tracer plan proves the security headers pipeline works (CSP report-only → violation reporting → security logging), the audit logging infrastructure captures auth events correctly, and the Settings page architecture supports tabbed navigation with reactive Alpine.js + Livewire preference controls that persist to database and apply instantly via CSS custom properties.

Output: Working CSP in report-only mode, security headers on all responses, audit_logs table with login event logging, Settings page with functional Appearance tab (theme toggle + density picker with live preview).
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-security-polish/05-CONTEXT.md
@.planning/phases/05-security-polish/05-RESEARCH.md
@.planning/phases/04-organization-intelligence/04-CONTEXT.md
@app/Providers/AppServiceProvider.php
@app/Http/Middleware/InstallLock.php
@app/Services/MessageSanitizer.php
@app/Models/Setting.php
@resources/views/layouts/mailbox.blade.php
@resources/views/layouts/app.blade.php
@config/openmail.php
</context>

<tasks>

<task type="tracer">
  <name>End-to-End Security Headers + Audit Logging + Settings Appearance Tab</name>
  <files>
    config/csp.php
    app/Support/Csp/LaravelViteNonceGenerator.php
    app/Support/Csp/OpenMailPreset.php
    app/Support/Csp/OpenMailReportOnlyPreset.php
    app/Http/Middleware/SecurityHeaders.php
    bootstrap/app.php
    app/Http/Controllers/CspReportController.php
    routes/web.php
    database/migrations/2026_09_08_000001_create_audit_logs_table.php
    app/Models/AuditLog.php
    app/Services/AuditService.php
    app/Http/Controllers/Auth/LoginController.php
    app/Console/Commands/PruneAuditLogs.php
    app/Console/Kernel.php
    app/Livewire/Settings/SettingsPage.php
    app/Livewire/Settings/AppearanceTab.php
    resources/views/livewire/settings/settings-page.blade.php
    resources/views/livewire/settings/appearance-tab.blade.php
    resources/views/layouts/app.blade.php
    resources/css/app.css
    resources/js/app.js
    config/openmail.php
  </files>
  <action>
    Implement the thinnest vertical slice proving CSP + Security Headers + Audit Logging + Settings Appearance tab work together:

    1. **CSP Configuration (per D-01, D-02, D-03, D-04)**:
       - Create config/csp.php with report_only=true, report_uri=/csp-report
       - Implement LaravelViteNonceGenerator using Vite::useCspNonce() for automatic nonce injection
       - Create OpenMailPreset with: base-uri 'self', form-action 'self', object-src 'none', frame-ancestors 'none', script-src 'self' + nonce + 'unsafe-inline' (fallback for Livewire 3/Alpine), style-src 'self' + nonce + 'unsafe-inline', img-src 'self' data: https:, connect-src 'self', font-src 'self' fonts.gstatic.com, media-src 'self'
       - Create OpenMailReportOnlyPreset for stricter testing (same but without unsafe-inline fallback)
       - Register spatie/laravel-csp middleware globally in bootstrap/app.php

    2. **Security Headers Middleware (SEC-10, per D-08 scope)**:
       - Create app/Http/Middleware/SecurityHeaders.php setting: X-Frame-Options: DENY, X-Content-Type-Options: nosniff, Referrer-Policy: strict-origin-when-cross-origin, Permissions-Policy: camera=(), microphone=(), geolocation=()
       - Add HSTS header when request is secure or config('app.force_https')
       - Register globally in bootstrap/app.php middleware stack (after CSP middleware)

    3. **CSP Violation Report Endpoint (per D-03)**:
       - Create app/Http/Controllers/CspReportController.php with store() method
       - Accept JSON POST, extract csp-report fields, log to Log::channel('security')->warning('CSP Violation', [...])
       - Return 204 no content
       - Route POST /csp-report with throttle:60,1 middleware

    4. **Audit Logging Infrastructure (per D-05, D-06, D-07, D-08)**:
       - Create migration for audit_logs table: id, user_id (nullable FK), event_type (string, indexed), description (text), ip (string), user_agent (text), metadata (JSON), created_at; indexes on user_id, event_type, created_at
       - Create app/Models/AuditLog.php with $fillable, $casts for metadata->array, user() belongsTo
       - Create app/Services/AuditService.php with static log() method and convenience methods: loginSuccess(), loginFailed(), lockout(), send() per D-06 scope
       - Create app/Console/Commands/PruneAuditLogs.php deleting records older than 90 days (configurable)
       - Register prune command in app/Console/Kernel.php schedule()->daily()

    5. **LoginController Integration (SEC-05, per D-08)**:
       - Update app/Http/Controllers/Auth/LoginController.php to call AuditService::loginSuccess() on successful auth
       - Call AuditService::loginFailed() on failed attempts with reason (invalid_credentials, throttled, etc.)
       - Call AuditService::lockout() when rate limiter triggers lockout

    6. **Settings Page Scaffold + Appearance Tab (per D-09, D-10, D-11, D-12)**:
       - Create app/Livewire/Settings/SettingsPage.php with tabs: profile, mail, appearance, security, signatures; activeTab state; setTab() method; #[Layout('layouts.mailbox')]
       - Create app/Livewire/Settings/AppearanceTab.php with theme (light/dark/system) and density (compact/regular/comfortable) properties loaded from Setting model in mount()
       - On updatedTheme/updatedDensity: persist via Setting::set() and dispatch browser events (theme-changed, density-changed)
       - Create resources/views/livewire/settings/settings-page.blade.php with tab navigation (role=tablist) and @switch for tab panels
       - Create resources/views/livewire/settings/appearance-tab.blade.php with radio groups for theme/density, live preview section using Alpine.js appearancePreview() component
       - Add route GET /settings → SettingsPage::class in routes/web.php (auth middleware)

    7. **Theme/Density CSS + Client Initialization (per D-10, D-11)**:
       - Extend resources/css/app.css: add @theme block with CSS custom properties (--spacing-unit, --font-size-base, --line-height-base, --border-radius) for regular density; .density-compact, .density-comfortable, .density-regular utility classes using @utility; @custom-variant dark (&:where(.dark, .dark *))
       - Update resources/views/layouts/app.blade.php: add @cspNonceMetaTag, inline themeInitializer() Alpine.js component on <html> with x-data/x-init that reads server-rendered user settings via @json(auth()->user()?->setting(...)) and applies dark class + density-* class immediately to prevent flash
       - Extend resources/js/app.js to import/initialize theme/density if needed

    8. **Config Extensions**:
       - Add CSP_REPORT_URI, CSP_ENABLED, CSP_REPORT_ONLY to config/openmail.php with env() defaults
  </action>
  <verify>
    <automated>php artisan migrate --force && php artisan test --filter=CspTest</automated>
    <fails_when>non-zero exit, or "0 passed" in summary, or CSP header missing from response</fails_when>
    <automated>php artisan test --filter=SecurityHeadersTest</automated>
    <fails_when>non-zero exit, or X-Frame-Options/X-Content-Type-Options/Referrer-Policy missing from response</fails_when>
    <automated>php artisan test --filter=AuditLogTest::test_auth_logging</automated>
    <fails_when>non-zero exit, or audit_logs table missing login success/failure records after test login</fails_when>
    <automated>php artisan test --filter=SettingsTest::test_theme</automated>
    <fails_when>non-zero exit, or theme/density not persisted to settings table, or CSS vars not applied to <html></fails_when>
  </verify>
  <done>
    CSP headers present in response with report-only mode; violations POST to /csp-report and appear in security log; SecurityHeaders middleware adds X-Frame-Options: DENY, X-Content-Type-Options: nosniff, Referrer-Policy: strict-origin-when-cross-origin; audit_logs table created with login events recorded on auth; Settings page accessible at /settings with Appearance tab showing theme/density controls that persist to DB and update UI live via CSS vars.
  </done>
  <reversibility>
    rating: reversible
    rationale: CSP report-only mode is configurable; security headers are middleware; audit_logs table is additive; Settings page is new route/component; all can be disabled/removed without data loss
  </reversibility>
</task>

<task type="auto">
  <name>CSP Production Hardening & Violation Monitoring</name>
  <files>
    config/csp.php
    app/Support/Csp/OpenMailPreset.php
    app/Support/Csp/OpenMailReportOnlyPreset.php
    resources/views/layouts/app.blade.php
    resources/views/components/email-renderer.blade.php
  </files>
  <action>
    Harden CSP configuration for production readiness and ensure all inline scripts/styles have nonce support:

    1. **Refine CSP Presets** (per D-02, D-04):
       - Audit all inline scripts/styles in codebase (grep for @cspNonceAttribute, onclick, onload, style= attributes)
       - Update OpenMailPreset to use nonce for script-src and style-src where Vite manages assets
       - Keep 'unsafe-inline' fallback only for Livewire 3 component hydration scripts and Alpine.js inline handlers that cannot yet be nonce-wrapped
       - Document each unsafe-inline exception with TODO comment for future removal

    2. **Email Renderer CSP Compatibility** (SEC-01, SEC-02):
       - Update resources/views/components/email-renderer.blade.php to use @cspNonceAttribute on the sandboxed iframe's srcdoc content if scripts are injected
       - Verify DOMPurify client-side sanitization works within CSP constraints (no inline scripts in sanitized HTML)
       - Ensure frame-src 'none' in CSP doesn't block the sandboxed iframe (use sandbox attribute instead)

    3. **CSP Report-Only Monitoring Setup**:
       - Verify /csp-report endpoint receives and logs violations correctly
       - Add Log::channel('security') configuration in config/logging.php if not present (single-line daily file)
       - Document process: monitor report-only violations for 1-2 weeks, then flip CSP_REPORT_ONLY=false in .env

    4. **Vite Build Integration**:
       - Verify vite.config.js outputs manifest.json to public/build/.vite/manifest.json
       - Test npm run build produces valid manifest for nonce generator
       - Ensure @vite directives in Blade templates include CSP nonce automatically via spatie/laravel-csp
  </action>
  <verify>
    <automated>php artisan test --filter=CspTest::test_nonce_generation</automated>
    <fails_when>non-zero exit, or nonce not present in script/style tags in production build</fails_when>
    <automated>php artisan test --filter=CspTest::test_report_only_logging</automated>
    <fails_when>non-zero exit, or CSP violations not logged to security channel</fails_when>
    <automated>npm run build && test -f public/build/.vite/manifest.json</automated>
    <fails_when>non-zero exit, or manifest.json missing</fails_when>
    <automated>grep -r "unsafe-inline" config/csp.php app/Support/Csp/ | grep -v "TODO\|FIXME" | wc -l</automated>
    <fails_when>output > 2 (allowing only Livewire/Alpine fallback exceptions)</fails_when>
  </verify>
  <done>
    CSP presets refined with minimal unsafe-inline exceptions (documented); email renderer works within CSP; violation reporting functional; Vite manifest generated; report-only monitoring ready for production flip.
  </done>
</task>

<task type="auto">
  <name>Settings Page Foundation & Dark Mode Integration</name>
  <files>
    app/Livewire/Settings/SettingsPage.php
    app/Livewire/Settings/AppearanceTab.php
    resources/views/livewire/settings/settings-page.blade.php
    resources/views/livewire/settings/appearance-tab.blade.php
    resources/views/layouts/mailbox.blade.php
    resources/css/app.css
    resources/views/layouts/app.blade.php
    app/Models/Setting.php
  </files>
  <action>
    Complete the Settings page foundation and ensure theme/density integration works across the entire mailbox UI:

    1. **Settings Page Layout Integration**:
       - Update resources/views/layouts/mailbox.blade.php to conditionally render SettingsPage when on settings route (or use dedicated settings layout extending mailbox layout)
       - Ensure Settings page inherits sidebar/navigation from mailbox layout for consistency
       - Add breadcrumb/back-link to mailbox in Settings page header

    2. **Appearance Tab Polish** (per D-10, D-11, D-12):
       - Enhance appearance-tab.blade.php live preview to show actual message row density changes (reuse message-row component styles)
       - Add transition animations for theme/density changes (Alpine.js x-transition)
       - Ensure radio button selection updates are debounced (Alpine.js $watch with 300ms debounce) before Livewire persist call
       - Add accessibility: proper labels, keyboard navigation, ARIA attributes on tab list

    3. **CSS Density Integration Across Mailbox**:
       - Audit all spacing/padding in mailbox components (message-row, sidebar-item, composer, message-viewer)
       - Replace fixed Tailwind padding utilities with CSS var references where density should apply
       - Example: .message-row { padding: var(--spacing-unit) 1rem; font-size: var(--font-size-base); }
       - Add density-* utility classes to mailbox container in mailbox.blade.php based on user setting

    4. **Setting Model Verification**:
       - Verify app/Models/Setting.php supports user-scoped keys (user_id + key unique index)
       - If Setting is currently global-only, add user_id column via migration and update model
       - Add scopeSetting($key, $default) helper method to User model for clean access

    5. **Theme Flash Prevention** (per RESEARCH.md Pitfall 3):
       - Verify inline themeInitializer in app.blade.php reads server-rendered settings before paint
       - Test: login as user with dark preference → refresh → no light flash
       - Fallback: if user not authenticated, read from localStorage
  </action>
  <verify>
    <automated>php artisan test --filter=SettingsTest::test_theme</automated>
    <fails_when>non-zero exit, or theme not applied on page load (flash detected via DOM snapshot)</fails_when>
    <automated>php artisan test --filter=SettingsTest::test_density</automated>
    <fails_when>non-zero exit, or density CSS vars not reflected in message-row/sidebar-item computed styles</fails_when>
    <automated>php artisan test --filter=SettingsTest::test_appearance_tab</automated>
    <fails_when>non-zero exit, or Appearance tab not accessible, or live preview not updating</fails_when>
    <automated>grep -r "p-[0-9]" resources/views/livewire/mailbox/ | grep -v "var(--spacing" | wc -l</automated>
    <fails_when>output > 5 (allowing some fixed padding for non-density elements)</fails_when>
  </verify>
  <done>
    Settings page fully integrated with mailbox layout; Appearance tab polished with accessible controls and live preview; density CSS vars applied across mailbox components; theme flash eliminated; Setting model supports per-user preferences.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Laravel (HTTP) | Untrusted client requests hit routes, middleware, controllers |
| Browser → CSP Violation Endpoint | Automated browser reports POST violation JSON (no auth) |
| Laravel → Database | Trusted application writes audit logs, settings |
| Laravel → IMAP/SMTP | Trusted application connects to mail server with user credentials |
| User → Settings UI | Authenticated user modifies own preferences |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | CSP nonce generation | high | mitigate | Use spatie/laravel-csp Vite nonce generator; nonces are cryptographically random per-request; verify in test |
| T-05-02 | Tampering | CSP violation reports | medium | mitigate | Throttle /csp-report at 60/min/IP; log only, no reflection; validate JSON structure before logging |
| T-05-03 | Spoofing | Audit log injection | low | mitigate | AuditService::log() called only from trusted server code (LoginController, ComposerService); no user input in event_type/description |
| T-05-04 | Information Disclosure | Audit log PII exposure | medium | mitigate | AuditService excludes passwords; IMAP password encrypted in session; log channel 'security' separate from default |
| T-05-05 | Tampering | Theme/density persistence | low | accept | User controls own preferences via authenticated Livewire component; Setting model scopes to user_id |
| T-05-06 | Denial of Service | Audit log table growth | medium | mitigate | 90-day retention via scheduled PruneAuditLogs command; indexes on created_at for efficient deletion |
| T-05-07 | Tampering | CSP bypass via unsafe-inline | high | mitigate | Report-only mode first; monitor violations; only Livewire/Alpine fallback uses unsafe-inline; documented for removal |
| T-05-08 | Elevation of Privilege | Settings access control | low | mitigate | Settings routes behind auth middleware; Livewire components check auth()->check(); user_id scoping on Setting model |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Package legitimacy audit completed in RESEARCH.md; all packages [OK]; no new installs in this phase |

</threat_model>

<verification>
Overall phase verification for Plan 1:
- CSP headers present in all responses (report-only mode)
- Security headers present in all responses
- /csp-report endpoint accepts POST, logs to security channel, returns 204
- audit_logs table exists with proper indexes; PruneAuditLogs command registered
- Login success/failure/lockout events create audit log entries
- Settings page at /settings renders with tabbed navigation
- Appearance tab: theme toggle (light/dark/system) and density picker (compact/regular/comfortable) work
- Preferences persist to settings table per user
- Theme/density apply immediately via CSS vars on <html> without page reload
- No theme flash on page load (inline initializer in app.blade.php)
- Vite build produces manifest.json for nonce generation
</verification>

<success_criteria>
- All tracer task verify commands pass
- Security headers middleware registered and functional
- CSP in report-only mode with violation reporting
- Audit logging captures auth events per SEC-05 scope
- Settings page Appearance tab fully functional with live preview
- Theme/density CSS vars drive mailbox UI spacing/typography
</success_criteria>

<output>
Create .planning/phases/05-security-polish/05-01-SUMMARY.md when done
</output>