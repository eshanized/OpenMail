---
phase: 05-security-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/csp.php
  - app/Support/Csp/LaravelViteNonceGenerator.php
  - app/Support/Csp/OpenMailPreset.php
  - app/Support/Csp/OpenMailReportOnlyPreset.php
  - app/Http/Controllers/CspReportController.php
  - app/Http/Middleware/SecurityHeaders.php
  - bootstrap/app.php
  - routes/web.php
autonomous: true
requirements:
  - SEC-03
  - SEC-10
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 3
  confidence: med
must_haves:
  truths:
    - CSP headers are sent on all responses in report-only mode
    - CSP violation reports POST to /csp-report and log to security channel
    - Security headers (X-Frame-Options, X-Content-Type-Options, Referrer-Policy) present on all responses
    - Nonce-based script/style loading works with Vite integration
    - Livewire 3 and Alpine.js function correctly with unsafe-inline fallback
  artifacts:
    - config/csp.php
    - app/Support/Csp/LaravelViteNonceGenerator.php
    - app/Support/Csp/OpenMailPreset.php
    - app/Support/Csp/OpenMailReportOnlyPreset.php
    - app/Http/Controllers/CspReportController.php
    - app/Http/Middleware/SecurityHeaders.php
    - bootstrap/app.php (modified)
    - routes/web.php (modified with /csp-report route)
  key_links:
    - CSP config → Vite nonce generator → Livewire/Alpine script execution
    - SecurityHeaders middleware → global response headers
    - CspReportController → security log channel → violation monitoring
---

<objective>
Implement Content Security Policy infrastructure with report-only mode, Vite nonce integration, and security headers middleware per D-01 through D-04 and D-10.

Purpose: Establish CSP foundation that protects against XSS while allowing Livewire 3 and Alpine.js to function via nonce + unsafe-inline fallback. Report-only mode enables violation monitoring before enforcement.

Output: CSP configuration, nonce generator, presets, violation endpoint, security headers middleware, and route registration.
</objective>

<execution_context>
@/home/snigdha/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/snigdha/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-security-polish/05-CONTEXT.md
@.planning/phases/05-security-polish/05-RESEARCH.md
@.planning/phases/05-security-polish/05-PATTERNS.md
</context>

<tasks>

<task id="01-tracer" type="execute">
  <name>End-to-end CSP infrastructure — config, nonce generator, presets, violation endpoint, security headers middleware</name>
  <files>
    - config/csp.php
    - app/Support/Csp/LaravelViteNonceGenerator.php
    - app/Support/Csp/OpenMailPreset.php
    - app/Support/Csp/OpenMailReportOnlyPreset.php
    - app/Http/Controllers/CspReportController.php
    - app/Http/Middleware/SecurityHeaders.php
    - bootstrap/app.php
    - routes/web.php
  </files>
  <read_first>
    - config/csp.php
    - app/Support/Csp/LaravelViteNonceGenerator.php
    - app/Support/Csp/OpenMailPreset.php
    - app/Support/Csp/OpenMailReportOnlyPreset.php
    - app/Http/Controllers/CspReportController.php
    - app/Http/Middleware/SecurityHeaders.php
    - bootstrap/app.php
    - routes/web.php
  </read_first>
  <action>
    Create CSP infrastructure as a vertical slice per D-01 through D-04 and SEC-10. Implement: (1) config/csp.php with report-only mode, Vite nonce generator reference, and preset configuration; (2) LaravelViteNonceGenerator implementing Spatie\Csp\Nonce\NonceGenerator using Vite::useCspNonce(); (3) OpenMailPreset with script-src/style-src nonces + unsafe-inline fallback for Livewire 3/Alpine, img-src self/data:/https:, connect-src self, font-src self/fonts.gstatic.com; (4) OpenMailReportOnlyPreset for stricter testing policy; (5) CspReportController logging violations to security channel per D-03; (6) SecurityHeaders middleware adding X-Frame-Options: DENY, X-Content-Type-Options: nosniff, Referrer-Policy: strict-origin-when-cross-origin, Permissions-Policy, and HSTS for HTTPS per SEC-10; (7) Register SecurityHeaders globally in bootstrap/app.php; (8) Add POST /csp-report route with throttle middleware in routes/web.php.
  </action>
  <verify>
    <automated>php artisan route:list | grep csp-report && php -r "require 'vendor/autoload.php'; $app = require 'bootstrap/app.php'; $app->make('Illuminate\Contracts\Console\Kernel')->bootstrap(); echo 'CSP config: ' . (config('csp.enabled') ? 'enabled' : 'report-only') . PHP_EOL;"</automated>
    <fails_when>Route /csp-report not registered, CSP config missing, SecurityHeaders middleware not in global stack, nonce generator not returning Vite nonce</fails_when>
  </verify>
  <done>
    - config/csp.php exists with report_only=true, nonce_generator set, presets configured
    - LaravelViteNonceGenerator implements NonceGenerator and returns Vite::useCspNonce()
    - OpenMailPreset configures all required directives with nonces and unsafe-inline fallback
    - CspReportController accepts POST, logs to security channel, returns 204
    - SecurityHeaders middleware sets all SEC-10 headers on every response
    - SecurityHeaders registered globally in bootstrap/app.php
    - POST /csp-report route exists with throttle:60,1 middleware
  </done>
  <reversibility rating="reversible">CSP config toggles via env vars; middleware removable; no schema changes</reversibility>
</task>

<task id="01-verify-csp" type="verify">
  <name>Verify CSP headers and violation reporting end-to-end</name>
  <files>
    - config/csp.php
    - app/Http/Controllers/CspReportController.php
    - app/Http/Middleware/SecurityHeaders.php
    - bootstrap/app.php
    - routes/web.php
  </files>
  <read_first>
    - config/csp.php
    - app/Http/Controllers/CspReportController.php
    - app/Http/Middleware/SecurityHeaders.php
    - bootstrap/app.php
    - routes/web.php
  </read_first>
  <action>
    Verify CSP implementation: (1) Check response headers contain Content-Security-Policy-Report-Only with nonce directives; (2) Verify X-Frame-Options, X-Content-Type-Options, Referrer-Policy present; (3) POST test violation to /csp-report and confirm security log entry; (4) Confirm Livewire component renders without CSP errors in browser console; (5) Verify Vite nonce injection works in production build.
  </action>
  <verify>
    <automated>php artisan test --filter=CspTest 2>/dev/null || echo "Test file not yet created - manual verification required: curl -I /mailbox | grep -i content-security-policy; curl -X POST /csp-report -H 'Content-Type: application/json' -d '{\"csp-report\":{\"blocked-uri\":\"test\",\"violated-directive\":\"script-src\"}}' -w '%{http_code}'"</automated>
    <fails_when>CSP header missing, security headers missing, violation endpoint returns non-204, Livewire broken by CSP</fails_when>
  </verify>
  <done>
    - Content-Security-Policy-Report-Only header present on mailbox page
    - Header contains nonce-* for script-src and style-src
    - Header contains 'unsafe-inline' fallback for script-src and style-src
    - All SEC-10 headers present (X-Frame-Options: DENY, X-Content-Type-Options: nosniff, Referrer-Policy)
    - POST /csp-report returns 204 and writes to storage/logs/security.log
    - Livewire components hydrate and Alpine.js interactions work
  </done>
</task>

<task id="01-docs" type="execute">
  <name>Document CSP configuration and violation monitoring procedure</name>
  <files>
    - config/csp.php
    - .env.example
    - .planning/phases/05-security-polish/05-CONTEXT.md
  </files>
  <read_first>
    - config/csp.php
    - .planning/phases/05-security-polish/05-CONTEXT.md
  </read_first>
  <action>
    Add inline documentation to config/csp.php explaining: report-only vs enforce mode toggle via CSP_ENABLED/CSP_REPORT_ONLY env vars; how to monitor security.log for violations; timeline for switching to enforce mode after violation review. Update .env.example with CSP_ENABLED=false, CSP_REPORT_ONLY=true, CSP_REPORT_URI=/csp-report.
  </action>
  <verify>
    <automated>grep -c "CSP_ENABLED\|CSP_REPORT_ONLY\|CSP_REPORT_URI" .env.example</automated>
    <fails_when>Env vars not documented in .env.example, config/csp.php lacks inline comments for mode toggle</fails_when>
  </verify>
  <done>
    - config/csp.php has inline comments for all settings
    - .env.example contains CSP_ENABLED, CSP_REPORT_ONLY, CSP_REPORT_URI with defaults
    - Documentation explains violation monitoring and enforcement transition
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Laravel | CSP violation reports from browser POST to /csp-report endpoint |
| Browser → Laravel | All HTTP responses carry CSP and security headers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | CSP violation report endpoint | medium | mitigate | Rate limit /csp-report (60/min), validate JSON structure, log only - no state change |
| T-05-02 | Spoofing | CSP nonce generation | high | mitigate | Use Vite::useCspNonce() cryptographically random nonce per request |
| T-05-03 | Elevation | Security headers missing | high | mitigate | Global middleware in bootstrap/app.php ensures headers on every response |
| T-05-04 | Information Disclosure | CSP violation logs | low | accept | Logs contain blocked URI, directive, source file, line - no sensitive data |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Package legitimacy audit in RESEARCH.md shows all approved; no new installs this phase |
</threat_model>

<verification>
Overall phase checks:
- CSP report-only header present on all authenticated pages
- Security headers present on all responses
- Violation endpoint accepts and logs reports
- Livewire 3 + Alpine.js fully functional
- No console CSP errors in development
</verification>

<success_criteria>
CSP infrastructure operational in report-only mode with violation monitoring; security headers globally applied; ready for enforcement after violation review period.
</success_criteria>

<output>
Create .planning/phases/05-security-polish/05-01-SUMMARY.md when done
</output>